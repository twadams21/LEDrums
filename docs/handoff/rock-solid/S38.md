# S38 — Signal previews + param-row ticks (group I, issue #57) — implementer report

Branch `slice/S38` off `group/I`. **Sweep: `pnpm typecheck` clean (6 projects) · `pnpm test`
1611 passed / 0 failed / 0 skipped** (core 532, web 889 +36, server 176, io 13, protocol 1).

## What changed
- **Web core (pure, testable)** `trigger-lab/signal-preview.ts`: `envelopeTrace`/`lfoTrace`/
  `ccPreviewValue`/`paramRowSignal` + `previewCtx`, all sampled through core
  `sampleEnvelope`/`sampleLfo`/`sampleCc`/`sampleSource` (never a reimplemented waveform).
  Envelope cursor loops `PREVIEW_LOOP_MS`; LFO cursor `frac(t/period)` with value ≡ `sampleLfo(t)`;
  `PREVIEW_STATIC_MS` = reduced-motion frame. Unit-tested (16).
- **Components**: `SignalFace.svelte` — reusable canvas primitive owning the SHARED
  `effect-thumb-ticker` (no new rAF) + IntersectionObserver + reduced-motion (one static frame);
  factored from EffectThumb's machinery. `NodeSignalPreview.svelte` (envelope/LFO trace + phase
  cursor; CC value bar + 0..127 readout) and `ParamRowTick.svelte` (row live tick). Ticker-wiring +
  reduced-motion policy tested (SignalFace.test, 3).
- **Wiring**: `TriggerNode` — source nodes (envelope/lfo/cc) show a face preview in the card thumb
  slot; each exposed param row gets a live `ParamRowTick`. Store: `modSourcesFor`, `ccNodeLiveValue`,
  `liveCcTable`. Styleguide: Signal-previews DemoCard + contract note; `docs/design-system.html` regen.

## Deviations
- Param-row tick shows the mean post-invert SOURCE signal (0..1) — the "is/how-hard it's driving"
  indicator, deliberately independent of each mapping's amount/range (the compositor owns the final
  param value). CC bar reads the sim mirror (`sim.ccTable`), the offline twin of the engine table.

Sweep: `pnpm typecheck` && `pnpm test` — green, no skips.

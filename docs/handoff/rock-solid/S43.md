# S43 — clipdoc module (group K, #55) — implementer report

Branch `slice/S43` off `group/K`. Pure ClipDoc module + tests ONLY — no UI (S44), no
setProject/server (S45), `packages/core` untouched. New: `apps/web/src/lib/trigger-lab/clipdoc.ts`
— versioned envelope, defensive parse, remap-on-materialize. Closure built via SHARED
`extractSongClosure` then un-namespaced (envelope carries source RAW ids → built-ins recognizable).

## Acceptance — all met
- [x] Serialize/parse round-trip per kind; malformed/foreign/bad-version/bad-kind ⇒ typed error, never a throw — `clipdoc.test.ts` round-trip (4) + defensive (12).
- [x] Remap A→B→A no-dup; double-paste exactly one dup set; modulation `param:`/modifier `mod` ports survive (node ids + ports verbatim); built-in ids never re-keyed even on content diff — remap suite (10).
- [x] Closure equivalence with S40 `extractSongClosure` via shared code, verified by test.

## Gates
typecheck 0 (6 pkgs) · test 1718 / 0 failed / 0 skipped (core 533 / io 13 / protocol 1 / server 185 / web 986; +26) · commits `S43: clipdoc module`, `S43: clipdoc tests`.

## Deviations
- Reuse is CONTENT-based (deep-equal), not id-based — the only reading that makes A→B→A dup-free.
- Effect ids are name-derived (`freshEffectId`): re-key only on collision, else the readable id stays (`nid()` is the graph/section/song/preset path). Matches doc 11 reuse intent.

## Context pack for S44 (UI) AND S45 (patch)
- **Serialize:** `buildGraphClipDoc(key, sources)` / `buildSectionClipDoc(section, sources)` / `buildSongClipDoc(song, sources)` / `buildPatchClipDoc(patch)` → `serialize(doc)`. `sources: ClosureSources` = store `{graphs, graphNames, effects, presets}`.
- **Parse:** `parse(text): ClipDoc | ClipParseError`; guard `isClipParseError`; `reason ∈ not-json|not-object|foreign|unsupported-version|unknown-kind|malformed` → toast. Never throws.
- **Materialize (authored only):** `remapClipDoc(doc, ctx): RemapResult | ClipParseError`. `ctx={graphs, effects, presets, isBuiltInEffectId, mint?}` — S44 passes store registry + `isBuiltInEffectId=(id)=>EFFECTS.some(e=>e.id===id)` (fixtures), optional `makeDefaultMint(ctx)`. `RemapResult={graphs,graphNames,effects,presets}` to UNION + `{graphKey|section|song}` to insert. **S44 must NOT re-implement** closure/re-key/`:default`-tracking/ref-rewrite — call remap + apply.
- **Payloads:** graph→graph +deps{effects,presets}; section→section +deps{graphs,graphNames,effects,presets}; song→song(sections) +full closure; patch→`payload.patch: Pick<Project,'kit'|'inputMap'|'output'>&{name?}`.
- **Patch (S45):** `remapClipDoc` REJECTS `patch` (typed error) — validate server-side (zod) + apply wholesale, no remap.

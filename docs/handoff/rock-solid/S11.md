# S11 — Kit mirror X/Y (Group D, issue #49)

## Summary
Kit-global `mirror` (none/x/y) applied as a FINAL world-space reflection in `buildPixelModel`
(world position + tangent + normal + drum effect origin), composing on top of S10's per-drum flip.
Geometry-only invariant held: pixel index order + DMX bytes are byte-identical regardless of mirror.
Plumbed on its own kit-global `setKitGlobal` message (not the per-drum `setKitTransform` carrier)
reaching BOTH hosts (Engine + VoiceHost, each rebuilds the model). Mirror SegmentedControl on the
Patch view header toolbar — applies live, persists with the project.

## Acceptance
- [x] Mirror reflects world coords; drums keep identity; mirror+flip compose — `mirror.test.ts`
      (world-X/Y reflection, index-order preserved, `mirror ∘ flip` compose case), all pass.
- [x] DMX map byte-identical regardless of mirror — `mirror.test.ts` `perPixel`/`universes` `toEqual`
      goldens for x, y, and flip+mirror vs flip-only.
- [x] Toolbar control applies live + persists — `store.routing.test.ts` (setKitMirror → local write +
      `setKitGlobal` send); both-host wiring in `kit-global-forwarding.test.ts`; live re-broadcast via
      the legacy structural path + autosave. UI shot: mirror control renders in Patch toolbar, strict-clean.

## Gates
`pnpm typecheck` 0 errors (all pkgs). `pnpm test` green, no skips (core 560, server 212, web 1107,
io 13, protocol 1). `packages/core` stays pure. UI verified via `pnpm ui-shot patch-graph --strict`.

## Deviations
- Also reflected each drum's `effectOriginWorld` (a world coord radial effects sample against mirrored
  pixels) — beyond the doc's literal "every pixel", required for effect consistency under mirror.
- `setKitGlobal` left OUT of client-message's output-reapply set (unlike `setKitTransform`): mirror never
  changes the dmxMap, so the plain structural broadcast + autosave suffices.

## Context pack
Group D nearly closed. New kit-global carrier `setKitGlobal` (protocol + both hosts' `setKitGlobal`) is
the template for any future kit-wide geometry field — mirror the pattern, not `setKitTransform`.

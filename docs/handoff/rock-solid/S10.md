# S10 — Per-drum flip (+ pixelsPerHoop forwarding fix) · Group D · PASS

**Summary:** Added `flip?: boolean` to the drum schema and applied it in `buildPixelModel` as a
geometry-only transform — reflect the hoop stack along local Z (negate `localZ`) and negate the
angular sweep BEFORE the start/spin offsets. Pixel index order, hoop indices, and the DMX byte
stream are untouched (flip never re-patches hardware). Plumbed end-to-end: inspector toggle →
`setDrumTransform` → `setKitTransform` WS msg → both server engine paths. Fixed the latent
`pixelsPerHoop` drop on the legacy-engine path (was silently dropped in `input-router` + excluded
from `engine.setKitTransform`'s `Pick`).

**Acceptance:**
- [x] Flip twice = identity / skins swap / wind reversed — `flip.test.ts`: reflection (x,-y,-z) is its
  own inverse; top hoop z 150→−150 (stack inverts); pixel-0→1 sweep +45°→−45°.
- [x] DMX map byte-identical flip on/off — `flip.test.ts` compares `perPixel` + `universes` (toEqual).
- [x] pixelsPerHoop forwarded on both paths — `kit-transform-forwarding.test.ts`: legacy
  `applyClientMessage` + voice `propagateToVoiceHost` both forward `pixelsPerHoop` and `flip`.
- [x] Inspector toggle live — `store.routing.test.ts`: `setDrumTransform({flip})` writes drum flag +
  sends `setKitTransform`. UI: `Toggle` primitive in a `Field` in `PatchDrumInspector.svelte`.

**Gates:** typecheck 0 errors. Tests: core 553, server 208, web 1106 — all pass, no skips.
`packages/core` stays pure (no new imports).

**Deviations:**
- "hoop-0 z ↔ hoop-N z" implemented per the doc's authoritative "negate localZ": hoop 0 stays at
  z=0, the stack reflects into −Z (top hoop's z negates). Test asserts the reflection, not a literal
  value swap.
- Voice path (`propagateToVoiceHost`) still omits `hoopSpacingMm`/`diameterIn` — a PRE-EXISTING gap
  (only `origin/rotation/spin/startAngle/pixelsPerHoop/flip` forwarded). Out of S10 scope; flagging.
- No `pnpm ui-shot` captured in this headless slice; the toggle composes existing `Toggle`+`Field`
  primitives with no new styling.

**Context pack for S11 (kit mirror none|x|y):**
- **Transform shape:** `DrumTransformPartial` (`apps/web/.../store/trigger-routing.ts`) + protocol
  `setKitTransform` (`packages/protocol/src/index.ts`). Mirror is KIT-GLOBAL not per-drum, so it does
  NOT belong on `setKitTransform`/drumSchema — add a `kit.global`-level `mirror` field + its own WS
  message/mutator; the drum-transform plumbing here is the *pattern* to copy, not the carrier.
- **Where in `buildPixelModel`:** flip mutates `local` INSIDE the per-drum loop (localZ + sweep sign).
  S11 mirror is a FINAL world-space reflection — apply it to `world` (and negate the matching
  component of `tangent`/`normal` to keep them consistent) AFTER `localToWorld`, gated on
  `kit.global.mirror`. Compose cleanly on top of flip (flip already baked into `local`→`world`).
- **Forwarding points:** legacy `input-router.ts` `setKitTransform` case + `engine.ts`
  `setKitTransform` `Pick`; voice `handlers/voice-input.ts` `propagateToVoiceHost` +
  `voice-engine-host.ts` `setKitTransform` `Pick`. A kit-global field needs a new message reaching
  `reloadKit()` on both hosts (rebuild model, not just dmxMap).
- **Invariant + golden to mirror:** geometry-only, `dmxMap` byte-identical — mirror `flip.test.ts`
  wholesale: the `perPixel`/`universes` `toEqual` check and an involution (double-mirror = identity)
  case; add a `mirror ∘ flip` compose case (both goldens agree).

# S04 — Input activity badges (B, issue #47)

## Summary
Every MIDI/OSC binding now shows a live **last-heard badge** — `note · value · age`
(e.g. `C4 · 92 · 2s`) — beside the field, so after MIDI-learn (or any bind) you can
watch the bound input actually arrive. It appears in the **trigger-source inspector**
(under the active MIDI-note / OSC field) and the **patch zone inspector** (under each of
its MIDI-note and OSC-address fields). The dot pulses on a fresh hit and the badge fades
to muted as it ages out; the age keeps counting between hits. Fed by the existing
`input` WS traffic through a pure, table-tested matcher — traffic for other notes/addresses
never churns an unrelated binding. New `InputActivityBadge` primitive added to the design
system.

## Commits
```
2fbfdc8 S04: styleguide demo for InputActivityBadge + regenerate design-system.html
422ca84 S04: record input activity + wire last-heard badges into both inspectors
9e11ebd S04: pure input-activity matcher (binding × event → badge view)
a3b24eb merge group/B → slice/S04: integrate S03 (store + design-system.html regen)
```
(SHAs above are pre-report; this report is the final commit on `slice/S04`.)

## Files touched
`git diff --stat group/B slice/S04` (group/B is an ancestor after the merge, so this is the
S04 surface only):
```
 apps/web/src/lib/app/docks/inspectors/PatchZoneInspector.svelte     |  16 +++
 apps/web/src/lib/app/docks/inspectors/TriggerSourceInspector.svelte |  16 +++
 apps/web/src/lib/styleguide/sections/SectionPrimitives.svelte       |  13 ++
 apps/web/src/lib/trigger-lab/input-activity.test.ts                 | 121 ++++++++
 apps/web/src/lib/trigger-lab/input-activity.ts                      | 133 ++++++++
 apps/web/src/lib/trigger-lab/store.svelte.ts                        |  72 ++++--
 apps/web/src/lib/ui/InputActivityBadge.svelte                       |  77 ++++++
 docs/design-system.html                                             |  38 +--
```

## Context pack
No slice depends directly on S04, but the seam is reusable:

- **Matcher seam — `apps/web/src/lib/trigger-lab/input-activity.ts`** (pure): `InputActivity`
  (the web mirror of the wire `input` event), `InputBinding`, `activityKey` (identity =
  note or address), `acceptsChannel` (the global MIDI channel filter, extracted so the store
  and the tests share one predicate), `bindingFromSource` (TriggerSource → binding; drum/CC/
  empty → null), and `deriveInputBadge(binding, activity, now) → InputBadgeView | null`. All
  formatting/tone/age lives here; the component is a dumb prop bag.
- **Store ingestion — `store.svelte.ts`**: `recordInputActivity()` upserts under `activityKey`
  from BOTH input paths (`forwardMidi` for local WebMIDI, `onInput` for the server echo — the
  server does not echo a client's own input back, which is why both fire). `inputBadge(binding)`
  is the reactive accessor inspectors call; `nowTick` is a coarse age clock advanced ~2×/s from
  the RAF 500 ms cadence block.
- **Design system**: `InputActivityBadge` (`lib/ui/InputActivityBadge.svelte`) composes
  `StatusDot`; demoed in `SectionPrimitives` → regenerated `docs/design-system.html`.
- **Scope note for later slices**: MIDI **CC** and drum-zone sources deliberately get no badge —
  CC never rides the `input` wire with a matchable field, and drum hits fire via the pad path.
  If a later slice adds a `cc` field to the `input` message, extend `activityKey`/`bindingFromSource`
  to cover it (one arm each).

## Acceptance
- [x] **Badge appears on both inspectors, updates on matching input, ages out visually** —
  `TriggerSourceInspector.svelte` renders `{#if heard}<InputActivityBadge>` under the active
  MIDI/OSC field; `PatchZoneInspector.svelte` renders `heardNote` + `heardOsc` under each
  field. Updates come from the `SvelteMap` upsert; ages out via `nowTick` (tone → muted past
  `STALE_MS`, `fresh` pulse within `LIVE_MS`, age label keeps counting).
- [x] **Pure matcher unit-tested (note/channel filters, OSC address match)** —
  `input-activity.test.ts`, 22 cases: `activityKey` distinctness, `acceptsChannel` (null/match/
  mismatch), `bindingFromSource` (7 cases inc. CC→null, drum→null), `deriveInputBadge` (note/OSC
  hits, velocity rounding, age labels, freshness/tone tiers).
- [x] **No badge churn from non-matching traffic** — the map is keyed by input identity and
  `deriveInputBadge` reads a SINGLE key, so a `set()` on another note/address doesn't invalidate
  an unrelated binding's `$derived`. Asserted directly by the "unrelated traffic leaves a
  binding's badge identical" test.

## Gates
- `pnpm typecheck`: **0 errors, 0 warnings**, all packages (web: 2213 files).
- `pnpm test` (integrated tree, no skips):
  - packages/core: 234 · packages/io: 13 · packages/protocol: 1
  - apps/server: 170 · apps/web: 653 (includes S03's +20 suites)
  - **1071 passed, 0 failed, 0 skipped.**

## Deviations
- **Merged `group/B` into `slice/S04`** (merge commit `a3b24eb`). `group/B` advanced with S03
  (store `OutputStatus`/packets-per-s adoption + controller panel + a design-system regen) after
  I forked at `8a3dcf8`. Both slices regenerate `docs/design-system.html`, so I integrated now
  and resolved that one conflict by **regenerating the artifact from the merged source tree**
  (the correct resolution for a generated single source). `store.svelte.ts` auto-merged cleanly
  (S03's and S04's additions are in different regions); gates re-run green on the combined tree.
  No S04 logic changed as part of the merge.
- No files touched outside the slice surface (matcher module, badge primitive, the two named
  inspectors, the styleguide demo + regen).

# S36 — LFO node (group I, issue #57) — implementer report

Branch `slice/S36` off `group/I`. **Sweep: `pnpm typecheck` clean (6 projects) · `pnpm test`
1566 passed / 0 failed / 0 skipped** (core 525 +18, web 851 +7, server 176, io 13, protocol 1).

## What changed
- **Core** (`voice/lfo.ts`, new): pure `sampleLfo(settings, timeMs, bpm)` — waveforms
  sine/triangle/saw/square/`sample-hold` (S&H = integer-hash per period, deterministic, no
  `Math.random`); rate as free Hz or musical division (REUSES `computeDelayMs`/`DELAY_DIVISIONS`
  from `delay.ts` — shared, not forked); phase offset. Seams (all minimal `// S36`): `ModSource`
  `lfo` arm + `sampleSource` case (reads `ctx.timeMs`/`bpm`, never `phase`); `MOD_SOURCE_KINDS`
  += `'lfo'` + `nodeModSource` case; `NodeKind` += `'lfo'`, `GraphNode.lfo?`; eval-graph inert case.
- **Web**: sim inert case, `NODE_KINDS` += `'lfo'`, `trigger-node-meta` maps (Waves icon), store
  `addNode('lfo')` seed + `lfoSettings`/`setLfo`, `node-options` LFO opts; new `LfoNodeInspector`
  (design-system primitives — Select/SegmentedControl/CommitInput/Slider), routed in Inspector.
  Palette/KIND_OPTS pick it up automatically via `isModSourceKind`. Design-system regenerated
  (NodeCard face record is exhaustive over `NodeKind`).

## Deviations
- None.

## Context pack (S38 previews)
- Sample the waveform for a node-face preview with core `sampleLfo(node.lfo ?? defaultLfoSettings(),
  timeMs, bpm)` → 0..1; phase cursor = `frac(timeMs / lfoPeriodMs(s, bpm) + s.phase)` (S&H = step).
  Continuous (absolute time), so no per-voice phase — one shared ticker value drives the face.

Sweep: `pnpm typecheck` && `pnpm test` — green, no skips.

# S30 — Modifier batch 2 (group H, issue #54)

## What changed
- **Bloom** (`spatial`): triangular-kernel neighbour spread along the strip, additive halo
  (`radius`/`intensity`); per-voice scratch snapshot → pure, order-independent, no hot-path alloc.
- **Sparkle** (`texture`): seeded per-voice `Prng` + per-pixel decaying glint buffer
  (`density`/`decayMs`); white points over dark, never `Math.random`.
- **Grain** (`texture`): animated monochrome value-noise brightness modulation (`amount`);
  per-frame field reseeded via `deriveSeed(base, frameCounter)` → animates yet replays identically.
- **Strobe** (`temporal`): rate/duty chop off the voice-local `ctx.timeMs` phase (`rate`/`duty`);
  stateless, `rate≤0`/`duty≥1` = identity.
- Registered as a contiguous `// S30` block in `modifiers/registry.ts`; palette/inspector/graph
  pick them up for free (no UI work).

## Deviations
- Sparkle/Grain import core's `Prng`/`deriveSeed` from `../../voice/prng` (the canonical no-`Math.random`
  RNG); `voice/prng` is a leaf util, so no import cycle with `voice/compositor → modifiers`.
- End-to-end wiring tests live in the S30 core test file (graph node → `resolveModifierChain` →
  voice → `createDefaultCompositor().render`), not `sim.modifiers.test.ts` (out of my file scope).

Sweep: `typecheck 0 · tests 1421 passed / 0 failed / 0 skipped` (core 412, web 819, server 176, io 13, protocol 1)

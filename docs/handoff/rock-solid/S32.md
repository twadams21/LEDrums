# S32 — Modifier second wave (group H, issue #54)

## What changed
- 8 new modifiers (S28 `ModifierDef` shape, pure/seeded/voice-local): **Slide** (spatial),
  **Blur 1D** (spatial), **Posterize/Threshold** (color), **Feedback** (temporal), **Kaleidoscope**
  (spatial), **Freeze** sample-and-hold (temporal), **Flicker** seeded (texture), **Chromatic** (color).
- `impl/strip.ts` — shared 1D-strip helpers (per-voice scratch snapshot + clamp/wrap index) for the
  spatial modifiers; registered as a contiguous `// S32` block in `registry.ts` `ALL[]`.
- `modifiers/palette.ts` — `listModifiersByCategory()` (+ order/labels), dynamic over the registry.
- UI: new `ModifierPalette.svelte` (category `SegmentedControl` filter + add-buttons over
  `listModifiersByCategory()`) in the Trigger graph toolbar; generic `modifier` kind dropped from the
  flat palette; `store.addModifierNode()`. Styleguide `SectionGraph` demos it; `design-system.html` regenerated.

## Deviations
- Grouping helper lives in **core** (`palette.ts`, exported from index) — categories are a core concept,
  keeps the palette test in the core S32 file. `registry.ts` touched ONLY as an `ALL[]` append (per conflict rule).
- Chromatic categorised as `color` (channel-shift); it is also spatial.

## Sweep
`typecheck 0 · tests 1437 passed / 0 failed / 0 skipped` (core 428, web 819, server 176, io 13, protocol 1)

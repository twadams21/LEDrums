# S35 — EnvMap migration + removal (group I, issue #57) — implementer report

Branch `slice/S35` off `group/I`. **Sweep: `pnpm typecheck` clean (6 projects) · `pnpm test`
1551 passed / 0 failed / 0 skipped** (core 507, web 854 +10, server 176, io 13, protocol 1).

## What changed
- **Core**: removed `Voice.env`, `PlayAction.env`, the legacy per-param env sweep in
  `applyEffectiveParams`, and the voice-pool env clone (compositor/eval-graph/voice-pool/engine).
  `GraphNode.env` stays — envelope-source shape slot (b) + modifier bridge (c) untouched.
- **Web migrator** (`store/hydrate.ts`): `migrateGraphEnvMaps` — each play `env[key]` → one
  `envelope` source node + a `param:<key>` edge (`amount=env.amount`, range=spec min/max ≡
  `envelopeToMapping`), legacy field then cleared. Wired into `normalizeGraphs` AFTER the ADSR-v2
  pass, fed the effect param specs. Idempotent + alias-stable.
- **Web sim** mirrors the core env removal (`sim.ts`/`render.ts`). **Play inspector** loses its
  per-param envelope button (authoring is the S34 ModulationParamsSection); modifier env authoring
  + EnvelopeEditor modal left intact (out of scope).

## Deviations
- Followed the assignment's exact recipe — **one envelope node per distinct play `env[key]`**, no
  shape-dedup (the slice doc's "per distinct shape per graph" is a not-taken optimization).
- Play-node colour-swatch `modulated` indicator now reads incoming modulation wires; the base
  slider no longer shows "swept"/disabled (mappings are base-relative, so base still matters).

# S37 — CC-In node (group I, issue #57) — implementer report

Branch `slice/S37` off `group/I`. **Sweep: `pnpm typecheck` clean (6 projects) · `pnpm test`
1557 passed / 0 failed / 0 skipped** (core 514 +7, web 853 +9, server 176, io 13, protocol 1).

## What changed
- **Core**: `modulation.ts` — `cc` ModSource arm + `CcTable`/`ccKey`/`ccValue01`/`sampleCc`, optional
  `ModSampleCtx.cc` (absent ⇒ 0 ⇒ maps to rangeMin). `modulation-graph.ts` — `'cc'` in
  `MOD_SOURCE_KINDS` + `nodeModSource` case. `types.ts` — `'cc'` NodeKind + `ccController`/`ccChannel`.
  `eval-graph`/sim inert case. `compositor.ts` threads `CcTable` via `CompositorFrame` +
  `applyEffectiveParams`. `engine.ts` — `'cc'` InputEvent + a `ccTable` updated ONLY in `processEvent`
  (writes omni + per-channel key; normalized 0..127→0..1), cleared on `setShow` → determinism.
- **Server**: `voice-input.ts` routes non-CC0 controllers to `voiceHost.applyInput({kind:'cc'})`;
  `VoiceEngineHost` maps it to the engine event. (CC 0 stays reserved for section recall.)
- **Web**: `sim.ts` mirror `ccTable`+`setCc`; `render.ts` threads `cc`. Store: cc-node
  accessors/setters (CC 0 + out-of-range rejected — validation, not throw), cc-node MIDI-learn that
  binds the next CC and skips CC 0, WebMIDI `cc` forward feeds `sim.ccTable`, `addNode('cc')` seeds
  controller 1/omni. `CcNodeInspector.svelte` (controller + channel + Learn) routed in Inspector;
  node meta maps + design-system regenerated.

## Deviations
- None. Learn wired on the LOCAL WebMIDI path (store test drives it); server→web CC echo for
  hardware-at-server learn is a future refinement (not in the acceptance set).

## Context pack (S38 — live CC value for a preview)
- Read the live value from the CC table: `voice.sampleCc(sim.ccTable, node.ccController ?? 1,
  node.ccChannel ?? null)` (0..1). The core engine holds the same `ccTable`; the web `Sim.ccTable`
  is the offline mirror the graph preview already reads. Reuse the shared `effect-thumb-ticker` (no
  new rAF) to draw the CC value bar + readout on the node face.

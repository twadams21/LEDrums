# S31 — Modifier batch 3 (group H, issue #54)

## What changed
- Five modifier defs (S28 shape) + a `// S31` registry block: **Echo**, **Pixelate**, **Mirror**, **HueShift**, **Levels**.
- **Echo** — per-voice ring-buffer feedback delay (`echo.ts`): the wet frame from `delayMs` ago re-enters scaled by `feedback`, giving a decaying ghost train. Ring sized to the voice range × 64 slots; delay clamps to that span.
- **Pixelate** (spatial block-average), **Mirror** (`axis` enum `low`/`high`/`flip`, in-place permutation), **HueShift** (`mode` enum `shift`/`colorize`, HSV), **Levels** (saturation × brightness × invert, HSV). All stateless, pure.
- New test file `modifiers.s31.test.ts`: per-modifier apply goldens, Echo temporal state pinned across ticks, bypass=identity each, Mirror/HueShift enum-spec asserts.

## Deviations
- "Saturation/Levels/Invert" shipped as ONE `levels` modifier (sat + brightness + invert), matching the assignment's 5-file list (`levels.ts`).
- Mirror `axis` enum is a 1D-strip reflection (`low`/`high`/`flip`), not geometric X/Y/Z — `apply` has no per-pixel geometry index; the enum + S18 Select requirement is met.
- Echo max delay effectively ~1s (64-slot ring); longer `delayMs` at low fps saturates rather than growing the buffer.

## Sweep
`typecheck 0 · tests 1433 passed / 0 failed / 0 skipped` (core 424, web 819, server 176, io 13, protocol 1)

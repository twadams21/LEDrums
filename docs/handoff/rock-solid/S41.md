# S41 — Library refs: resolve/detach/guards (group J, #53) — implementer report

Branch `slice/S41` off `group/J` (S40 tip). Pure resolver + store ops + guards + song-library rune
wiring (deferred from S40) + tests ONLY — no UI (S42 owns all surfaces).

## What changed
- **Pure `store/song-library-refs.ts`**: `resolveSongRefs` (inverse of extract — unions namespaced
  closures into a runtime view, collision-free; look `busId`s stay show-level); `detachLibrarySong`
  (deep-clone + re-key to a fresh namespace); library CRUD (`withLibrarySong`/`renameLibrarySongIn`/
  `removeLibrarySong`); ref-list edits (`addSongRef`/`removeSongRef`); guard (`showsUsingSong`/
  `planDeleteLibrarySong`).
- **`songRefs` on AuthoredState** — coercion (dedupe, drop non-strings), seed `[]`, store carry.
- **Song-library rune wired into the store** (S40 deferred): boot load (`SONGS_STORAGE_KEY`),
  autosave write-through + `setSongLibrary` push, cold-load adopt + viewer follow via
  `SongLibrarySync`, ws client `onSongLibrary` + `state.songLibrary` (+ `songLibrary` added to
  `SERVER_TYPES` so `decodeServer` accepts it).
- **Store ops**: `exportSongToLibrary`/`importSongReference`/`detachSongReference`/`renameLibrarySong`/
  `deleteLibrarySong`(guarded) + `resolvedView`/`resolvedSongs`/`songLibraryList` derived.

## Acceptance — all met
- [x] Import → resolve → render parity — `store/song-library-refs.test.ts` (resolve reproduces the
      reached closure, internally consistent) + store `export→import→resolve`.
- [x] Edit library song ⇒ every referencing show's resolved view updates; detach isolates —
      propagation + detach isolation tests (pure + store).
- [x] Delete blocked while referenced (reports using shows) — guard tests (pure + store); the
      no-cross-show-bleed test (`store.shows.test.ts`) updated to state the library exception.

## Gates
typecheck 0 (6 pkgs) · test 1679 / 0 failed / 0 skipped (core 532 / io 13 / protocol 1 / server 185 /
web 948, +18) · resolvedView is a pure derived, not on the render loop.

## Deviations
- None. resolution stays a DERIVED view (`resolvedView`); persisted authored state holds refs, not
  copies, so propagation stays canonical. Engine/UI consumption of `resolvedView` is S42.

## Context pack for S42 (UI)
- **Store ops** (all viewer-guarded): `exportSongToLibrary(songId): libId|null` (extract+add, no
  ref); `importSongReference(libId)` (adds to active show's `songRefs`); `detachSongReference(libId):
  localSongId|null` (clones closure into authored runes, drops ref); `renameLibrarySong(libId,name)`;
  `deleteLibrarySong(libId): usedBy[]` (empty = deleted, non-empty = blocked); `showsUsingSong(libId)`.
- **Referenced song in the runtime view**: `resolvedSongs` (local + referenced Songs) and
  `resolvedView` (merged graphs/effects/presets) — point the Songs rail + engine seam here; raw
  `songs`/`graphs` stay un-resolved (refs, not copies). Referenced sections carry `lib:<id>/` keys.
- **Used-by for the UI**: `songLibraryList` = `{id,name,usedBy:{id,name}[]}[]`; disable delete when
  `usedBy` non-empty and list the shows.

# S49 — Controller test patterns + takeover state (Group L, issue #56) — FINAL SLICE

## Summary
Drove the PixLite's built-in test-data mode from the controller panel with a LOUD, server-authoritative
takeover state the ENTIRE time a pattern runs. Firmed `ModeTestDataParams` against API §7.7.2 and added
`modeLive` (§7.7.1) to `PixliteClient` as the real "back to live" exit. The server monitor owns the
takeover (`ControllerStatus.testPattern`), so every watcher's banner + output pill agree, and auto-reverts
to live when the last panel watcher leaves (panel close / disconnect). `packages/core` untouched.

## Acceptance
- [x] **Test patterns start/stop from the panel** — solid-colour swatches (setColor, all ports/pixels) +
  RGBW cycle + colour fade; fake-backed monitor test asserts `modeTestData` params + echoed `testPattern`.
- [x] **Takeover visible on panel AND output pill the whole time** — amber warn banner (not the red fault
  family) under the header; `deriveOutputPill(link, output, takeover)` forces a pulsing `TEST` tone over
  LIVE/ARMED/DRY (below link-down + genuine ERR). Pure-predicate + component tests both.
- [x] **Back-to-live one click; auto-revert on disconnect** — exit from banner AND ops row (`modeLive`);
  monitor test proves auto-revert fires only when the LAST watcher leaves (multi-watcher safe), never lies
  about a takeover on a failed `modeTestData`.
- **Smoke-load:** `UI_SHOT_BASE=…:5199 LEDRUMS_ENGINE=voice pnpm ui-shot patch-controller --strict` → **clean
  console, no throws** (dedicated stack on :5199/:4399; :5173 was a sibling worktree). Un-adopted state
  captured (no hardware); the takeover UI is proven by component tests + the styleguide takeover demo.

## Gates
`pnpm typecheck` 0 errors · `pnpm test` all green (io 51, core 560, protocol 1, server 227, web 1170) ·
design-system regenerated + committed (added the ControllerStatusPanel takeover state to its demo).

## Deviations
- Added `PixliteClient.modeLive` (§7.7.1) — S46 only sketched `modeTestData`; "back to live" + auto-revert
  are impossible without it. Minimal, mirrors `modeTestData` (client + fake + serializer + tests).
- Takeover is **server-authoritative** (echoed on `ControllerStatus.testPattern`, optional field) rather
  than a local `$state`, so multi-client watchers agree and it survives reconnect. Per-port/per-pixel kept
  to the default (all outputs) — the `pixPortNum`/`pixNum` params are wired through but the ui-light panel
  exposes only all-outputs targeting.

## Context pack (group L closes with S49)
Nothing cross-lane. New wire surface: client msgs `controllerTestData`/`controllerBackToLive`, and the
optional `ControllerStatus.testPattern`. Auto-revert hooks the existing watcher lifecycle (`dropWatcher`).

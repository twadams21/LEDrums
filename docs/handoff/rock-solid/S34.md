# S34 — Modulation graph layer: Envelope node + param rows (group I, issue #57) — implementer report

Branch `slice/S34` off `group/I`. **Sweep: `pnpm typecheck` clean (6 projects) · `pnpm test`
1541 passed / 0 failed / 0 skipped** (core 507 +18, web 844 +21, server 176, io 13, protocol 1).

## What changed
- **Core** (`voice/modulation-graph.ts`, new): pure `resolveNodeModulations` (incoming `param:<key>`
  edges → `Mapping[]`) + `nodeModSource`/`paramKeyOf`/`isModSourceKind`/`ENVELOPE_NODE_KEY`; wired into
  `eval-graph` (play `modulations`, envelope inert) + `modifier-graph` (link `modulations` from authored
  `node.env` via `envelopeToMapping` — closes group-H residual — **and** param edges). Types: `envelope`
  NodeKind, `modInputs`, `GraphEdge.param:<key>` + per-mapping amount/invert/rangeMin/rangeMax.
- **Web sim/store/wiring**: sim play calls the shared resolver (offline == engine); `param` scoping
  (never throws); `addNode('envelope')` seeds a shape, `connect` bakes the edge mapping from spec,
  `add/removeModInput` (removal deletes wires), `mappingsFor`/`setMapping{Amount,Invert,Range}`,
  envelope-shape accessors; graph-to-flow `param:` handle + `data.modulation`.
- **UI**: `EnvelopeEditorView` (extracted from the S24 modal, shared with new `EnvelopeNodeInspector`);
  `ModulationParamsSection` (Add parameter + target-side mapping edits + remove-confirm) in play+modifier
  inspectors; TriggerNode param-row faces + scoped handles; modulation palette group; `--role-modulation`
  dotted wire (3rd role); drop-anywhere → param row. Design system regenerated (SectionGraph demo).

## Deviations
- Play-node mapping ranges are baked onto the edge by the store at wire time (effect specs aren't in
  `eval-graph`; resolver falls back to `[0,1]`); modifier ranges resolve from the registry spec. Play
  legacy `env` still applies via the S33 sweep until S35 removes `v.env`.

## Context pack (group I tracer)
- **S36 LFO / S37 CC source kind, end-to-end**: `NodeKind` arm + add to the inert `case` list in
  eval-graph/sim; add to `MOD_SOURCE_KINDS` (nodeHasInput / palette / KIND_OPTS then automatic); `ModSource`
  arm + `sampleSource` case (LFO: `ctx.timeMs/bpm`; CC: add a `cc` table to `ModSampleCtx` + engine
  `processEvent`); `nodeModSource` case reading the node's settings; trigger-node-meta maps + a
  `*NodeInspector` routed in Inspector. (Envelope stores its shape in `node.env[ENVELOPE_NODE_KEY='shape']`.)
- **S35 env→node**: per distinct play `env[key]` make one envelope node (shape in the `shape` slot),
  `addModInput` the key, add a `param:<key>` edge `amount=env.amount, invert=false, range=spec.min/max`
  ≡ `envelopeToMapping`; parity via `modulation-parity` + `resolveNodeModulations`.
- **S38 live ticks**: rows = `store.modInputsOf` → `TriggerNode.modRows` (`.modrow/.pdot/param-handle`,
  `.wired` from `mappingsFor`); add per-row value tick + envelope sparkle via the shared
  `effect-thumb-ticker` (no new rAF).

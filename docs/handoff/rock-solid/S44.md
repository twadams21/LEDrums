# S44 — Clipboard copy/paste UI (group K, #55) — implementer report

Branch `slice/S44` off `group/K`. UI + store adapter over the S43 clipdoc module — NO
closure/re-key/ref-rewrite re-implemented (calls `buildGraphClipDoc`/`buildSectionClipDoc`/
`buildSongClipDoc` + `serialize` on copy; `parse`→`remapClipDoc`→union on paste). `packages/core`
untouched. Copy sources = `store.resolvedView` (S42) so referenced-library content copies whole.

## Acceptance — all met
- [x] Copy graph/section/song → system clipboard (navigator.clipboard); pastes cross session/server,
      closure intact — remap unions fresh deps, reuses built-in/content-equal ones (no dup on re-paste).
- [x] Non-ClipDoc / foreign / wrong-kind clipboard ⇒ friendly toast, no state change.
- [x] Paste-song destination dialog works for BOTH: this-show (remap+insert) and Song Library
      (`extractSongClosure`→`withLibrarySong`, mirrors export).

## New / changed
- `ui/toast.svelte.ts` + `ui/ToastHost.svelte` (generic toast primitive, mounted in AuthorShell;
  styleguide demo + `docs/design-system.html` regenerated) · `trigger-lab/clipboard-io.ts` (navigator adapter).
- `store.svelte.ts` "system clipboard copy/paste" region: `copy{Graph,Section,Song}ToClipboard`,
  pure `materializePaste`, paste orchestrators + `pasteFallback`/`songPasteOpen` UI state.
- Views: PasteSongDialog + PasteFallbackDialog; copy on graph/song rows + section headers; paste
  affordances in Objects (graphs/songs) + section header.
- Tests: `store.clipboard.test.ts` (8), `toast.test.ts` (6).

## Gates
typecheck 0 (6 pkgs) · test all green (core 33F / io 3F / protocol 1F / server 18F / web 95F, 1001 web +14).
Commits: toast+store region · dialogs+wiring · tests+styleguide · this report.

## Deviations
- In-app section clipboard kept unchanged as the blocked-read fallback (copy writes BOTH in parallel).
- Section/song copy source the RESOLVED view, so a referenced-library section/song copies its
  materialized content (its section ids are `lib:…`-namespaced — expected, remap re-keys on paste).
- No global Cmd+C/V (risky, out of scope) — copy via row menus/quick-actions, paste via header buttons.

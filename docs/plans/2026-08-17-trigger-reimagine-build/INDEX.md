# Trigger reimagine — build waves

Source of truth for scope: `docs/plans/2026-08-16-trigger-reimagine/P1-verdicts-and-build-brief.md`
(Trent's verdicts, recorded 2026-08-17 from Trent directly). Open questions answered by Trent
in-session 2026-08-17 (this machine): **strength = profile curvature** (greys out for
Linear/Snap); **exactly two handles** (not N); **Add-node palette = on-canvas popover**.
Visual reference: the four prototypes on `proto/trigger-reimagine` @ `e06b726`.

## Slices

| ID | What | Effort | State |
|----|------|--------|-------|
| S1 | SVG envelope-handle roundness fix (fix (b): px-true viewBox) | opus/medium | wave 1 |
| S2 | "Now playing" indicator on graph-list cards | opus/medium | wave 1 — fact CONFIRMED 2026-08-17: `Voice.pad` does NOT reach the wire (dropped in `stats()` `engine.ts:742`, absent from `voiceStatSchema` `schemas.ts:297` which strips unknowns + `_LockVoiceStats` at `schemas.ts:471` forces core+protocol edited together; sim hardcodes `pad:'preview'` `sim.ts:523`; section-slot prefixes are `key#slot` → strip suffix). Slice includes the wire addition. Liveness must read `dockVoices`/`serverVoices`, never the smoothed display list. |
| S3 | Inspector → window-right slideover over all chrome + Add-node on-canvas popover | opus/high | wave 1 |
| S4 | Effect inspector: progressive disclosure + filter (winner: option 4) | opus/medium | wave 2, after S3 (inspector seam) |
| S5 | Face params riding the existing modInputs param rows | opus/high | wave 2, after S3 |
| S6a | Envelope param control — standalone two-handle primitive (UI only) | opus/high | wave 2. MUST include an optional live-input overlay API: recent hits plotted on the curve (input x → mapped output y, fading markers) — required by S8 |
| S6b | Envelope-valued life/decay through core/protocol/engine | — | NOT SLICEABLE YET — model design with Trent first |
| S7 | Param-key normalisation (hue & decay/life families) | opus/medium | backlog, after S4 |
| S8 | Per-TRIGGER MIDI velocity sensitivity curve (Trent, 2026-08-17): one curve per drum (NOT per zone), lives in Settings near the drum trigger zones, edited with the S6a envelope control, live hit feedback in the editor while drumming so tuning is visible. Needs: curve storage in the input model (per drum), applied on every velocity path (server MIDI, WebMIDI forward, keyboard/local — mutation parity), monitor feed for the live overlay | — | after S6a; model placement needs a short design pass |

## Seam table

- S1: `EnvelopeEditorView.svelte`, `envelope-editor-geom.ts` (+test) — disjoint from everything.
- S2: `GraphsDock.svelte` + one new pure selector — disjoint.
- S3: shell (`Overlays.svelte` area), `NodeEditor.svelte`, `TriggerGraphView`, new AddNodePopover —
  the inspector seam. S4/S5 queue behind it.
- S4: `EffectNodeInspector` + new common-section components.
- S5: `TriggerNode.svelte` paramFooter, `ModulationParamsSection.svelte`, store mutators.
  S4 ∥ S5 only because their fences are disjoint; any overlap discovered → serialize, S4 first.
- S6a: new files only (`lib/ui/` primitive + styleguide entry).
- `docs/design-system.html` is regenerated by S3/S4/S5/S6a — always resolved by regeneration at
  merge time, never hand-merged. Land PRs serially (or as a stack, Trent's preferred landing).

## Standing context for every implementer

- Non-negotiables in `AGENTS.md` apply: design-system use-or-extend + regen in-change,
  `/make-interfaces-feel-better` pass, ui-shot verification, core purity.
- Prototype files are the visual reference, not the spec — the verdicts doc overrides them.
- Report contract: commit body is the report (<30 lines); completion message = one line naming
  sha + branch; PR into main per slice, no merging without the orchestrator's gate.

## Recommendation parked with Trent

Merge **PR #182** (voice-life) before S6b: envelope-by-default for life/decay builds directly on
#182's `voiceLife` mechanism; without it the life family doesn't drive sustain on 9+ effects.
Also: once #182 lands, `segments` needs a one-line `voiceLife` declaration (flagged 2026-08-16).

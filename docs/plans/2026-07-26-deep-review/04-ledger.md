# 04 — Triage Ledger (rendered view; 04-ledger.json is canonical)

Baseline: `3708648` · generated by `artifacts/phase4-triage.mjs`

**107 findings** = 44 refuted · 13 auto-fix (12 land now, 1 held) · 50 structural in 13 initiatives.

## Auto-fix (Phase 5)

| finding | action |
|---|---|
| dead-code-0001 | **HELD** — Trent 2026-07-28: 'patch copy paste was meant to be fixed or changed, i cant remember.' Product question, not cleanup — components un-mounted by 39b7d6b but store.copyPatch() and trigger-lab/patch-diff.ts are alive. Do not delete until resolved. |
| dead-code-0003 | land |
| dead-code-0004 | land |
| dead-code-0005 | land |
| dead-code-0006 | land |
| dead-code-0007 | land |
| dead-code-0009 | land |
| dead-code-0010 | land |
| dead-code-0011 | land |
| dead-code-0012 | land |
| dead-code-0013 | land |
| dead-code-0014 | land |
| dead-code-0015 | land |

## Structural initiatives (Phases 7–10)

### INIT-01-single-render-stack (P1) — One render stack: retire legacy engine/, absorb sim.ts voice semantics into core

5 findings · 1 critical · ~1770 LOC

| sev | finding | claim |
|---|---|---|
| critical | divergent-change-0001 | The legacy `packages/core/src/engine` stack (engine.ts 438 / compositor.ts / modulation.ts / control-state.ts / transport.ts) and the `packages/core/src/voice`  |
| major | divergent-change-0003 | Beyond the engine/voice duplication, the web app carries a THIRD implementation of the same semantics: `Sim` (sim.ts:280-702) owns voice lifecycle, bus polyphon |
| major | duplicated-code-0004 | apps/web/src/lib/trigger-lab/sim.ts declares `EffectDef`, `Preset`, `Bus` and `Polyphony`, and sim.envelopes.ts declares `ParamSpec`, all of which packages/core |
| minor | duplicated-code-0011 | `hash2` (`Math.sin(ix*127.1 + iy*311.7) * 43758.5453` scramble), `fade`, `valueNoise` (the full 14-line bilinear lattice interpolation) and the fbm accumulator  |
| minor | resilience-hole-0011 | `applyModifierChain` does `const def = tryGetModifier(link.modifierId); if (!def) continue;` and `renderLayer` does `const effect = tryGetEffect(clip.effectId); |

### INIT-03-io-output-resilience (P1) — Art-Net/sACN silent-failure closure: error surfacing, ready-flag lifecycle, send seam

3 findings · 1 critical · ~210 LOC

| sev | finding | claim |
|---|---|---|
| critical | resilience-hole-0003 | Both output adapters install `this.socket.on('error', () => {})` — a total swallow — and gate every send on a private `ready` flag that is only set inside the b |
| major | primitive-obsession-0006 | `PixelOutput.send(universe: number, ...)` (interfaces.ts:6) is the single seam behind which BOTH Art-Net and sACN sit, but the two protocols have different vali |
| minor | duplicated-code-0012 | `ipToInt` (4-part split, per-octet integer/range validation, throw on malformed, `>>> 0` accumulate) and `intToIp` are declared privately and byte-identically i |

### INIT-04-server-runtime-hardening (P1) — Server main.ts decomposition + fault containment (render loop, boot, WS liveness)

8 findings · 3 critical · ~945 LOC

| sev | finding | claim |
|---|---|---|
| critical | resilience-hole-0001 | `scheduleNext()` calls `this.loop()` inside a bare `setTimeout` callback with no try/catch, and nothing between it and `gen.render(...)`/`compositor.render(...) |
| critical | resilience-hole-0002 | `initialProject()` is executed at module scope (main.ts:155) and, when `default.local.json` exists, unconditionally calls `loadProject(LIVE_PROJECT)` — which do |
| critical | speculative-generality-0002 | Six collaborators on `ClientMessageDeps` are declared optional "when the wiring provides none", and each absent branch is a silent no-op or empty reply (~20 `de |
| major | divergent-change-0004 | main.ts is 801 lines of module-level mutable state and closures over it (`host`, `voiceHost`, `liveShowLibrary`, `liveSongLibrary`, three autosavers, `oscListen |
| major | duplicated-code-0006 | createHostEventHandler and createNativeMidiHandler open with the same ~28-line preamble: path match, 405 on non-POST, `isTrustedHost({remoteAddress, headers, ur |
| major | resilience-hole-0004 | Neither side implements ping/pong or any staleness check (`grep -rn "ping\|pong\|heartbeat\|isAlive"` across the ws client, main.ts and boot.ts returns zero releva |
| major | resilience-hole-0005 | `statsTimer` fires every 10ms and calls `broadcastJson`, which serializes a full stats object (including per-voice arrays under voice mode) and pushes it to eve |
| minor | resilience-hole-0013 | The message handler's catch does `ws.send(encodeServer({ t: 'error', message }))` where `message` is `err.message` verbatim. For a ZodError that is a multi-line |

### INIT-02-store-decomposition (P2) — TriggerLab store god-object: real extraction (kill forwarders), web state architecture

7 findings · 3 critical · ~3825 LOC

| sev | finding | claim |
|---|---|---|
| critical | divergent-change-0002 | `TriggerLab` is the repo's single highest-churn file and every unrelated concern converges on it. In the last 400 commits it was edited 32 times, and the commit |
| critical | middle-man-0001 | R20-R24 extracted five rune-owning collaborators (ShowsController, SectionsController, ControllerMonitor, ControllerTest, MidiController) out of the 3641-line s |
| critical | speculative-generality-0001 | The R20/R22/R23/R24 store split created five controller classes, each with its own exported injectable `*Host` collaborator interface, but the store's own comme |
| major | duplicated-code-0003 | Both files declare a `{adopt\|seed\|noop}` plan union and a controller class with identical fields (`lastLibrarySig`, `serverStateSeen`) and identical method bodi |
| major | resilience-hole-0007 | `writeStoredLibrary` and `writeStoredSongLibrary` are void functions with a bare `catch { /* ignore */ }` around `localStorage.setItem` — the docstring calls th |
| major | speculative-generality-0007 | shell-nav.ts states its own rationale: "a PURE reducer (no runes, no DOM) so the invariants are unit-testable in the node test env. shell-store.svelte.ts is a t |
| minor | data-clumps-0005 | `fromId, toId, fromPort?, toPort?` travel as a four-slot positional tuple through classifyConnection, classifyReconnect, canConnect, canReconnect and the privat |

### INIT-06-graph-node-type-model (P2) — GraphNode/NodeKind discriminated-union redesign + voice ctx/param clumps

9 findings · 1 critical · ~926 LOC

| sev | finding | claim |
|---|---|---|
| critical | primitive-obsession-0001 | `NodeKind` is a 19-member union (types.ts:200-219) but `GraphNode` (types.ts:246-358) is a single flat interface with 40 top-level fields, 27 of them optional,  |
| major | data-clumps-0001 | The tuple {timeMs, bpm, cc, osc, notes} travels through the voice path under three names and one un-named positional spread. compositor.ts:80 already declares ` |
| major | data-clumps-0003 | Three functions carry the identical 8-slot positional tuple (state, graph\|plan, pad, startIds\|childIds, ctx, viaPrefix, seen, draft) and do nothing but forward  |
| major | data-clumps-0004 | applyModifierChain takes 8 positional params, three of which (model, timeMs, dt) are assembled into a `ModifierContext` object on the very first line of the bod |
| major | middle-man-0003 | voice/index.ts hand-picks five symbols out of modulation-parity.ts — a table of legacy-vs-new envelope cases plus a reimplementation of the OLD envelope math (l |
| major | primitive-obsession-0003 | `GraphNode.targetId` (types.ts:274-281) encodes a structured domain value — a drum plus a set of 1-based hoop indices — as `"<drumId>#<hoop>[,<hoop>]"`. The str |
| major | primitive-obsession-0009 | "Has this node structurally changed?" is a domain question answered by string concatenation. `triggerNodeSignature` (line 28) builds a `${id}:${kind}:pos=…:mod= |
| major | repeated-switches-0001 | Every other NodeKind discriminant site in the web app spells the effect node as the PAIR `kind === 'play' \|\| kind === 'effect'` (22 non-test occurrences under a |
| major | repeated-switches-0002 | The set {envelope, lfo, cc, note, osc, randomMod} is written out longhand in six independent places. Only render-plan.ts:70-76 is compiler-enforced (nodeCategor |

### INIT-05-pin-auth-hardening (P3) — Tunnel PIN gate: credential types + brute-force resistance

2 findings · 0 critical · ~130 LOC

| sev | finding | claim |
|---|---|---|
| major | primitive-obsession-0010 | pin-gate.ts holds two credentials, both typed `string \| null`, and enforces their invariants asymmetrically because nothing in the type says they are the same k |
| major | resilience-hole-0006 | `resolvePin` mints a 6-digit numeric PIN (10^6 space) whenever the Cloudflare tunnel is enabled, and that PIN is the ONLY thing standing between the public inte |

### INIT-07-patch-node-identity (P3) — Patch node id grammar → typed identity; {drumId,slot} trigger address object

3 findings · 0 critical · ~223 LOC

| sev | finding | claim |
|---|---|---|
| major | primitive-obsession-0008 | Patch node identity is a stringly-typed grammar `<kind>:<a>[:<b>]` (patch-topology.ts:110) with builders scattered across three modules, and the `output:` prefi |
| minor | data-clumps-0007 | `{drumId, slot}` is the repo's trigger address, and it already exists as an object in four separate core schemas (midiNoteMapSchema:73-77, oscMapSchema:80-84, d |
| minor | dead-code-0008 | The three id-builder arrows at patch-topology.ts:113/114/117 are each called exactly once, from the layout builder in the same file (:178, :189, :217). No other |

### INIT-08-kit-schema-split (P3) — kit-schema.ts: split schema surface / migration ladder / defaults

1 findings · 0 critical · ~560 LOC

| sev | finding | claim |
|---|---|---|
| major | divergent-change-0005 | One file holds three independent change axes: (1) the zod schema surface and its inferred types (lines 5-236), (2) the v1→v7 migration ladder — `shiftOutputHoop |

### INIT-11-telemetry-resilience (P3) — Telemetry transport + ingest Worker fault handling

2 findings · 0 critical · ~95 LOC

| sev | finding | claim |
|---|---|---|
| major | resilience-hole-0008 | `createHttpTransport` collapses every non-2xx into one opaque `Error('ingest responded ' + status)`, and `doShip` catches that identically regardless of status: |
| minor | resilience-hole-0010 | The Worker's `fetch` has no try/catch anywhere. Every `d1Store` method awaits a raw D1 call, and every `r2Store` method a raw R2 call, so a D1 rate-limit, a sch |

### INIT-12-kit-mirror-removal (P3) — Kit mirror removal (decided): client path deletion; server half pending Trent

1 findings · 0 critical · ~46 LOC

| sev | finding | claim |
|---|---|---|
| major | dead-code-0002 | PatchMirrorControl.svelte has no importer in apps/web/src. It was added by 06bcf7b ("S11: plumb setKitGlobal (mirror) end-to-end + Patch toolbar control") and d |

> **Decision:** Trent 2026-07-28: kit mirror is REMOVED by intent (option b of the finding's two fixes). Delete PatchMirrorControl.svelte + unreachable setKitGlobal client path. Trent 2026-07-29: the SERVER half goes too — input-router.ts:186 path, handlers/voice-input.ts:149, handlers/client-message.ts:524, kit-global-forwarding.test.ts, and any protocol setKitGlobal message type.

### INIT-09-ui-component-dedup (P4) — Design-system dedup: control contract, inspector chrome, MIDI-learn, ControllerStatusPanel split

6 findings · 0 critical · ~1236 LOC

| sev | finding | claim |
|---|---|---|
| major | data-clumps-0002 | Eight design-system primitives each independently declare the identical four-member control contract — `onChange?: (v: T) => void; disabled?: boolean; ariaLabel |
| major | divergent-change-0007 | A single 847-LOC 'presentational' inspector covers controller identity, discovery/adoption (including subnet recommendation and Adopt-by-IP), per-universe rx ta |
| minor | duplicated-code-0010 | The two canvas components carry identical copies of two `$effect` blocks: the IntersectionObserver visibility gate with the `hasBeenVisible` pre-layout guard (s |
| minor | duplicated-code-0013 | The same inspector-title treatment — `.ihead` (flex row, space-3 gap/padding, hairline bottom border), `.titles` (flex:1; min-width:0), `h3` (text-md/700/--ink) |
| minor | duplicated-code-0014 | The MIDI-learn affordance — a `.note-row` two-column grid plus a 29px-high `.learn` pill with its hover/active and disabled states — is written out identically  |
| minor | middle-man-0007 | Overlays.svelte's whole body is three sibling mounts (EffectGallery, ClipSettings, EnvelopeEditor), each of which reads its own open state off the store, so the |

### INIT-10-test-helper-dedup (P4) — Shared test harness: in-memory Storage (×37) + effects test helpers (×6)

2 findings · 0 critical · ~1070 LOC

| sev | finding | claim |
|---|---|---|
| major | duplicated-code-0001 | An identical ~19-line in-memory `Storage` implementation (length/key/getItem/setItem/removeItem/clear) is declared verbatim in 37 separate test files under apps |
| major | duplicated-code-0002 | batch-a/-b/-c/-d/-e.test.ts and effects.test.ts each declare their own copy of the same six helpers — `model(drums,hoopCount)` (parseKit+buildPixelModel), `tran |

### INIT-13-core-surface-trims (P4) — Small core API trims (pixel-grid wrappers, graph-integrity throwing form, listCanvasScenes)

1 findings · 0 critical · ~25 LOC

| sev | finding | claim |
|---|---|---|
| minor | speculative-generality-0011 | Phase 1's hunch that pixel-grid has no live caller is WRONG — buildPixelGrid, forEachPixelWithin and nearestPixelIdWithin are each used by real effects (rain-3d |


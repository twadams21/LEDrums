# 10 — Ranked Approval Table (rendered view; 10-ranked.json is canonical)

Baseline `3708648` · generated by `artifacts/phase10-rank.mjs`

Nothing structural executes until Trent approves rows. Rank is a recommendation — reorder freely.

| # | initiative | crit | ~LOC | risk | payoff | status | why here |
|---|---|---|---|---|---|---|---|
| 1 | INIT-03-io-output-resilience | 1 | 660 | high | high | **ready** | Closes the live-show-goes-dark silent failure (total error swallow in both output adapters) at the smallest cost on the table (660 LOC, 11 steps); zero protocol/UI change. Highest payoff per unit risk — land first. |
| 2 | INIT-04-server-runtime-hardening | 3 | 2500 | high | high | **ready** | Resolves 3 criticals including the unprotected render loop (one throw kills output silently) and module-scope boot fragility. Operational resilience second only to INIT-03, and independent of the engine consolidation. |
| 3 | INIT-01-single-render-stack | 1 | 2450 | high | high | **ready** | Removes an entire duplicated subsystem (legacy engine stack + sim.ts voice mirror, ~2450 LOC mostly deletion). Third despite scale because INIT-02/INIT-06 rework overlapping territory (trigger-lab store, core voice types) — landing this first shrinks what they must rebase onto. |
| 4 | INIT-02-store-decomposition | 3 | 2750 | medium | medium | **ready** | Resolves 3 criticals in the god-object cluster via a 23-commit strangler with a durable resting state after each. After INIT-01 because both rework trigger-lab; medium risk but the largest estimate (2750 LOC). |
| 5 | INIT-06-graph-node-type-model | 1 | 950 | high | medium | **ready** | Buys compiler-enforced totality over the 19-kind node model and fixes the live effect-arm dispatch bug. Last of the five because it touches persisted-show compatibility and benefits from INIT-01 having deleted the legacy consumers of these types. |

Each row's plan: see `plan_ref` — an adversarially-reviewed synthesis with per-step verification. Open questions inside each plan need answers before or during execution.

## Not ranked (retained in dropped)

**Executed this stint:** INIT-12-kit-mirror-removal — Executed this stint as fix batch-02 (commits a7c46eb + 792cb23, gates green 2968, Phase 6 review non-blocking): mirror field removed at every layer; setKitGlobal survives carrying live kit config.

**Light track, held for repair pass (all verdicts sound-with-repairs):** INIT-05-pin-auth-hardening, INIT-07-patch-node-identity, INIT-08-kit-schema-split, INIT-09-ui-component-dedup, INIT-10-test-helper-dedup, INIT-11-telemetry-resilience, INIT-13-core-surface-trims. Each has a plan in 07-plans/ and an attack in 08-refutations/; a cheap repair pass (one agent per initiative applying the attack's named repairs) promotes them into 09-synthesis for re-ranking.

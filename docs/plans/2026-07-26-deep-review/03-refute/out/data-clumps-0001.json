{"finding_id":"data-clumps-0001","refuted":false,"reasoning":"I checked the declarations and actual production call path. The same five values travel together through at least three call sites: VoiceEngine passes them positionally to applyEffectiveParams at packages/core/src/voice/engine.ts:711, passes them again within the CompositorFrame argument to compositor.render at packages/core/src/voice/engine.ts:713-717, and applyEffectiveParams forwards them in a ModSampleCtx literal to applyModulations at packages/core/src/voice/compositor.ts:71; render additionally converts the frame back into FrameModCtx at packages/core/src/voice/compositor.ts:195-201 and passes it through multiple modCtxFor calls. I found no semantic distinction that breaks the clump: this.bpm is assigned from transport.bpm at packages/core/src/voice/engine.ts:698, and the CC, OSC, and note tables are the identical engine fields in both paths.","criterion_applied":"co-travelling-params","counter_evidence":"none found","confidence":"high"}

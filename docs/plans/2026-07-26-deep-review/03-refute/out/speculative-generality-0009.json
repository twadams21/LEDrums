{"finding_id":"speculative-generality-0009","refuted":true,"reasoning":"I checked both barrels and their callers. Under the deletion test, removing the re-exports does not make the boundary complexity vanish: it redistributes direct `@ledrums/protocol` knowledge across dozens of server and web callers and splits cohesive imports that currently obtain a side-specific codec and its associated wire types from one module, as shown in `apps/server/src/main.ts:56-65` and `apps/web/src/lib/ws/client.ts:3-20`. The server and web modules are two real consumer adapters with opposite-direction decoding/encoding responsibilities, and their differing export lists match differing consumer needs rather than proving erroneous contract drift; therefore the forwarding is earning its keep as each adapter's local type surface.","criterion_applied":"deletion-test","counter_evidence":"Deleting the forwarding fans protocol imports back out across many callers; mixed runtime-and-type imports demonstrate the earned local seams at apps/server/src/main.ts:56-65, apps/server/src/handlers/client-message.ts:7, and apps/web/src/lib/ws/client.ts:3-20. The supposedly drifted web-only types are actually consumed through that web surface, e.g. VoiceStat at apps/web/src/lib/trigger-lab/store.svelte.ts:49, while server-specific types are consumed through the server surface.","confidence":"high"}

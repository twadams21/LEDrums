{"finding_id":"speculative-generality-0006","refuted":true,"reasoning":"I confirmed there are no production callers, but the exact deletion test still defeats the finding: deleting modulation-parity.ts would force its parity cases, phase matrix, and frozen historical formula to reappear across two independent callers—the core modulation regression suite and the web hydration-migration suite. Those suites exercise different adapters/paths (`mappingEnvValue` through the modulation model versus `migrateGraphEnvMaps` through graph hydration), so this is a real shared seam rather than a pass-through whose complexity vanishes; moving it may change API hygiene, but the specified criterion does not justify deleting or condemning the module.","criterion_applied":"deletion-test","counter_evidence":"packages/core/src/voice/modulation.test.ts:219-238 and apps/web/src/lib/trigger-lab/store/hydrate.envmap.test.ts:25-47 independently reuse the shared oracle against different execution paths; packages/core/package.json:6-8 also exposes only the root entry point, so the suggested testing subpath would add package-boundary machinery rather than eliminate the abstraction.","confidence":"high"}

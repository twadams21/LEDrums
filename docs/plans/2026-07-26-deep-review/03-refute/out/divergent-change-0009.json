{"finding_id":"divergent-change-0009","refuted":true,"reasoning":"The stated scatter is three edit sites across only two files: the interface is in packages/protocol/src/index.ts, while both the schema and lock assertion are in packages/protocol/src/schemas.ts:440-472. Git history contains only two post-introduction commits that add a hand-written payload, matching schema, and lock (c7bf94d for OscListenInfo and fbd63a2 for BackupSnapshotMeta); the only third commit touching both files is d5bed06, which introduced the schema system itself rather than evidencing another payload-field change. Although the two feature commits touched 26 and 10 files overall, those additional files implement the OSC and backup features and do not show that changing the payload declaration itself requires edits in four or more files. Therefore the required evidence of one logical change needing >=4 files across >=3 commits is absent.","criterion_applied":"change-scatter","counter_evidence":"packages/protocol/src/index.ts:293-303 contains OscListenInfo, while its schema and lock both remain in the single packages/protocol/src/schemas.ts file at lines 329-335 and 459; likewise BackupSnapshotMeta is declared at packages/protocol/src/index.ts:66-72 and its schema plus lock are both in packages/protocol/src/schemas.ts:229-234 and 472. Only commits c7bf94d and fbd63a2 demonstrate this pattern after d5bed06 introduced it, so neither the >=4-file requirement nor the >=3-commit evidence requirement is established.","confidence":"high"}

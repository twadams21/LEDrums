{"finding_id":"resilience-hole-0005","refuted":false,"reasoning":"I checked the cited broadcast, connection-admission, stats-production, preview-stream, and disconnect paths. `statsTimer` broadcasts every 10ms, `broadcastJson` and `broadcastBinary` gate only on OPEN state, and the server contains no `bufferedAmount`, ping/pong heartbeat, termination, or slow-consumer handling; close/error cleanup therefore does not prevent an OPEN but non-draining socket from accumulating queued data. The trigger is plausible without an attack: voice telemetry can contain up to 256 active voice summaries, and an authenticated remote phone or temporarily stalled TCP connection receives that 100 Hz JSON stream alongside the approximately 30 fps binary preview. `ClientRegistry.admit` is intentionally additive and has no capacity limit, while the PIN gate performs only equality checking and supplies no admission-rate or connection-count control.","criterion_applied":"unhandled-failure-path","counter_evidence":"none found; apps/server/src/main.ts:239-244 and apps/server/src/main.ts:429-432 send without backpressure checks, apps/server/src/main.ts:748-772 emits stats at 100 Hz, apps/server/src/client-registry.ts:45-54 admits additively without a cap, and packages/core/src/voice/voice-pool.ts:15 bounds voices at 256 but does not prevent a substantial repeated payload","confidence":"high"}

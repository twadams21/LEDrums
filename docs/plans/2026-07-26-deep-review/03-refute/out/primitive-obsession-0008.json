{"finding_id":"primitive-obsession-0008","refuted":false,"reasoning":"I checked the cited builders, their production callers, the Inspector decoder, and the graph-to-routing/wiring adapters. Although buildPatchTopology and its positional outputId builder appear to be legacy code with no production caller outside tests, the string primitive still represents patch-node identity and carries at least two invariants enforced across call sites: output payloads must round-trip an OutputConfig.id through buildZoneGraph/routingFromGraph/patchEditorFor, and prefixes plus payload shapes determine node stage, legal chain-wire source type, and decoded domain references such as 1-based hoop indices. The active graph also demonstrates the claimed presentation mismatch: output nodes are labelled by array position in patch-zones.ts, while describePatchNode reconstructs the Inspector title from the raw id payload.","criterion_applied":"domain-concept-with-invariants","counter_evidence":"The positional outputId builder is confined to the apparently legacy buildPatchTopology path, which has no production caller found outside apps/web/src/lib/app/patch-topology.test.ts; the active view instead calls buildZoneGraph at apps/web/src/lib/app/views/PatchGraphView.svelte:105-112 and builds output IDs from OutputConfig.id at apps/web/src/lib/app/patch-zones.ts:210-212. This narrows the alleged runtime collision but does not defeat the criterion.","confidence":"high"}

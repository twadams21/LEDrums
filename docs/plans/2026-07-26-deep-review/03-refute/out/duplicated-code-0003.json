{"finding_id":"duplicated-code-0003","refuted":false,"reasoning":"I read both controller implementations, their serializers/deserializers, exported-plan usages, and the shared caller. The clone exceeds the stated threshold: the two classes contain matching state fields and multiple contiguous method bodies of at least 12 lines with only library type and codec identifiers substituted, and both callers apply the resulting plans with the same reconciliation sequence at apps/web/src/lib/trigger-lab/shows-controller.svelte.ts:574-595. The codecs have different validation details, but those differences are already isolated behind equivalent serialize/deserialize contracts and do not change the controllers' reconciliation semantics; consolidating the two exported classes and two plan unions into one generic controller and one plan union would reduce rather than merely relocate the interface.","criterion_applied":"duplication-threshold","counter_evidence":"none found","confidence":"high"}

You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The SAME discriminant switched in >=3 places, so adding a variant needs lockstep edits with nothing enforcing it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: repeated-switches-0005
lens: repeated-switches
criterion: repeated-discriminant
severity: major (author confidence: high)
locations:
  apps/web/src/lib/app/docks/Inspector.svelte:73-141 (node-kind inspector ladder)
  apps/web/src/lib/app/views/TriggerNode.svelte:183-199 (source-thumbnail ladder)
  apps/web/src/lib/app/views/AddPalette.svelte:85-129 (preview-node ladder)
  apps/web/src/lib/app/views/add-node-taxonomy.ts:64-93 (buildAddGroups Modulate group)

claim: Adding a node kind requires coordinated edits to at least four UI enumerations of the same discriminant, none checked by the compiler: the Inspector's 12-branch `{#if node.kind === …}` ladder (73-141), TriggerNode's 6-branch source-thumbnail ladder (183-199), AddPalette's preview ladder (85-129), and add-node-taxonomy's palette items (64-93). Svelte `{#if}` chains carry no exhaustiveness obligation, so a missed arm renders nothing at all — the node appears in the graph with no inspector and no preview, with no build or test error.

evidence the author gave:
  [grep] grep -n "kind ===" apps/web/src/lib/app/docks/Inspector.svelte :: apps/web/src/lib/app/docks/Inspector.svelte lines 73,75,83,91,99,106,113,120,132,134,136,138 — twelve `node.kind === '<literal>'` branches over the node-kind discriminant.
  [grep] grep -n "kind ===" apps/web/src/lib/app/views/TriggerNode.svelte :: apps/web/src/lib/app/views/TriggerNode.svelte lines 183,191,193,195,197,199 — the same six modulation-source literals again.
  [read]  :: apps/web/src/lib/app/views/AddPalette.svelte:85-91 and :114-129 branch on `kind` / `it.previewKind` literals to build preview nodes.
  [read]  :: apps/web/src/lib/app/docks/inspectors/ contains one <Kind>NodeInspector.svelte per kind (Cc, Delay, Envelope, Lfo, Modifier, Note, Osc, Output, Play, RandomMod, Scope, Container) — the components exist; only the mapping is hand-wired.
  [read]  :: Contrast apps/web/src/lib/app/views/trigger-node-meta.ts:32/63/87 which already maps kind -> icon/tint/label through `Record<NodeKind, …>` and IS enforced.

proposed fix: Add one `Record<NodeKind, { inspector: Component | null; preview: PreviewKind | null }>` next to the existing kindIcon/tint/kindLabel Records in trigger-node-meta.ts, and drive the Inspector, TriggerNode thumbnail and AddPalette preview off it. The compiler then rejects a new kind with no entry, and four ladders collapse to one table.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"repeated-switches-0005",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"repeated-discriminant",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   One logical change requires edits in >=4 files, evidenced from >=3 commits in git log.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: divergent-change-0004
lens: divergent-change
criterion: change-scatter
severity: major (author confidence: high)
locations:
  apps/server/src/main.ts:1-801 (module top-level)

claim: main.ts is 801 lines of module-level mutable state and closures over it (`host`, `voiceHost`, `liveShowLibrary`, `liveSongLibrary`, three autosavers, `oscListen`, `tunnelControl`, `pinGate`, stats timer, monitor sink). Because everything is wired at module scope, every unrelated feature must edit this file: the last 20 commits touching it add OSC listen reporting, native-MIDI decoupling, pre-risk snapshots, observability dedup keys, output port reconciliation, SnapshotStore wiring, controller admin-password, controller test-data, tunnel start/stop, song-library messages, and per-voice stats. There is no seam a new feature can attach to without editing the root.

evidence the author gave:
  [git-history] for c in $(git log --format='%h' -20 -- apps/server/src/main.ts); do git log -1 --format='%h %s' $c; done :: 20 commits touching main.ts, all with distinct reasons: c7bf94d (OSC), 81b2a8f (native MIDI), e361d8a (snapshots), a6ef21f (observability dedup), b950a4d (output port fallback), fbd63a2 (SnapshotStore), 2dee93a/de6f7e7 (observability), ecd31e1/da3342d (controller auth + subnet), bd5a928 (stats caden
  [read]  :: main.ts:157-200 constructs host, voiceHost, autosaver, showLibraryLoad/liveShowLibrary, songLibraryLoad/liveSongLibrary and showLibraryAutosaver as module-level `const`/`let`, and stateMessage() (main.ts:446-462) closes over all of them — so a new persisted slot cannot be added anywhere else.
  [grep] grep -n "host\.|voiceHost" apps/server/src/main.ts :: 31 references to `host.`/`voiceHost` inside main.ts alone, spread from line 157 to line 790.

proposed fix: Extract a composable app factory: `createServerApp(deps) -> { stateMessage, broadcast, shutdown }` holding the currently module-level state as fields, with the persisted slots (project / show library / song library / snapshots) behind one `PersistedSlots` registry so adding a slot is one registration rather than five edits (loader, live let, autosaver, stateMessage field, shutdown flush). main.ts then becomes env parsing + `createServerApp(...)` + `boot(...)`.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"divergent-change-0004",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"change-scatter",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
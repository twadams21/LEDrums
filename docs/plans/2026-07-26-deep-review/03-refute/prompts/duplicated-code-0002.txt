You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   >=2 sites, >=12 lines each, >=90% token similarity, SEMANTICALLY equivalent. Consolidation must REDUCE total interface, not relocate it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: duplicated-code-0002
lens: duplicated-code
criterion: duplication-threshold
severity: major (author confidence: high)
locations:
  packages/core/src/effects/batch-a.test.ts:14-70 (model/transport/ctx/render/litCount)
  packages/core/src/effects/batch-c.test.ts:14-75 (model/transport/ctx/trig/render/litCount)
  packages/core/src/effects/effects.test.ts:51-100 (model/transport/ctx/trig/render/litCount)

claim: batch-a/-b/-c/-d/-e.test.ts and effects.test.ts each declare their own copy of the same six helpers — `model(drums,hoopCount)` (parseKit+buildPixelModel), `transport(beat,timeMs)`, `ctx(m,opts)`, `trig(...)`, `render(effect,m,c,params,state)` and `litCount(fb)`/`allFiniteInRange(fb)` — differing only in a default argument (`drums = 1` vs `2`). This is the whole of the 57-clone core count's top band: batch-c vs batch-d 63 lines/480 tokens, batch-a vs batch-b 51/434, batch-a vs batch-e 48/397, batch-c vs effects.test 45/323. The helpers are pure builders over immutable inputs, so a shared harness cannot couple the suites.

evidence the author gave:
  [read]  :: Read packages/core/src/effects/batch-a.test.ts:14-70 and effects.test.ts:51-100 — `transport()` is byte-identical (`{ timeMs, beat, bar: Math.floor(beat / 4), beatInBar: beat % 4, bpm: 120, beatsPerBar: 4, playing: true }`), as are `ctx()`, `render<S>()` and `litCount()`.
  [grep] for f in batch-b batch-c batch-d batch-e; do sed -n '12,70p' $f.test.ts | grep -n '^function'; done :: All five batch files declare the same function set at the same relative offsets; only batch-c/-d/-e add the identical `trig()` builder and batch-e swaps litCount for litIds.
  [tool-output]  :: jscpd-report.json rows: batch-c.test.ts:12 vs batch-d.test.ts:12 = 63 lines/480 tokens (largest clone in the repo); batch-a:12 vs batch-b:12 = 51/434; batch-a:12 vs batch-e:11 = 48/397; batch-c:12 vs effects.test.ts:50 = 45/323; batch-a:14 vs batch-c:14 = 34/243.
  [read]  :: ls packages/core/src — there is no test-support module in the package; the harness has nowhere shared to live today.

proposed fix: Add packages/core/src/effects/effect-test-harness.ts exporting model/transport/ctx/trig/render/litCount/litIds/allFiniteInRange (with the current default args as parameters), and import it from all six suites. Test-only module, not re-exported from packages/core/src/index.ts, so the package's public surface is unchanged.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"duplicated-code-0002",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"duplication-threshold",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
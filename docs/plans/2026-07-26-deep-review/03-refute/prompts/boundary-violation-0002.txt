You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   packages/core reaching Node/DOM/@ledrums/io, or a runtime (not type-only) cycle that actually matters.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: boundary-violation-0002
lens: boundary-violation
criterion: import-boundary
severity: major (author confidence: high)
locations:
  .github/workflows/ci.yml:16-27 (jobs.checks)
  AGENTS.md:7-10 (Non-Negotiables)

claim: AGENTS.md states three import-boundary rules as non-negotiable (core imports no Node/DOM/IO; all IO lives behind packages/io interfaces; core never imports io). CI runs only `pnpm typecheck` and `pnpm test`. There is no ESLint config anywhere in the repo, no `no-restricted-imports`, no dependency-cruiser, and no madge/cycle gate — madge, knip and depcheck exist as devDependencies but are invoked only ad hoc, not from any script in CI. The rules therefore hold today purely by author discipline, and as finding 1 shows the typecheck does not in fact cover the DOM half of the rule.

evidence the author gave:
  [read]  :: .github/workflows/ci.yml: the `checks` job steps are checkout, pnpm setup, node setup, `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm test`. The `desktop` job builds the sidecar and cargo. No lint or boundary step in either.
  [grep] ls -a /Users/trent/Documents/dev/ledrums :: No .eslintrc*, eslint.config.*, or .dependency-cruiser.* at the repo root. knip.json exists but only lists entry points for unused-export detection, not boundary rules.
  [tool-output]  :: docs/plans/2026-07-26-deep-review/00-raw/depcheck.json shows madge/knip/depcheck as root devDependencies — tooling present, but nothing in package.json scripts or ci.yml runs them.

proposed fix: Add a `boundaries` step to the CI `checks` job that fails on violation. Minimal version needs no new dependency: a vitest test in packages/core that globs src/**/*.ts and asserts no import specifier matches /^node:|^@ledrums\/(io|protocol)$/, plus finding 1's `lib: ["ES2022"]` to cover DOM. Fuller version: add a dependency-cruiser config with three forbidden rules (core→node builtins, core→@ledrums/io, core→@ledrums/protocol) and run `depcruise` in CI.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"boundary-violation-0002",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"import-boundary",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
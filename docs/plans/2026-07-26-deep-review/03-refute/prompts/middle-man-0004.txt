You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: middle-man-0004
lens: middle-man
criterion: deletion-test
severity: major (author confidence: medium)
locations:
  packages/core/src/voice/index.ts:12-19 (export * from './types' … './render-plan')
  packages/core/src/voice/index.ts:29-74 (curated export blocks)

claim: The module deliberately curates exports for eval-graph, lfo, modifier-graph, modulation-graph, compositor, engine and delay — but blanket-exports types, diagnostics, easing, envelope, modulation, scope, graph-integrity, render-plan and prng. The nine blanket lines publish 106 top-level symbols, of which 29 are never referenced anywhere in apps/web or apps/server: the whole RenderPlan* cache family (RenderPlan, RenderPlanCache, RenderPlanChild, RenderPlanNode, RenderPlanNodeCategory, createRenderPlanCache, renderPlanSignature, nodeCategory), the graph-integrity validators (validateTriggerGraphIntegrity, assertTriggerGraphIntegrity, TriggerGraphIntegrityResult, TriggerGraphIssue, TriggerGraphIssueCode, TriggerGraphSystemActions), hoop-target internals (HoopTarget, ParseHoopTargetOptions, EmptyHoopFallback), and more. The result is that nobody can tell which of voice's ~150 symbols are contract and which are leakage — the barrel is a middle-man that has stopped making a decision.

evidence the author gave:
  [tool-output]  :: python3 scan of the 9 blanket-exported voice modules counted 106 top-level `export (function|const|class|interface|type|enum)` symbols; cross-grepping each name against apps/web/src and apps/server/src found 29 with zero references outside packages/core, listed above.
  [read]  :: packages/core/src/voice/index.ts:12-19 (nine `export *` lines) sitting directly above line 20's hand-picked block and lines 29-74's six more curated blocks — the two styles are interleaved with no stated rule.
  [read]  :: Phase-1 core-voice-fx map records the same module as 'Curates the package-facing export surface for the voice subsystem, mixing blanket re-exports with hand-picked named ones.'

proposed fix: Convert the nine `export *` lines to curated named blocks matching the style already used from line 29 down, listing only what apps/web and apps/server actually import. Anything not listed stays module-internal and becomes free to change. The 29-symbol list above is the starting deletion candidate set; each needs a per-symbol judgement call (some, e.g. the graph-integrity validators, may be intended contract that is simply not wired yet — that is a decision for a human, not a codemod).

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"middle-man-0004",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
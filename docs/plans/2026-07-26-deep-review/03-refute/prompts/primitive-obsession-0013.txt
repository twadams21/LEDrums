You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The primitive stands for a domain concept with >=2 invariants enforced at call sites.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: primitive-obsession-0013
lens: primitive-obsession
criterion: domain-concept-with-invariants
severity: minor (author confidence: medium)
locations:
  apps/web/src/lib/ws/client.ts:40-52 (WSCallbacks.onState)
  apps/web/src/lib/trigger-lab/store.svelte.ts:1322 (onState handler)

claim: The server's whole state push is delivered as nine positional arguments (`project, model, effects, projects, output, showLibrary, songLibrary, tunnel, osc`) rather than one named `ServerState` object. Three consecutive slots are nullable (`ShowLibraryBlob | null`, `SongLibraryBlob | null`, `TunnelInfo | null`) and two consecutive slots are arrays (`EffectSpec[]`, `string[]`) — so at the call sites that pass empties or nulls, which is every test (store.song-library.test.ts:250 `(project, MODEL, [], [], OUTPUT, null, blob, null, OSC_LISTEN)`, store.pin.test.ts:81), the type system provides no positional protection at all. Adding a tenth field, or reordering two nullable slots, is a silent mis-wire. The sole real consumer (store.svelte.ts:1322) already destructures by renaming and discards two with `_effects, _projects`, which is the tell that a record was wanted.

evidence the author gave:
  [read]  :: apps/web/src/lib/ws/client.ts:40-52 — nine-parameter callback signature; slots 6-8 are all `X | null`.
  [grep] grep -rn 'onState' apps/web/src --include=*.ts --include=*.svelte :: apps/web/src/lib/trigger-lab/store.svelte.ts:1322 `onState: (project, model, _effects, _projects, output, showLibrary, songLibrary, tunnel, osc) => {` — the only production consumer, ignoring two positions.
  [read]  :: apps/web/src/lib/trigger-lab/store.song-library.test.ts:250 and store.pin.test.ts:81 both invoke it with bare `[], [], …, null, …, null` literals, where TypeScript's inference offers no positional discrimination.

proposed fix: Change the callback to `onState?: (state: ServerState) => void` where `ServerState` is a named interface with the nine fields. The wire message already has named keys on the server side, so the change is confined to the client's decode and the single store handler plus two tests; every future field becomes an additive named property that cannot be mis-positioned.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"primitive-obsession-0013",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"domain-concept-with-invariants",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
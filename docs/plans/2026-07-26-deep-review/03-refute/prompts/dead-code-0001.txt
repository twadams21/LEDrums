You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   Unreachable from ANY entry point AND no dynamic-access pattern (string-keyed registry, dynamic import, reflection, template literal) resolves to it. NOTE: a symbol used only inside its own module is NOT deletable — but dropping its `export` IS valid and reduces interface by 1.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: dead-code-0001
lens: dead-code
criterion: unreachable-and-no-dynamic-access
severity: major (author confidence: high)
locations:
  apps/web/src/lib/app/views/PatchClipboardToolbar.svelte:1-191 (PatchClipboardToolbar)
  apps/web/src/lib/app/views/PatchDiffDialog.svelte:1-192 (PatchDiffDialog)

claim: Both components are unreachable from any entry point. PatchClipboardToolbar has zero importers anywhere in apps/web/src; PatchDiffDialog's only importer is PatchClipboardToolbar itself (line 13 / line 124), so the pair forms a closed dead cluster. Together they are 383 LOC of shipped-but-unrenderable UI, plus they were the only production consumer of trigger-lab/patch-diff.ts (diffPatch is otherwise referenced only by patch-diff.test.ts).

evidence the author gave:
  [tool-output]  :: knip.json lists both files under `files` (unused files) for apps/web: entries 5 and 6 of the issues array.
  [grep] grep -rn "PatchClipboardToolbar|PatchMirrorControl|PatchDiffDialog" apps/web/src/ :: Only three hits total, all *inside* PatchClipboardToolbar.svelte itself (its own doc comment line 4, the import at line 13, the usage at line 124). No other src file names either component.
  [git-history] git log --oneline -S"PatchClipboardToolbar" -- apps/web/src/lib/app/views/PatchGraphView.svelte :: Added by fdfd18c ("S45: Patch copy/paste UI"), removed from PatchGraphView by 39b7d6b ("D1b Step 2 — Patch Graph v2 zone canvas"). The v2 canvas rewrite dropped the mount and left the files behind.
  [grep] grep -rn "import(" apps/web/src :: The only real dynamic imports in apps/web are ./lib/styleguide/Styleguide.svelte (main.ts:13), ./lib/app/shot-seam (App.svelte:30) and @tauri-apps/api/{core,event}. No string-keyed component registry or template-literal import resolves to either file.

proposed fix: Delete apps/web/src/lib/app/views/PatchClipboardToolbar.svelte and PatchDiffDialog.svelte. Then decide separately whether patch copy/paste is a feature that should be re-mounted in the v2 Patch canvas; if not, trigger-lab/patch-diff.ts and patch-diff.test.ts go with them. Also drop the two component entries from the styleguide/design-system source so docs/design-system.html stops advertising them.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"dead-code-0001",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"unreachable-and-no-dynamic-access",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
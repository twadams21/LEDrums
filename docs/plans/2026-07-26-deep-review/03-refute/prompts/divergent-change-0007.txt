You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   One logical change requires edits in >=4 files, evidenced from >=3 commits in git log.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: divergent-change-0007
lens: divergent-change
criterion: change-scatter
severity: major (author confidence: high)
locations:
  apps/web/src/lib/app/docks/inspectors/ControllerStatusPanel.svelte:1-847 (ControllerStatusPanel)

claim: A single 847-LOC 'presentational' inspector covers controller identity, discovery/adoption (including subnet recommendation and Adopt-by-IP), per-universe rx tables, health/takeover banners, admin-password auth, and test-pattern controls. Each is a distinct change axis and the history shows them arriving as separate commits into the same file: 0360c10 (takeover banner + test-pattern controls), de62307 (derivations + panel creation), 4731718 (admin-password field), da3342d (subnet recommendation + Adopt-by-IP), 6c12f97 (PixLite status hardening), 8dc92b9 (partial extraction of AdoptByIpRow + UniverseRxTable). The 8dc92b9 extraction confirms the seam exists and was only partly cut.

evidence the author gave:
  [tool-output] wc -l apps/web/src/lib/app/docks/inspectors/ControllerStatusPanel.svelte :: wc -l apps/web/src/lib/app/docks/inspectors/ControllerStatusPanel.svelte = 847.
  [git-history] for c in $(git log --format='%h' -30 -- apps/web/src/lib/app/docks/inspectors/ControllerStatusPanel.svelte); do git log -1 --format='%h %s' $c; done :: Commits touching the file, each for an unrelated reason: 8dc92b9 refactor(controller): extract AdoptByIpRow + UniverseRxTable composites; da3342d subnet recommendation + Adopt-by-IP; 4731718 admin-password field; 0360c10 S49 takeover banner + test-pattern controls; de62307 S48 derivations/panel; 6c1

proposed fix: Finish the extraction 8dc92b9 started, along the five axes: `ControllerIdentityCard`, `ControllerDiscoveryPanel` (subnet recommendation + adoption, wrapping the existing AdoptByIpRow), `ControllerUniverseRx` (already partly extracted as UniverseRxTable), `ControllerHealthBanner` (takeover + auth state), `ControllerTestPatterns`. Register each in apps/web/src/lib/styleguide/sections/ per AGENTS.md and regenerate docs/design-system.html in the same change. ControllerStatusPanel becomes the composition + data-derivation shell.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"divergent-change-0007",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"change-scatter",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   packages/core reaching Node/DOM/@ledrums/io, or a runtime (not type-only) cycle that actually matters.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: boundary-violation-0001
lens: boundary-violation
criterion: import-boundary
severity: major (author confidence: high)
locations:
  packages/core/tsconfig.json:1-9 (compilerOptions)
  tsconfig.base.json:7 (lib)
  packages/core/src/voice/engine.ts:813-815 (perfNow)

claim: AGENTS.md declares `packages/core` must import no Node and no DOM. Node is in fact mechanically blocked (core/tsconfig.json sets `"types": []`, and pnpm's strict workspace linking means `@ledrums/io` would not resolve from core at all), but DOM is not: core inherits `"lib": ["ES2022", "DOM", "DOM.Iterable"]` from tsconfig.base.json, so `document`, `window`, `fetch`, `localStorage` and friends would typecheck silently inside the pure package. The hole is not hypothetical — `voice/engine.ts:814` already reaches for the ambient `performance` global, whose type comes only from lib.dom.d.ts. It is the single reference standing between core and a DOM-free lib setting.

evidence the author gave:
  [read]  :: packages/core/tsconfig.json extends ../../tsconfig.base.json and overrides only outDir/rootDir/types/moduleResolution — it does not narrow `lib`. tsconfig.base.json line 7: "lib": ["ES2022", "DOM", "DOM.Iterable"].
  [grep] grep -rnE "\\b(document|window|navigator|localStorage|HTMLElement|CanvasRenderingContext2D|requestAnimationFrame|OffscreenCanvas|ImageData|WebSocket|globalThis|process\\.env|Buffer)\\b" packages/core/src --include="*.ts" :: Every hit is inside a comment (the word "window" in freeze.ts/strobe.ts, "document" in canvas/*.ts prose). No DOM API is actually called.
  [grep] grep -rnE "\\b(console|structuredClone|setTimeout|TextEncoder|URL|crypto|performance|Blob|AbortController)\\b" packages/core/src --include="*.ts" | grep -v test :: Exactly one real usage: packages/core/src/voice/engine.ts:814 `return typeof performance !== 'undefined' && typeof performance.now === 'function' ? performance.now() : 0;`
  [grep] grep -rhoE "from '[^.'][^']*'" packages/core/src --include="*.ts" | sort | uniq -c :: Core's only non-relative imports are 'vitest' (69, test files) and 'zod' (2). Zero node: imports, zero @ledrums/io imports — the Node half of the rule holds today.

proposed fix: Add `"lib": ["ES2022"]` to packages/core/tsconfig.json's compilerOptions, and rewrite perfNow to reach the host clock through globalThis (an ES2020 lib symbol) instead of the DOM ambient: `const p = (globalThis as { performance?: { now?: () => number } }).performance; return typeof p?.now === 'function' ? p.now() : 0;`. The feature-detect fallback is preserved verbatim; only the type source changes. `pnpm --filter @ledrums/core typecheck` then becomes a real purity gate.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"boundary-violation-0001",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"import-boundary",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   Unreachable from ANY entry point AND no dynamic-access pattern (string-keyed registry, dynamic import, reflection, template literal) resolves to it. NOTE: a symbol used only inside its own module is NOT deletable — but dropping its `export` IS valid and reduces interface by 1.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: dead-code-0010
lens: dead-code
criterion: unreachable-and-no-dynamic-access
severity: minor (author confidence: high)
locations:
  apps/web/src/lib/trigger-lab/store/ids.ts:20 (reserveId)
  apps/web/src/lib/trigger-lab/store/transport.ts:17 (showSig)
  apps/web/src/lib/trigger-lab/store/graphs.ts:16-47 (OUTPUT_ANCHOR_ID, firstEffectId)
  apps/web/src/lib/trigger-lab/store/canvas-scenes.ts:22 (CANVAS_BUS_ID)
  apps/web/src/lib/trigger-lab/sim.graph-compilation.ts:24 (blockChildren)
  apps/web/src/lib/trigger-lab/clipdoc.ts:453 (makeDefaultMint)

claim: Each of these six symbols is called exactly once or twice, always from within its own declaring module: reserveId (ids.ts:20 → :29), showSig (transport.ts:17 → :33,:39), firstEffectId (graphs.ts:47 → :57), CANVAS_BUS_ID (canvas-scenes.ts:22 → :50), blockChildren (sim.graph-compilation.ts:24 → :166), makeDefaultMint (clipdoc.ts:453 → :507). graphs.ts's OUTPUT_ANCHOR_ID is likewise unread across modules — sim.graph-compilation.ts:147 and packages/core/src/voice/graph-integrity.ts:38 each declare their own private copy of the same literal rather than importing it. These are un-exports, not deletes.

evidence the author gave:
  [tool-output]  :: knip.json flags reserveId (ids.ts:20), showSig (transport.ts:17), firstEffectId + OUTPUT_ANCHOR_ID (graphs.ts:47/:16), CANVAS_BUS_ID (canvas-scenes.ts:22), blockChildren (sim.graph-compilation.ts:24), makeDefaultMint (clipdoc.ts:453) as unused exports.
  [grep] grep -rn per-symbol across apps/ and packages/ (*.ts, *.svelte) :: Every symbol's hits are confined to its own file. OUTPUT_ANCHOR_ID's 13 hits split into three independent private declarations (graphs.ts:16, sim.graph-compilation.ts:147, core/voice/graph-integrity.ts:38) plus their local uses — no cross-module import.

proposed fix: Drop the `export` keyword from all seven symbols. Note the separate (non-dead-code) smell surfaced here: three modules each hard-code OUTPUT_ANCHOR_ID='output' privately — worth routing to the duplication lens rather than fixing under this finding.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"dead-code-0010",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"unreachable-and-no-dynamic-access",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   >=3 parameters travelling together across >=3 call sites.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: data-clumps-0005
lens: data-clumps
criterion: co-travelling-params
severity: minor (author confidence: high)
locations:
  apps/web/src/lib/trigger-lab/store/graph-wiring.ts:74-80 (classifyConnection)
  apps/web/src/lib/trigger-lab/store/graph-wiring.ts:95-102 (classifyReconnect)
  apps/web/src/lib/trigger-lab/store/graph-wiring.ts:143-150 (canConnect)
  apps/web/src/lib/trigger-lab/store/graph-wiring.ts:157-165 (canReconnect)
  apps/web/src/lib/app/views/TriggerGraphView.svelte:565-579 (drop-target validity)
  apps/web/src/lib/trigger-lab/store.svelte.ts:2678-2726 (connect / reconnect)

claim: `fromId, toId, fromPort?, toPort?` travel as a four-slot positional tuple through classifyConnection, classifyReconnect, canConnect, canReconnect and the private sameSlot helper, and are re-spelled at 8 call sites. Two of the four slots are `string | undefined` in adjacent positions, so a from/to port transposition type-checks. TriggerGraphView.svelte:565 and :573 deliberately swap fromId/toId to validate a reverse drag (`canConnect(g, toId, from.nodeId, undefined, toPort)`) — exactly the manoeuvre a named `WireEnds { from, to, fromPort?, toPort? }` would make legible and a positional tuple makes an easy transposition bug.

evidence the author gave:
  [grep] grep -rn 'canConnect(|classifyConnection(|canReconnect(|classifyReconnect(|sameSlot(' apps/web/src packages :: 17 hits across 3 non-test files; 5 declaration sites all carrying the identical 4-slot tail, 8 call sites re-spelling it, including 5 in TriggerGraphView.svelte alone.
  [read]  :: apps/web/src/lib/trigger-lab/store/graph-wiring.ts:150 and :165 — canConnect/canReconnect are pure pass-throughs whose entire body is the same 4-slot forward: `classifyConnection(graph, fromId, toId, fromPort, toPort) === null`.

proposed fix: Add `export interface WireEnds { from: string; to: string; fromPort?: string; toPort?: ToPort }` in graph-wiring.ts. Signatures become `classifyConnection(graph: TriggerGraph, w: WireEnds)`, `classifyReconnect(graph, edgeId, w)`, and the two `can*` verdicts likewise. Call sites in TriggerGraphView.svelte become named-field literals, which makes the deliberate reverse-drag swap self-documenting.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"data-clumps-0005",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"co-travelling-params",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
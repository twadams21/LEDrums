You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: speculative-generality-0008
lens: speculative-generality
criterion: deletion-test
severity: major (author confidence: high)
locations:
  apps/web/src/lib/trigger-lab/sim.envelopes.ts:15-47 (type aliases + value re-exports of voice.*)
  apps/web/src/lib/trigger-lab/sim.ts:29-55 (re-export block "so the public ./sim API is unchanged")

claim: sim.envelopes.ts contains almost no behaviour: lines 17-24 are eight `export type X = voice.X` aliases, lines 32-47 destructure eleven values straight off `voice` and re-export them. The only original code is `ENV_KINDS`, `clampUnit`, and a `ParamSpec`/`ParamValue` pair that the file's own comment says is "structurally identical to the core type". sim.ts then re-exports twenty of those same symbols again (lines 30-53) with the comment "Re-export the extracted modules so the public `./sim` API is unchanged" — a second aliasing hop whose stated goal is import-path stability inside a single app that has no external consumers. The scan for unused exports flags `ENV_KINDS` (sim.envelopes.ts:49) as referenced by nothing outside its own file. So the app has a three-hop path — `@ledrums/core` → sim.envelopes → sim → 134 consumer files — where every hop is identity. Deletion test: delete sim.envelopes and the sim.ts block, and complexity does not reappear across the consumers; each one imports the type from `@ledrums/core` directly, which is where the truth already lives.

evidence the author gave:
  [read]  :: sim.envelopes.ts read in full: lines 17-24 are pure `export type X = voice.X`; lines 32 and 35-47 are `export const { … } = voice`. The file's own header calls it single-sourcing and the value re-exports carry the comment "proof there is one implementation, not a copy".
  [read]  :: sim.ts:28-55 — "Re-export the extracted modules so the public `./sim` API is unchanged" followed by twenty symbol re-exports plus two `export *` lines, all originating in core.
  [tool-output] unused-export scan over all non-test .ts/.svelte files, matching `export (function|class|const|interface|type|enum) NAME` against a token index of every other file :: Reported `apps/web/src/lib/trigger-lab/sim.envelopes.ts:49 ENV_KINDS testfiles=0 infile=1` — declared, exported, referenced by nothing.
  [grep] rg -l "trigger-lab/sim'|from './sim'|from '../sim'|sim.envelopes|sim.graph-compilation|sim.trigger-source" apps/web/src | wc -l :: 134 files route through the sim barrel — the blast radius of the indirection, and the reason it has never been unwound.

proposed fix: Delete the aliasing. Run a codemod that rewrites `import { Envelope, sampleEnvelope, … } from './sim'` (and './sim.envelopes') to `import { voice } from '@ledrums/core'` for every symbol that sim.envelopes merely forwards, then delete sim.envelopes.ts's alias/re-export sections and the sim.ts re-export block, keeping only genuinely web-local code (clampUnit, and ParamSpec if it truly diverges from voice.ParamSpec — if it does not, delete it too and import the core type). Delete ENV_KINDS outright. This is mechanical but wide; it belongs on the structural plan, not in a drive-by.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"speculative-generality-0008",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
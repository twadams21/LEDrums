You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   Unreachable from ANY entry point AND no dynamic-access pattern (string-keyed registry, dynamic import, reflection, template literal) resolves to it. NOTE: a symbol used only inside its own module is NOT deletable — but dropping its `export` IS valid and reduces interface by 1.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: dead-code-0014
lens: dead-code
criterion: unreachable-and-no-dynamic-access
severity: minor (author confidence: medium)
locations:
  apps/server/src/ws-protocol.ts:10-31 (BackupReason, MonitorEventType, SerializedDrum, TunnelStatus, VoiceStats)

claim: ws-protocol.ts:10-31 is a back-compat `export type { … } from '@ledrums/protocol'` barrel. Five of its names are never imported from this path — consumers reach for @ledrums/protocol directly instead (e.g. apps/server/src/tunnel-control.ts:1 imports TunnelStatus straight from @ledrums/protocol; apps/web/src/lib/ws/client.ts:19 gets VoiceStats via its own protocol-types.ts). The barrel's stated purpose ("so the server's existing ./ws-protocol import paths keep working") no longer holds for these five entries.

evidence the author gave:
  [tool-output]  :: knip.json: apps/server/src/ws-protocol.ts → types: [BackupReason:11, MonitorEventType:23, SerializedDrum:24, TunnelStatus:30, VoiceStats:31].
  [grep] grep -rn "\bVoiceStats\b|\bTunnelStatus\b" apps packages --include=*.ts --include=*.svelte :: TunnelStatus is imported at apps/server/src/tunnel-control.ts:1 from '@ledrums/protocol', not from './ws-protocol'. VoiceStats reaches the web client via apps/web/src/lib/ws/protocol-types.ts:25 and client.ts:19 — again not through the server barrel.
  [read]  :: Read apps/server/src/ws-protocol.ts:1-35 — the block is a pure type re-export with an explicit back-compat comment, so removing entries is behaviour-free at runtime.

proposed fix: Delete the five unused names from the `export type { … }` block at ws-protocol.ts:10-31, keeping the ones with live './ws-protocol' importers. Confidence is medium rather than high because the block is explicitly documented as a compatibility shim — confirm with a typecheck that no server file still resolves these names through it before trimming.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"dead-code-0014",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"unreachable-and-no-dynamic-access",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
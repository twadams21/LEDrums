You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The SAME discriminant switched in >=3 places, so adding a variant needs lockstep edits with nothing enforcing it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: repeated-switches-0009
lens: repeated-switches
criterion: repeated-discriminant
severity: minor (author confidence: medium)
locations:
  packages/core/src/voice/eval-graph.ts:389-589 (evalGraphGen3FromPlan traversal switch)
  packages/core/src/voice/render-plan.ts:49-78 (nodeCategory)

claim: The Phase 1 lead is half right. render-plan.ts:49 nodeCategory IS compiler-enforced (no default, non-undefined declared return type under `strict`), so plan categorisation cannot silently miss a kind. The traversal switch at eval-graph.ts:389 sits in a void while-loop with no default and no never-assertion, so it is NOT enforced: a new NodeKind gets a plan category but no traversal arm and becomes an invisible dead end — a route that reaches it produces no light and raises no issue. The two must be edited in lockstep and only one of them tells you.

evidence the author gave:
  [read]  :: packages/core/src/voice/eval-graph.ts:389 `switch (node.kind) {` … final arm at :582-588 `case 'randomMod': case 'envelope': case 'lfo': case 'cc': case 'note': case 'osc': break;` then `}` at :589 — no default, no exhaustiveness assertion.
  [read]  :: packages/core/src/voice/render-plan.ts:49-78 `export function nodeCategory(kind: NodeKind): RenderPlanNodeCategory { switch (kind) { … } }` — every NodeKind covered, no default; under strictNullChecks a missing arm is TS2366.
  [type-check] cat tsconfig.base.json :: tsconfig.base.json sets strict:true (hence strictNullChecks) and noFallthroughCasesInSwitch:true, but no noImplicitReturns — confirming the asymmetry: value-returning switches are enforced, void ones are not.

proposed fix: Add `default: { const _never: never = node.kind; void _never; break; }` to the eval-graph.ts:389 switch. The compiler then flags the traversal site whenever nodeCategory must change, restoring lockstep at zero runtime cost. Deliberately-inert kinds keep their explicit no-op arms at :582-588, which documents the intent.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"repeated-switches-0009",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"repeated-discriminant",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The SAME discriminant switched in >=3 places, so adding a variant needs lockstep edits with nothing enforcing it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: repeated-switches-0006
lens: repeated-switches
criterion: repeated-discriminant
severity: major (author confidence: high)
locations:
  apps/server/src/handlers/client-message.ts:21-28 (ENGINE_INPUTS)
  apps/server/src/main.ts:373-418 (monitorInput)
  apps/server/src/handlers/voice-input.ts:115-123 (voice-only tag list)
  apps/server/src/handlers/client-message.ts:507 (markInput tag test)

claim: `ClientMessage['t']` is partitioned into input vs authoring by four independent hand-written lists. ENGINE_INPUTS (client-message.ts:21) holds six tags; monitorInput (main.ts:375-417) switches over eight, adding `fireGraph`; voice-input.ts:117-122 lists six again with different membership; client-message.ts:507 tests just `midi|osc` for latency stamping. None is derived from another, so a new input message must be added in four places and a miss is silent — the message ends up unmonitored, unstamped, or accidentally editor-gated. The fireGraph divergence is the live symptom: main.ts monitors it as an input while ENGINE_INPUTS omits it, so requiresEditor('fireGraph') is true.

evidence the author gave:
  [read]  :: apps/server/src/handlers/client-message.ts:21-28 — `const ENGINE_INPUTS: ReadonlySet<ClientMessage['t']> = new Set(['midi','osc','cc','programChange','key','recallSection']);` (no fireGraph).
  [read]  :: apps/server/src/main.ts:411-414 — `case 'fireGraph': monitor({ type: 'graph', direction: 'in', … })` alongside the midi/cc/programChange/osc/key arms; the switch has no default.
  [read]  :: apps/server/src/handlers/client-message.ts:55-57 — `requiresEditor(t) { return !ENGINE_INPUTS.has(t) && !UNGATED_NON_INPUTS.has(t); }`; the comment at :49-54 documents the deny-by-default intent, so the omission is security-relevant.
  [grep] grep -n "msg.t ===" apps/server/src/handlers/voice-input.ts :: apps/server/src/handlers/voice-input.ts:117-122 lists setShow|key|fireGraph|recallSection|cc|programChange — a fourth, differently-shaped membership test.

proposed fix: Declare ONE `Record<ClientMessage['t'], 'engine-input'|'authoring'|'read'|'voice-only'>` (apps/server/src/handlers/message-taxonomy.ts) so TypeScript demands a role per tag, and derive ENGINE_INPUTS, the voice-only test and the monitor label table from it. Encode fireGraph's CURRENT role ('authoring') so behaviour is preserved; raise the gating question separately rather than folding it into the mechanical unification.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"repeated-switches-0006",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"repeated-discriminant",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
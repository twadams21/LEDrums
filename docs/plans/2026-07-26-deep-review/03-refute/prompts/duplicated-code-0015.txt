You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   >=2 sites, >=12 lines each, >=90% token similarity, SEMANTICALLY equivalent. Consolidation must REDUCE total interface, not relocate it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: duplicated-code-0015
lens: duplicated-code
criterion: duplication-threshold
severity: minor (author confidence: high)
locations:
  apps/web/src/lib/app/views/PasteFallbackDialog.svelte:47-145 (style block)
  apps/web/src/lib/app/views/PasteSongDialog.svelte:87-198 (style block)

claim: PasteFallbackDialog and PasteSongDialog share ~85 lines of identical style rules: the `.head`/`h2` title bar, `.body` column, `.hint` caption, the `.paste-area` textarea with its focus-visible ring, `.actions` row, and the full `.ghost`/`.primary` button pair including hover/disabled/active-scale and the prefers-reduced-motion override. Their scripts and markup have genuinely diverged (PasteSongDialog adds a destination SegmentedControl and a clipboard-read attempt), so only the chrome is duplicated — but the chrome is the part that must stay consistent, and there is no shared text Button primitive in lib/ui to hang it on.

evidence the author gave:
  [read]  :: Read PasteFallbackDialog.svelte:47-145 in full and diffed the whitespace-stripped files: the script/markup diverge (destination control, showText flow) while the style block matches from `.head` through the reduced-motion block.
  [tool-output]  :: jscpd-report.json flags three overlapping CSS ranges for this pair: 95-144 vs 148-197 = 50 lines / 295 tokens; 74-95 vs 126-147 = 22/163; 48-60 vs 87-99 = 13/87.
  [grep] grep -rl "scale: 0.96" --include=*.svelte apps/web/src :: 14 components re-implement the press-scale button affordance, confirming there is no shared Button primitive; lib/ui has IconButton but no text Button.

proposed fix: Extract the shared dialog chrome into a `DialogShell`/`DialogActions` pair plus a text `Button` primitive in apps/web/src/lib/ui (the press-scale + reduced-motion contract belongs in one place), register them in the styleguide, regenerate docs/design-system.html, and reduce both dialogs to their distinct markup.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"duplicated-code-0015",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"duplication-threshold",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
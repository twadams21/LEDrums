You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: middle-man-0002
lens: middle-man
criterion: deletion-test
severity: major (author confidence: high)
locations:
  apps/server/src/ws-protocol.ts:6-32 (export type { ... } from '@ledrums/protocol')
  apps/web/src/lib/ws/protocol-types.ts:1-26 (export type { ... } from '@ledrums/protocol')
  packages/protocol/src/index.ts:1-8 (module header acknowledging the duplication)

claim: The wire contract has one home (@ledrums/protocol) and three published surfaces. apps/server/src/ws-protocol.ts re-exports 22 protocol types and apps/web/src/lib/ws/protocol-types.ts re-exports 22 (overlapping but NOT identical — server has MonitorEventType and TunnelStatus, web has VoiceStat; neither is a superset). Both modules state the only reason in a comment: 'so the server's existing ./ws-protocol import paths keep working' / 'for the web's existing import paths'. protocol/src/index.ts:6-8 acknowledges it from the other side. That is the deletion test failing by the module's own admission: the barrels add no behaviour, and adding one wire type now means editing up to three files, with drift already visible in the three-name delta. 27 server files and 42 web files import through the barrels.

evidence the author gave:
  [read]  :: apps/server/src/ws-protocol.ts:6-9 comment: 'Re-export the types here so the server's existing ./ws-protocol import paths keep working; this module owns the server-side runtime helpers (decode/encode/serialize) below.' Lines 10-32 list 22 type names.
  [read]  :: apps/web/src/lib/ws/protocol-types.ts:1-4 comment: 'This module re-exports those types for the web's existing import paths and adds the web-side runtime helper (decodeServer)'. Lines 5-26 list 22 type names.
  [grep]  :: Diff of the two lists: server-only = MonitorEventType, TunnelStatus; web-only = VoiceStat. Confirms the duplicated surface is already drifting rather than being kept in lockstep.
  [grep]  :: `grep -rn "from '.*ws-protocol'" apps/server/src` -> 27 importing files. `grep -rn "protocol-types'" apps/web/src | wc -l` -> 42 importing lines.
  [read]  :: packages/protocol/src/index.ts:6-8: 'The server's ws-protocol.ts and the web's lib/ws/protocol-types.ts both re-export these types and add their own runtime (de)serialization helpers on top.'

proposed fix: Delete both `export type { ... }` blocks. Keep each module as a pure runtime-helper module: ws-protocol.ts keeps decodeClient/encodeServer/serializeModel/effectSpecs/frameToRgbBytes, protocol-types.ts keeps decodeServer (and should be renamed decode-server.ts once it no longer publishes types). Repoint the ~69 importing files to `@ledrums/protocol` for types — a mechanical codemod, since the names are identical. The protocol package becomes the single import path it already claims to be.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"middle-man-0002",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
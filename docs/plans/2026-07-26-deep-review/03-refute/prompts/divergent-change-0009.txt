You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   One logical change requires edits in >=4 files, evidenced from >=3 commits in git log.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: divergent-change-0009
lens: divergent-change
criterion: change-scatter
severity: minor (author confidence: high)
locations:
  packages/protocol/src/index.ts:45-300 (payload interfaces)
  packages/protocol/src/schemas.ts:440-472 (_Lock* assertions)

claim: Adding or changing one field on any wire payload requires three coordinated edits inside a single package: the hand-written `interface` in index.ts (21 exported interfaces/types between lines 45 and 300), the corresponding zod schema in schemas.ts, and — for any newly added payload — a `_Lock<Name> = Assert<Equals<z.infer<typeof …Schema>, …>>` line in schemas.ts:453-472. The lock assertions exist precisely because the two declarations can drift; they are a compile-time patch over a structural duplication that `z.infer` would eliminate outright.

evidence the author gave:
  [read]  :: packages/protocol/src/index.ts:14-15 states outright: "The payload interfaces below (SerializedModel, EffectSpec, OutputStatus, …) are locked to their schemas by a type-level assertion in ./schemas" — the duplication is acknowledged in the source.
  [grep] grep -n "interface |^export type |z.infer" packages/protocol/src/index.ts; grep -n "_Lock" packages/protocol/src/schemas.ts :: grep on index.ts finds 21 exported interfaces/types (ShowLibraryBlob:45, SongLibraryBlob:54, BackupSnapshotMeta:66, SerializedDrum:74, SerializedModel:82, EffectSpec:96, OutputStatus:105, VoiceStat:120, VoiceStats:137, ControllerUniverseRx:155, DiscoveredController:176, NetworkAdapter:195, Controlle
  [git-history] for c in $(git log --format='%h' -200 -- packages/protocol/src/schemas.ts); do git show --name-only --format='' $c; done :: Confirmed as a live scatter: fbd63a2 ('wire SnapshotStore') edits both packages/protocol/src/index.ts and packages/protocol/src/schemas.ts plus schemas.test.ts in one commit, alongside 5 server files.

proposed fix: Invert the direction: keep the zod schemas as the sole declaration and replace each hand-written interface with `export type SerializedModel = z.infer<typeof serializedModelSchema>;` (doc comments move onto the type alias / schema). The 15 `_Lock*` assertions then become unnecessary and delete. Exported names and their structural shapes are unchanged, so no consumer edits.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"divergent-change-0009",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"change-scatter",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The SAME discriminant switched in >=3 places, so adding a variant needs lockstep edits with nothing enforcing it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: repeated-switches-0007
lens: repeated-switches
criterion: repeated-discriminant
severity: minor (author confidence: high)
locations:
  apps/web/src/lib/app/views/trigger-node-meta.ts:118-158 (kindSummary)
  apps/web/src/lib/app/views/trigger-node-meta.ts:32-108 (kindIcon / tint / kindLabel)

claim: One file maps NodeKind four times. kindIcon (:32), tint (:63) and kindLabel (:87) are `Record<NodeKind, …>` and therefore compiler-enforced; kindSummary (:118) is a switch ending in `default: return ''` at :156-157. A new kind is forced into the three Records but silently drops out of the fourth, shipping a node card with an icon, a tint and a title but a blank sub line. The fourth map is the only one that can go stale and the only one written as a defaulted switch.

evidence the author gave:
  [read]  :: apps/web/src/lib/app/views/trigger-node-meta.ts:32 `export const kindIcon: Record<NodeKind, Component> = {…}` — 20 keys, enforced.
  [read]  :: apps/web/src/lib/app/views/trigger-node-meta.ts:118-158 `export function kindSummary(node: GraphNode): string { switch (node.kind) { … default: return ''; } }` — 14 arms plus a silent default.
  [type-check] cat tsconfig.base.json :: tsconfig.base.json sets `strict: true` and `noFallthroughCasesInSwitch: true` but NOT `noImplicitReturns`; a defaulted switch therefore carries no exhaustiveness obligation while a Record<NodeKind,…> does.

proposed fix: Convert kindSummary to `const KIND_SUMMARY: Record<NodeKind, (n: GraphNode) => string>` alongside the other three maps and look it up, so all four fail the build together when a kind is added. Kinds with genuinely no summary get an explicit `() => ''` entry, documenting the intent instead of hiding it in a default arm.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"repeated-switches-0007",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"repeated-discriminant",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: speculative-generality-0007
lens: speculative-generality
criterion: deletion-test
severity: major (author confidence: high)
locations:
  apps/web/src/lib/app/shell-nav.ts:36-74 (initialNav / setView / select / clearSelection / isSelected)
  apps/web/src/lib/app/shell-store.svelte.ts:13-57 (ShellStore)

claim: shell-nav.ts states its own rationale: "a PURE reducer (no runes, no DOM) so the invariants are unit-testable in the node test env. shell-store.svelte.ts is a thin rune wrapper over this." The invariant being protected is two lines — `setView` returns `{...nav, view, selection: null}` when the view changes (shell-nav.ts:46-49). Around that, the split produces an exported `ShellNav` type, five exported transition functions, and a ShellStore whose every member is a one-line forward (`get view` → `this.s.view`, `setView` → `nav.setView(this.s, view)`, `select`, `clearSelection`, `isSelected` — shell-store.svelte.ts:28-56). The reducer has exactly one consumer, and the only state ShellStore genuinely owns is `liveRouting`, which the Phase-1 map already flags as not belonging to navigation. Deletion test: fold shell-nav into ShellStore and nothing reappears at other callers — there are none; the invariant still has one home (the setter), and the tests move from testing `setView(nav, v)` to testing `new ShellStore().setView(v)`, which is the behaviour users actually get.

evidence the author gave:
  [read]  :: shell-nav.ts read in full (79 lines): VIEWS, initialNav, setView, select, clearSelection, isSelected, parseSearch, plus View/PatchNodeId/Selection/ShellNav types. setView's entire invariant is the `selection: null` at line 48.
  [read]  :: shell-store.svelte.ts read in full (57 lines): every public member except patchRouting/setPatchRouting is a one-line delegation to the nav module; the class holds one $state<ShellNav> and one $state<PatchRouting|null>.
  [grep] rg -n "shell-nav" apps/web/src :: Imported by shell-store.svelte.ts and its own test; components consume the store, not the reducer — confirming a single production consumer for the extracted module.

proposed fix: Inline the transitions into ShellStore (setView keeps the clear-selection line; select/clearSelection/isSelected become three short methods) and keep only the genuinely shared, genuinely pure pieces in a small module: the `View`/`Selection`/`PatchNodeId` types, `VIEWS`, and `parseSearch` (which parses a URL and has real edge cases). Retarget shell-nav.test.ts at ShellStore. Separately, move `liveRouting`/`patchRouting` off the navigation store — a Patch-view side channel is not navigation state.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"speculative-generality-0007",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
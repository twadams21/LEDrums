You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: speculative-generality-0004
lens: speculative-generality
criterion: deletion-test
severity: major (author confidence: high)
locations:
  apps/web/src/lib/ui/Slider.svelte:44-70 (inferUnit / unit $derived)
  apps/web/src/lib/app/docks/inspectors/ContainerNodeInspector.svelte:75-113 (format={pct})
  apps/web/src/lib/app/docks/inspectors/ModulationParamsSection.svelte:110 (format={pct})

claim: `inferUnit(formatted, numeric)` (Slider.svelte:65-70) reverse-engineers a unit suffix out of the caller's already-formatted string by string-subtraction: strip the numeric prefix, else the numeric suffix, else — the final fallback at line 69 — `return formatted` wholesale. The generality is unearned AND actively wrong, because the app's dominant formatter rescales the value. With `format={pct}` where `pct = v => `${Math.round(v * 100)}%`` (node-options.ts:116, and re-defined in EnvelopeEditor.svelte:51, ModulationParamsSection.svelte:32, EnvelopeEditorView.svelte:178), a value of 0.6 gives `inputValue = "0.6"` and `display = "60%"`. `"60%"` neither starts nor ends with `"0.6"`, so inferUnit hits line 69 and returns `"60%"` as the *unit*; the markup then renders the text input (`0.6`) next to a unit span reading `60%` (lines 130-161). Six live call sites use `format={pct}`: Gate threshold, band cutoffs, chance, modulation depth, sweep amount. The deletion test is decisive — delete inferUnit and complexity does not reappear anywhere, because the caller already knows its unit and could simply pass it.

evidence the author gave:
  [read]  :: Slider.svelte:44 `const unit = $derived(inferUnit(display, inputValue));`, :65-70 the four-branch heuristic ending in `return formatted;`, and :130-161 where the raw `inputValue` goes into the <input> while `unit` is rendered as a sibling <span class="unit">.
  [grep] rg -n "const pct|export const pct" apps/web/src/lib -g '*.ts' -g '*.svelte' :: pct is defined as `v => `${Math.round(v * 100)}%`` in node-options.ts:116, EnvelopeEditor.svelte:51, ModulationParamsSection.svelte:32, EnvelopeEditorView.svelte:178 — a rescaling formatter, so format(v) never contains the numeric text inferUnit is trying to subtract.
  [grep] rg -n "<Slider" apps/web/src -A4 | rg "format=" :: Live `format={pct}` sliders: ContainerNodeInspector.svelte:75 (Gate threshold), :92 (Band cutoff), :113 (Chance, inline pct), ModulationParamsSection.svelte:110 (Modulation depth), EnvelopeEditor.svelte:67 (Sweep amount).

proposed fix: Replace the inference with declaration: add a `unit?: string` prop, render `{unit}` directly, and delete `inferUnit` entirely. Callers that currently pass `format={pct}` become `unit="%"` plus either a scaled `value`/`min`/`max` (0-100) or a `display` transform pair — the important part is that the component stops guessing what the caller meant. Leave `format` for the pure-display case (`showValue` readouts with no editable input) or drop it in favour of `unit` + `precision`.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"speculative-generality-0004",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
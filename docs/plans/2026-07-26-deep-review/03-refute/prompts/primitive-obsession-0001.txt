You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The primitive stands for a domain concept with >=2 invariants enforced at call sites.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: primitive-obsession-0001
lens: primitive-obsession
criterion: domain-concept-with-invariants
severity: critical (author confidence: high)
locations:
  packages/core/src/voice/types.ts:246-358 (GraphNode)
  packages/core/src/voice/types.ts:200-240 (CanonicalGraphNodeKind)

claim: `NodeKind` is a 19-member union (types.ts:200-219) but `GraphNode` (types.ts:246-358) is a single flat interface with 40 top-level fields, 27 of them optional, grouped only by `// play` / `// modifier` / `// lfo source` / `// cc source` comments. Nothing ties a field to the kind it belongs to. Worse, 13 fields are REQUIRED on every node regardless of kind — `mode: PlayMode`, `scope: Scope`, `effectId: string`, `presetId: string`, `busId: string`, `params`, `env`, `noRepeat`, `on: SwitchOn`, `valueMode`, `threshold`, `invert`, `bands`, `p`, `delayMode`, `ms`, `division` — so a `trigger` root node must carry an `effectId: ''`, a `p: 0` chance and a `division`. Every consumer therefore re-derives, by hand and unchecked, which fields a kind actually uses: `graph-integrity.ts:65` hand-fills the defaults, `trigger-flow-projection.ts:28` hand-lists per-kind signature fields, each `*NodeInspector.svelte` hand-picks its subset, `persistence.ts:190` filters migrations on `node.scope !== 'hoop'`. A discriminated union would make every one of those exhaustive.

evidence the author gave:
  [read] sed -n '246,358p' packages/core/src/voice/types.ts | grep -E '^  [a-zA-Z]+\??:' :: packages/core/src/voice/types.ts:246-358 read in full; `sed -n '246,358p' | grep -E '^  [a-zA-Z]+\??:' | wc -l` returned 40 top-level fields, of which `grep -oE '^\s+[a-zA-Z]+\?:'` counted 27 optional.
  [read]  :: packages/core/src/voice/types.ts:199-240 — LegacyGraphNodeKind ('play') plus CanonicalGraphNodeKind with 19 members: trigger, effect, all, random, sequence, switch, chance, toggle, delay, modifier, mix, scope, output, envelope, lfo, cc, note, osc, randomMod.
  [grep]  :: Downstream hand-maintained per-kind dispatch confirmed at apps/web/src/lib/app/views/trigger-flow-projection.ts:30-52 (switch over 9 of the 19 kinds, `default: return base`) and packages/core/src/voice/graph-integrity.ts:65 (literal default fill including `division: '1/8'`).

proposed fix: Split GraphNode into a discriminated union keyed on `kind`: a `GraphNodeBase { id; kind; x; y }` plus per-kind payload interfaces (PlayNode, DelayNode, LfoNode, CcNode, ScopeNode, MixNode, SwitchNode, …). Every per-kind switch in the codebase then becomes exhaustive under `strict`, and adding a 20th kind produces compile errors at each site that must handle it instead of a silent default branch. Do this as a mechanical type-level split first (payload fields keep their names), leaving persistence shapes untouched, then tighten hydrate to narrow on read.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"primitive-obsession-0001",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"domain-concept-with-invariants",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
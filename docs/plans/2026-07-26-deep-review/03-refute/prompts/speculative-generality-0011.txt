You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: speculative-generality-0011
lens: speculative-generality
criterion: deletion-test
severity: minor (author confidence: high)
locations:
  packages/core/src/geometry/pixel-grid.ts:128-136 (nearestPixelWithin)
  packages/core/src/voice/graph-integrity.ts:218-223 (assertTriggerGraphIntegrity)
  packages/core/src/canvas/registry.ts:33-35 (listCanvasScenes)

claim: Phase 1's hunch that pixel-grid has no live caller is WRONG — buildPixelGrid, forEachPixelWithin and nearestPixelIdWithin are each used by real effects (rain-3d, spark-arc, lightning, confetti-burst). But the FOURTH function, `nearestPixelWithin` (pixel-grid.ts:128), is explicitly documented as an "Object-returning convenience over nearestPixelIdWithin" and every one of its call sites is in pixel-grid.test.ts. It exists because the allocation-free primitive felt unfriendly, for a caller that never materialised — and it re-derives the distance the primitive deliberately discarded. Likewise `assertTriggerGraphIntegrity` (graph-integrity.ts:218) is a throwing wrapper over `validateTriggerGraphIntegrity` with no caller anywhere, including tests: a fail-fast affordance nobody opted into, in a codebase whose actual policy is to surface issues as data (`issues[]`). `listCanvasScenes` (canvas/registry.ts:33) completes the registry's CRUD quartet and is likewise uncalled — the web reads scenes from its own store, not from core's registry.

evidence the author gave:
  [grep] rg -n "forEachPixelWithin|nearestPixelIdWithin|nearestPixelWithin|buildPixelGrid" -g '!node_modules' -g '!dist' -g '!*.json' | grep -v 'geometry/pixel-grid.ts:' :: buildPixelGrid/forEachPixelWithin/nearestPixelIdWithin appear in confetti-burst.ts, rain-3d.ts, spark-arc.ts, lightning.ts (production). nearestPixelWithin appears ONLY at pixel-grid.test.ts:4/84/129 — this both rejects the Phase-1 hunch and isolates the one genuinely unused function.
  [tool-output] unused-export scan (export declarations vs a token index of every other non-defining file) :: Reported `packages/core/src/voice/graph-integrity.ts:218 assertTriggerGraphIntegrity testfiles=0 infile=1` and `packages/core/src/canvas/registry.ts:33 listCanvasScenes testfiles=0 infile=1` — declared and exported, referenced nowhere, not even by their own tests.
  [read]  :: pixel-grid.ts:120-136 — the doc on nearestPixelIdWithin states the allocation-free contract ("no closure, no result object — safe on a per-particle-per-frame hot path; callers needing the distance recompute it"), and nearestPixelWithin immediately undoes it for a hypothetical caller.

proposed fix: Delete all three. For nearestPixelWithin, rewrite the two test assertions against nearestPixelIdWithin (the test is already proving brute-force equivalence, which the id alone establishes). For assertTriggerGraphIntegrity, if a throwing form is ever wanted the caller can write `if (issues.length) throw` in one line at the site that wants it. For listCanvasScenes, delete unless a scene-browser UI is imminent — and if it is, add it with its caller.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"speculative-generality-0011",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
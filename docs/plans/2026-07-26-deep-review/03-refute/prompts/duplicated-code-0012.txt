You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   >=2 sites, >=12 lines each, >=90% token similarity, SEMANTICALLY equivalent. Consolidation must REDUCE total interface, not relocate it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: duplicated-code-0012
lens: duplicated-code
criterion: duplication-threshold
severity: minor (author confidence: high)
locations:
  apps/server/src/network-adapters.ts:32-46 (ipToInt/intToIp)
  packages/io/src/pixlite/sweep.ts:28-42 (ipToInt/intToIp)

claim: `ipToInt` (4-part split, per-octet integer/range validation, throw on malformed, `>>> 0` accumulate) and `intToIp` are declared privately and byte-identically in both files. apps/server already depends on packages/io, so there is a dependency edge to carry a shared helper along; today two copies of the same address-validation rule exist, one reached from the network-adapter enumeration path and the other from the PixLite CIDR sweep.

evidence the author gave:
  [read]  :: Read apps/server/src/network-adapters.ts:32-46 and packages/io/src/pixlite/sweep.ts:28-42 — identical including the error message `invalid IPv4 address: ${addr}` and the `(n * 256 + o) >>> 0` accumulation.
  [grep] grep -rn "function ipToInt|function intToIp|function expandCidr|function prefixFromMask" --include=*.ts apps packages | grep -v test :: Two ipToInt and two intToIp definitions; expandCidr exists only in io, prefixFromMask only in server — i.e. the shared primitives are the duplicated pair, the callers are genuinely different.
  [tool-output]  :: jscpd-report.json: apps/server/src/network-adapters.ts:27-46 vs packages/io/src/pixlite/sweep.ts:26-42 = 20 lines / 152 tokens.

proposed fix: Export ipToInt/intToIp from packages/io (e.g. a small `net/ipv4.ts` re-exported from the io index) and import them in apps/server/src/network-adapters.ts, deleting the server copies. Direction matters: io is the lower package, so it owns them.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"duplicated-code-0012",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"duplication-threshold",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
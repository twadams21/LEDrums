You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   >=3 parameters travelling together across >=3 call sites.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: data-clumps-0003
lens: data-clumps
criterion: co-travelling-params
severity: major (author confidence: high)
locations:
  packages/core/src/voice/eval-graph.ts:262-275 (evalGraphGen3From)
  packages/core/src/voice/eval-graph.ts:277-286 (evalGraphGen3FromPlan)
  packages/core/src/voice/eval-graph.ts:615-625 (evalChildren)
  packages/core/src/voice/eval-graph.ts:266-270 (evalGraphGen3)

claim: Three functions carry the identical 8-slot positional tuple (state, graph|plan, pad, startIds|childIds, ctx, viaPrefix, seen, draft) and do nothing but forward it to each other: evalGraphGen3 -> evalGraphGen3FromPlan (line 269), evalChildren -> evalGraphGen3From -> evalGraphGen3FromPlan (lines 625, 274). Six of the eight members (state, graph/plan, pad, ctx, viaPrefix, seen) are one traversal cursor; only startIds and draft vary per hop. Eight same-shaped positional args across three hops is the classic silent-transposition hazard — `seen` and `viaPrefix` are both invariant-carrying and adjacent, and `viaPrefix: string` sits next to `pad: string`, so a swapped pair type-checks cleanly.

evidence the author gave:
  [read]  :: packages/core/src/voice/eval-graph.ts:262-275 and :277-286 — the two signatures differ only in `graph: TriggerGraph` vs `plan: RenderPlan`; every other slot is identical in name, order and type.
  [tool-output] python3 scratchpad/big.py (regex scan for functions with >=5 params across all 8 units) :: eval-graph.ts is the top hit in the repo with three separate 8-param functions; only compositeInto (blend.ts:29, an rgba splat) and applyModifierChain tie it.
  [read]  :: packages/core/src/voice/eval-graph.ts:615-626 — the exported `evalChildren` re-lists all 8 and its whole body is a single forwarding call.

proposed fix: Introduce `interface EvalCursor { state: EvalState; pad: string; ctx: TriggerCtx; viaPrefix: string; seen: Set<string> }` local to eval-graph.ts. Signatures become `evalGraphGen3FromPlan(cursor: EvalCursor, plan: RenderPlan, startIds: readonly string[], draft: PlayDraft | null)` and likewise for the graph variant. Keep `evalChildren`'s exported signature intact as the public seam (it is called from the engine's pending-fire drain) and have it construct the cursor — that confines the churn to one file.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"data-clumps-0003",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"co-travelling-params",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
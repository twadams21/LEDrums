You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   >=2 sites, >=12 lines each, >=90% token similarity, SEMANTICALLY equivalent. Consolidation must REDUCE total interface, not relocate it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: duplicated-code-0009
lens: duplicated-code
criterion: duplication-threshold
severity: major (author confidence: high)
locations:
  apps/web/src/lib/trigger-lab/sim.ts:582-600 (Sim.drainPendingFires)
  packages/core/src/voice/engine.ts:671-690 (VoiceEngine.drainPendingFires)

claim: The delay-node scheduler drain is written twice: the early-out, the `filter(f => f.fireAtMs <= timeMs)` due-set, the complementary re-filter of the queue, the `(fireAtMs, enqueueOrder)` stable sort and the `evalChildren(..., viaPrefix, seen, draft ?? null)` re-entry are identical in both. The web copy's comment says "mirrors core engine.ts drainPendingFires". Delay ordering is a determinism-critical path — the AGENTS.md contract is that the render loop is deterministic given (time, inputs, model) — and here the ordering rule exists in two places that must be kept in lockstep by hand.

evidence the author gave:
  [read]  :: Read apps/web/src/lib/trigger-lab/sim.ts:582-600 and packages/core/src/voice/engine.ts:671-690 — five consecutive statements are identical, including `due.sort((a, b) => a.fireAtMs - b.fireAtMs || a.enqueueOrder - b.enqueueOrder);`. Only the action-application tail differs (the sim inlines play/stop
  [tool-output]  :: jscpd-report.json: sim.ts:578-594 vs engine.ts:669-686 = 17 lines / 116 tokens.

proposed fix: Export a pure `takeDuePendingFires(queue, timeMs): { due, remaining }` from packages/core/src/voice/engine.ts (or a sibling scheduler module) that owns the partition + stable sort, and have both drain loops call it before applying their own actions.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"duplicated-code-0009",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"duplication-threshold",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   THE DELETION TEST: imagine the module gone. If complexity VANISHES it was a pass-through (finding stands). If complexity REAPPEARS across N callers it was earning its keep (finding falls). Also: one adapter = hypothetical seam, TWO adapters = real seam.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: speculative-generality-0010
lens: speculative-generality
criterion: deletion-test
severity: minor (author confidence: high)
locations:
  packages/io/src/pixlite/index.ts:34-40 (export { FakePixliteClient, makeFakeProber, defaultIdentity, defaultStats, type FakePixliteClientOptions })
  packages/io/src/pixlite/fake.ts:1-126 (FakePixliteClient / defaultIdentity / defaultStats / makeFakeProber)

claim: packages/io/src/index.ts does `export * from './pixlite'`, and pixlite/index.ts:34-40 re-exports the in-memory fake, its options type, and two canned-fixture builders. The module's own header calls it "the second implementation of PixliteClient that makes the interface a real seam" — but a fake used only by tests does not make a seam real; the production tree has exactly one implementation, HttpPixliteClient, constructed once at apps/server/src/main.ts:603. The five fake symbols are consumed only by apps/server/src/controller-monitor.test.ts:3. Same shape as the modulation-parity finding, in a different package: a test double occupying the production API surface, shipped in the built package. Note the seam is doubled — HttpPixliteClient ALSO takes an injectable `HttpTransport` (client.ts:36, defaulting to nodeHttpTransport at :86/:144), so the code carries two layers of substitutability for one real network implementation.

evidence the author gave:
  [read]  :: packages/io/src/pixlite/index.ts read in full — the barrel exports FakePixliteClient, makeFakeProber, defaultIdentity, defaultStats and FakePixliteClientOptions alongside the production surface; packages/io/src/index.ts:5 does `export * from './pixlite'`, hoisting them to the package root.
  [grep] rg -n "FakePixliteClient|makeFakeProber|HttpTransport|nodeHttpTransport|expandTargets|expandCidr|RankedCandidate" -g '!node_modules' -g '!dist' -g '!*.md' -g '!*.json' | grep -v 'packages/io/src/pixlite/' :: The only consumer outside the defining directory is apps/server/src/controller-monitor.test.ts:3/39/44/56. No production file imports the fake.
  [read]  :: packages/io/src/pixlite/client.ts:20-38 declares a second injectable seam (`HttpTransport`), defaulted to `nodeHttpTransport` at :86 and :144 — a transport-level substitution point layered under the client-level one, with one real implementation each.

proposed fix: Move the fake behind a separate entry point (`@ledrums/io/testing`, or a `pixlite/testing.ts` the server test imports by deep path) and drop the five symbols from the package barrel. Then pick ONE substitution layer and delete the other: if tests inject FakePixliteClient, HttpPixliteClient does not also need an injectable HttpTransport (its own tests can use the transport seam and the server tests the client seam — but the code should not carry both merely because each was convenient once).

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"speculative-generality-0010",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"deletion-test",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
You are an ADVERSARIAL REFUTER. Your job is to KILL this finding, not to agree with it.

A code-quality review produced the finding below. Findings that survive refutation get acted on
— some automatically. False positives are expensive. You are the gate.

## Rules
1. **Try to refute it.** Look for the caller it missed, the dynamic access it did not grep for,
   the semantic difference behind the surface similarity, the guard that already handles the case.
2. **DEFAULT TO REFUTED UNDER UNCERTAINTY.** If you cannot satisfy yourself that the finding is
   true, it is refuted. "Probably right" is refuted.
3. **Apply EXACTLY this criterion — do not substitute your own:**
   The SAME discriminant switched in >=3 places, so adding a variant needs lockstep edits with nothing enforcing it.
4. Read the actual code at the cited locations. Do not reason from the claim alone.
5. READ-ONLY. No edits, no tests, no builds.

## The finding
id: repeated-switches-0001
lens: repeated-switches
criterion: repeated-discriminant
severity: major (author confidence: high)
locations:
  apps/web/src/lib/app/views/trigger-flow-projection.ts:31-52 (triggerNodeSignature)

claim: Every other NodeKind discriminant site in the web app spells the effect node as the PAIR `kind === 'play' || kind === 'effect'` (22 non-test occurrences under apps/web/src/lib, e.g. store.svelte.ts:2568, 2790, 2842, TriggerNode.svelte:60, Inspector.svelte:132). trigger-flow-projection.ts:34 writes only `case 'play'`. Graphs reaching the store are normalised by voice.normalizeTriggerGraphToGen3 (hydrate.ts:400/418/455), which rewrites every `play` node to `effect` (graph-integrity.ts:148/154), so the `play` arm is dead and every real effect node falls to `default: return base` — its playType/effectId/canvasScene never enter the signature the projection cache compares.

evidence the author gave:
  [read]  :: apps/web/src/lib/app/views/trigger-flow-projection.ts:34 is `case 'play': return `${base}:playType=…:effect=${n.effectId}:canvas=…`;` with no `case 'effect'`, and line 50 is `default: return base`.
  [grep]  :: packages/core/src/voice/graph-integrity.ts:148 `addNode(node.kind === 'play' ? { ...node, id, kind: 'effect' } : { ...node, id });` and :154 the same — normalisation converts play to effect.
  [grep] grep -n normalizeTriggerGraphToGen3 apps/web/src/lib/trigger-lab/store/hydrate.ts :: apps/web/src/lib/trigger-lab/store/hydrate.ts:400,418,455 all call `voice.normalizeTriggerGraphToGen3(graph)`, so store graphs contain no `play` nodes.
  [grep] grep -n "makeNode('effect'" apps/web/src/lib/trigger-lab/store.svelte.ts :: apps/web/src/lib/trigger-lab/store.svelte.ts:2574/3241/3256 mint nodes with makeNode('effect', …); nothing mints kind 'play'.
  [grep] grep -rn "=== 'play' || " apps/web/src/lib | grep -v test | wc -l :: 22 non-test occurrences of the `=== 'play' || ` pair idiom under apps/web/src/lib; trigger-flow-projection.ts is the single site using only 'play'.

proposed fix: Change line 34 to `case 'play': case 'effect':` so the arm actually runs. Better: replace the switch with a `Record<NodeKind, (n: GraphNode) => string>` so TypeScript demands an entry per kind and the drift cannot recur.

## Output — return ONLY this JSON object, no prose, no fence
{"finding_id":"repeated-switches-0001",
 "refuted":true or false,
 "reasoning":"<at least 2 sentences: what you checked, what you found, why it stands or falls>",
 "criterion_applied":"repeated-discriminant",
 "counter_evidence":"<the specific caller/guard/difference you found, with file:line — or \"none found\" if you could not refute it>",
 "confidence":"high|medium|low"}
# A1 — Audio modulation, integrated vertical slice

You are the Fable/low implementer for GH #214 audio. Read SPEC.md here. Work only your assigned worktree branch feat/audio-modulation. Another worker owns Spatial Field generator/registry and a later worker owns MIDI clock. Parent is Pi, so write AUDIO-RESULT.md here, commit, go idle (native SendMessage may not reach parent). Do not spawn agents, run full sweeps, publish/push/merge, or change .mex/CAD files. At most targeted tests; no dev server/browser (parent performs integrated UI verification). User wants an integrated experiment, not a throwaway fake.

## Main seam

Extend existing graph modulation with `audio` source (four bands: level/bass/mids/highs), supplied by compact `audioFeatures` WS events. Do NOT encode audio as CC/OSC (OSC can fire triggers). Reuse target-side mappings, graph persistence, and same source sampling for effect and modifier params, connected server and offline simulator. No renderer rewrite, no GPU.

## Explore these existing paths before changing

- packages/core/src/voice/{types,modulation,modulation-graph,modulation-parity,engine,compositor,render-plan}.ts and graph schemas: OSC/Note are templates for continuous source propagation.
- packages/protocol/src/{schemas,index}.ts, schemas.test.ts (schema is authority)
- apps/server/src/{voice-engine-host,handlers/client-message}.ts and input routing
- apps/web/src/lib/trigger-lab/{store.svelte,sim,render}.ts and store/mod-graph, hydration + undo
- apps/web/src/lib/app/views/{trigger-node-meta,add-node-taxonomy}.ts, graph-to-flow, trigger-node previews/inspectors; copy OSC source pattern, retain blue modulation wire semantics.
- apps/web/src/lib/app/settings/panes/InputPane.svelte and settings metadata
- packages/core/src/engine/render-context.ts (audio shouldn't leak browser APIs here)

## Detailed contract

1. Define four feature keys and finite [0,1] frame schema in appropriate core pure module/schema. Protocol imports/reuses it. New strict message `t:'audioFeatures'` with `level,bass,mids,highs` (flat, or `features` object if consistent; choose once and propagate); no client timestamp. Core input event carries same frame. Host stamps current engine time. Active graph/modifier mapping `kind:'audio', band` reads latest features when age <=500ms, otherwise zero. Engine-owned state, no Date/performance/DOM. Unknown/unheard band zero. Stop sends all zeros; expiry handles tab crash/connection loss. Audio event must never invoke trigger resolution. Apply freshness to offline simulator and all compositor paths, including nested Mix/splice context if required. Prefer one shared sampling function rather than duplicate expiry logic.
2. Browser capture adapter outside core (`apps/web/src/lib/audio/`): injected dependencies so lifecycle is testable. getUserMedia with selected audio device, echoCancellation/noiseSuppression/autoGainControl false. One AudioContext/MediaStream source/analyser; FFT 2048 or 4096 (choose and document), no audio connected audibly to speakers. Reuse typed arrays. Sampling max 30Hz via managed timer, NOT tied to settings component mount. Do not stream raw audio. Explicit user start only. Feature-detect secure context/API. AudioContext.resume from user activation; handle suspended/interrupted context honestly.
3. Pure numeric analysis helper: RMS time signal for level, power-aware FFT magnitudes for fixed bands (bass 20–250Hz, mids 250–2000Hz, highs 2000–12000Hz clipped at Nyquist), convert dB appropriately rather than average dB. Gain 0..4 or dB equivalent, noise floor, attack/release smoothing derived from dt. Stable finite normalized output including -Infinity FFT bins. No automatic loudness normalization that pumps silence. Choose documented useful reference scale. Test synthetic silence/single-band tone and sample rate changes.
4. Lifecycle controller in web store (not only InputPane): start/stop/device change, busy/running/error status and latest meter frame, local device/gain/floor/smoothing settings (safe localStorage persistence, device IDs NOT in show). Pending permission promise cancellation must stop late-returned tracks; restarting closes old context/timer/tracks/listeners. Device-lost/track ended or suspended capture must zero features and surface status. Release on app/store dispose and on loss of host/editor ownership. Remote viewer cannot start or send authoritative audio. Explicitly zero on stop and don't indefinitely queue stale feature events during WS reconnect. Offline preview receives local feature event directly; connected host gets one WS event and server drives visible kit. Do not emit monitor noise each frame.
5. UI: Settings > Input adds Audio Input section with device selector, Enable/Stop, compact truthful status, Level/Bass/Mids/Highs meters, Gain/Noise floor/Smoothing controls, short routing help: 'Route Ableton to an audio-interface loopback or virtual input, then select it here. Audio is analysed locally; it is not recorded.' Browser permission labels may be unavailable before enable; allow default input and refresh names after permission, never request on load. Handle denied/no-device/unsupported states with actionable copy. Existing dense flat DS, use Field/Select/Button/Slider/StatusPill. New reusable meter component gets styleguide entry. Read /make-interfaces-feel-better at /Users/trent/.claude/skills/make-interfaces-feel-better/SKILL.md and repo impeccable skill/PRODUCT.md before UI changes. New node labelled Audio, band select (Level/Bass/Mids/Highs), normal source preview/inspector and add menu. Existing OSC source UI is precedent; no bespoke canvas editor.
6. Desktop: inspect Tauri Info.plist/microphone permission usage string and add required description if absent. No new native capture backend, entitlement hacks or loopback driver installs. State WKWebView support limits in report if cannot verify. Don't claim microphone/Ableton hardware tested.

## Tests/finish

Synthetic DSP behavior; injected capture start/stop/cancellation/failure/ended cleanup; protocol invalid/out-of-range/nonfinite rejection; core event→audio mapping→render (existing effect and modifier), stale-to-zero without a new event; offline parity; node create/band edit persistence roundtrip + appropriate UI tests. Keep tests public-behavior focused. Commit code + AUDIO-RESULT.md reporting SHA, tests and caveats. Parent handles full typecheck/test, design-system regeneration and ui-shot ONCE in integration tree; do not run heavy browser/build jobs yourself. For screenshot seam add support for selecting an Audio node or synthetic meter state if straightforward, but do not fake capture in production. Never modify MIDI clock/WebMIDI/native_midi.rs: later serial timing slice owns these.

# Effects Library v2 - U7 Close-out Report

Date: 2026-07-05
Issue: GitHub #60
Branch: `feat/u7-effects-library-closeout`
Base context: open PR #61 (`docs/u5-canvas-ui-plan`) and PR #63 (`feat/u6-day1-library-fill`)

## Summary

U7 closes Effects Library v2 by verifying the shipped library end-to-end, refreshing the
stale ui-shot gallery captures left by U5/U6, and confirming the canvas authoring path works
in the live app on `:5173`.

Implementation changes:
- `scripts/ui-shot/ui-shot.mjs` supports optional clicks so "Take over" can be skipped when
  the app is already in editor mode.
- `scripts/ui-shot/shots.json` now opens a local graph before typed Add/gallery actions and
  captures current collection, Canvas, Lenses, and filtered states.
- `scripts/ui-shot/README.md` documents the optional action flag.
- `packages/io/src/pixlite/client.test.ts` gives real node:http PixLite transport probes a
  realistic timeout under full workspace parallel test load. Runtime code is unchanged.

## ui-shot Sweep

`pnpm ui-shot --all --strict` passed and generated 25 screenshots:

`shell`, `top-bar`, `transport`, `left-rail`, `trigger-graph`, `node-editor-add`,
`trigger-node-selected`, `graphs-dock`, `visualizer`, `buses`, `patch-graph`, `objects`,
`song-library`, `sections`, `section-detail`, `perform`, `monitor`, `settings-dialog`,
`node-editor-play`, `patch-controller`, `effect-gallery`, `effect-gallery-collection`,
`effect-gallery-canvas`, `effect-gallery-lenses`, `effect-gallery-filtered`.

Manual image review covered the Effects gallery collection, filtered Textures state, Canvas
collection, and Lenses tab. The captures show populated cards and rendered thumbnails.

## Live Spot-check

Live app target: `http://localhost:5173`.

Checklist:
- Created a new authored Canvas Scene in Objects -> Canvas Scenes.
- Renamed it `U7 Spot Check 2026-07-05`.
- Edited the JSON description and tweaked the first element hue/speed values.
- Selected a Canvas play node in the Snare graph.
- Picked the authored scene from the Canvas scene selector.
- Reloaded the app and confirmed the scene binding persisted.
- Verified the selected node thumbnail canvas was lit after reload:
  `width=56`, `height=32`, `lit=1792`, `sum=116984`.

## Gates

Passed:
- `pnpm --filter @ledrums/core exec vitest run src/canvas/canvas-determinism.test.ts --reporter=verbose`
  - Canvas perf log: `2.469 ms/frame on 548px` against a 5ms budget.
- `pnpm --filter '!@ledrums/desktop' -r test`
  - Core: 48 files / 633 tests.
  - IO: 9 files / 51 tests.
  - Protocol: 1 file / 1 test.
  - Server: 22 files / 227 tests.
  - Web: 120 files / 1217 tests.
  - Canvas perf log in the full run: `1.742 ms/frame on 548px`.
- `pnpm typecheck`
  - `svelte-check` reported 0 errors and 0 warnings.

Known benign test stderr remained present in the web suite:
- jsdom does not implement canvas `getContext` in thumbnail/projection tests.
- Existing Svelte `derived_inert` warnings in a few component tests.

## GROW Notes

Ground: Effects Library v2 is now complete through U7. The close-out proves the library is
visible in gallery states, Canvas scenes can be authored and selected live, and the new canvas
engine remains deterministic and under budget.

Record: `.mex/ROUTER.md` and the plan tracker were updated to COMPLETE.

Orient: `.mex/patterns/ui-shot-sweep.md` records the repeatable ui-shot close-out workflow.

# Effects Library v2 — U6 report

Date: 2026-07-05
Branch: `feat/u6-day1-library-fill`
Base: `docs/u5-canvas-ui-plan` (U5 implementation branch)
Issue: #59

## Summary

U6 fills the day-1 Effects Library v2 surface with authored Canvas content and four gap-fill Gen-3 native effects.

The implementation keeps the U4/U5 architecture intact:

- Canvas scenes are authored `CanvasScene` data.
- Built-in scene docs are registered through the existing core canvas registry and render through `canvas:<sceneId>` / `tryGetCanvasEffect`.
- Web gallery exposure projects the same scene docs into normal `EffectDef` cards and default presets.
- New native effects use the existing emission contract (`createEmitterState` + `updateEmissions`) and do not fork the compositor or voice bridge.

## Canvas scene inventory

Core file: `packages/core/src/canvas/presets.ts`

The seed library registers 23 built-in scenes:

| id | name | sampler | lens family |
|---|---|---:|---|
| `stripe-band` | Stripe Band | `cylinder` | none |
| `checker-spin` | Checker Spin | `cylinder` | none |
| `strip-rain` | Strip Rain | `strip` | none |
| `hoop-orbits` | Hoop Orbits | `hoop` | none |
| `tunnel-rings` | Tunnel Rings | `footprint` | `polar` |
| `polar-spokes` | Polar Spokes | `footprint` | `polar` |
| `unpolar-ribbons` | Unpolar Ribbons | `footprint` | `unpolar` |
| `unpolar-rose` | Unpolar Rose | `footprint` | `unpolar` |
| `log-tunnel` | Log Tunnel | `footprint` | `log-polar` |
| `log-nebula` | Log Nebula | `footprint` | `log-polar` |
| `kaleido-bloom` | Kaleido Bloom | `footprint` | `kaleido` |
| `kaleido-shards` | Kaleido Shards | `footprint` | `kaleido` |
| `mobius-ribbon` | Mobius Ribbon | `footprint` | `mobius` |
| `mobius-glass` | Mobius Glass | `footprint` | `mobius` |
| `tile-matrix` | Tile Matrix | `cylinder` | `tile` |
| `tile-confetti` | Tile Confetti | `footprint` | `tile` |
| `swirl-vortex` | Swirl Vortex | `footprint` | `swirl` |
| `swirl-petal` | Swirl Petal | `footprint` | `swirl` |
| `hyper-drift` | Hyper Drift | `footprint` | `hyper4d` |
| `aurora-veil` | Aurora Veil | `cylinder` | none |
| `spiral-galaxy` | Spiral Galaxy | `footprint` | `log-polar` → `swirl` (chained) |
| `prism-fan` | Prism Fan | `hoop` | `kaleido` |
| `hyper-checker` | Hyper Checker | `footprint` | `hyper4d` |

Coverage:

- At least 10 seed Canvas scenes: **23 shipped** (incl. `spiral-galaxy`, the first scene composing two chained lenses).
- 2 presets/scenes for each U6 lens family: **polar, unpolar, log-polar, kaleido, mobius, tile, swirl, hyper4d**.
- Each scene carries `description` and `tags`, including the `canvas` tag; lens scenes carry `lens`.

## Web gallery / preset inventory

Web file: `apps/web/src/lib/trigger-lab/fixtures.ts`

The web fixture registry now adds `CANVAS_EFFECTS`, projected from `BUILTIN_CANVAS_SCENES`:

- `EffectDef.id = canvas:<sceneId>`
- `EffectDef.generatorId = canvas:<sceneId>`
- `playType = 'canvas'`
- `busId = 'base'`
- `scope = 'kit'`
- params come from `CANVAS_PARAM_SPEC`
- every built-in canvas effect gets a default preset via the existing `PRESETS` seed path

Compatibility note: U5's `setCanvasScene()` picker currently binds canvas play nodes only to scene docs present in `store.canvasScenes`. To make the built-in gallery cards selectable in a blank show without editing U5's large store seam, `seedAuthored()` now seeds `canvasScenes: structuredClone(BUILTIN_CANVAS_SCENES)`. This is intentionally documented for U7 verification: the cards are selectable today, but a later hardening pass could make built-ins read-only/non-deletable in Objects instead of treating them as seeded editable templates.

## New Gen-3 native effects

Registry file: `packages/core/src/effects/registry.ts`

Four new gap-fill native effects were added and registered:

| id | file | collection | contract |
|---|---|---|---|
| `orbit-comet` | `packages/core/src/effects/impl/orbit-comet.ts` | Particles | emission-based, per-drum, hoop-aware, seeded |
| `scan-plane` | `packages/core/src/effects/impl/scan-plane.ts` | Waves | emission-based, 3D kit-wide airspace sweep |
| `drum-sonar` | `packages/core/src/effects/impl/drum-sonar.ts` | Waves | emission-based, per-drum hoop sonar rings |
| `gravity-drops` | `packages/core/src/effects/impl/gravity-drops.ts` | Particles | emission-based, per-drum falling bead/trail |

Each effect:

- uses `createEmitterState` and `updateEmissions`;
- spawns once per trigger seq;
- layers concurrent hits as independent emissions;
- has deterministic payload derivation from trigger data;
- emits finite clamped channels through the existing framebuffer path;
- carries D1 metadata directly on the generator (`description`, controlled-vocabulary tags).

## Tests added

File: `packages/core/src/effects/u6-gap-fill.test.ts`

Coverage:

- built-in canvas scene count, descriptions, tags, and registry lookup through `canvas:<sceneId>`;
- at least two scene presets for every U6 lens family;
- all four gap-fill natives render finite `[0,1]` frames and light pixels from hits;
- deterministic replay for identical seeded runs;
- one live emission per new hit for all four effects.

## U7 verification checklist

U7 should verify:

1. Gallery Canvas collection is populated with the 20 built-in canvas cards.
2. Selecting a Canvas card creates/retargets a canvas play node and the scene picker shows the seeded scene doc.
3. `pnpm typecheck` passes.
4. `pnpm --filter '!@ledrums/desktop' -r test` passes, including `u6-gap-fill.test.ts`.
5. `pnpm ui-shot` captures the populated Canvas collection and at least one selected U6 canvas card.
6. Confirm Objects → Canvas Scenes behaviour is acceptable for seeded templates, or follow up with a read-only built-in-template hardening slice.

## Verification status

I could not run local gates from this ChatGPT connector environment because there is no checked-out repository with installable dependencies available. The PR should run the normal CI/gates. The implementation is intentionally covered by the added unit tests and kept inside existing U4/U5 seams.

## Review fixes (local review pass, 2026-07-05)

CI was red on the original push; a local review pass fixed:

- **typecheck**: `seedAuthored()` assigned `structuredClone(BUILTIN_CANVAS_SCENES)` (readonly array type) to the mutable `canvasScenes` — now clones per-scene into a fresh mutable array.
- **`registry.test.ts` catalog count** not bumped for the 4 new natives (45 → 49; the 4 ids added to the presence list).
- **`gravity-drops` dead-zone bug**: with `trail` (default 0.22) smaller than a drum's hoop gap (1/3 on a 4-hoop drum) the falling bead could sit between hoops and light nothing (visible flicker on sparse-hoop drums; also the failing test). The trail reach now widens to at least 1.1× the drum's hoop gap.
- Restored `fixtures.ts` rationale comments (S18/S19 param-mapping notes, generator scope rationale, seed-tree notes) that the U6 diff had stripped.
- Added 3 more scenes: `aurora-veil`, `spiral-galaxy` (chained lenses), `prism-fan`.

## Spec-alignment pass (round 2, same review session)

The earlier "seed built-ins into `canvasScenes`" compat shim deviated from D4 and duplicated
every canvas card (static `CANVAS_EFFECTS` in fixtures `EFFECTS` + the U5 store's derived
`canvasEffects` both emitted `canvas:<id>`). Reworked to the locked spec:

- **Built-ins live in core only** (D4): `seedAuthored().canvasScenes` is `[]` again; fixtures'
  `CANVAS_EFFECTS` removed. The store now derives gallery cards from
  `allCanvasScenes = BUILTIN_CANVAS_SCENES (unshadowed) + authored`, one source of truth.
  `setCanvasScene` / `addPlayNode('canvas')` / preset defaults / the Inspector picker all
  resolve through it.
- **Objects view**: authored scenes are listed first; a "Built-in library" group renders the
  core scenes read-only (View JSON + Duplicate only — duplicating creates an editable
  authored copy). `CanvasSceneRow` gains a `builtin` mode; `canvasSceneRows(scenes, builtin)`.
- **Lenses collection** (D6): the gallery rail now shows a "Lenses" tab — a filtered view of
  the canvas pool (`lens`-tagged scenes). It's NOT a new PlayType (a lens preset node is
  still a canvas node), so the D3 PlayType==Collection identity is untouched; a canvas-locked
  node keeps both Canvas and Lenses tabs browsable.
- **Engine guard**: `VoiceBusEngine.syncCanvasScenes` re-registers a core built-in after
  unregistering a show scene that shadowed its id (a U6-era doc could otherwise nuke a
  built-in from the global registry on show switch).
- **Live crash fixed (pre-existing U5 bug)**: `buildShow` called `structuredClone` on the live
  store's `$state`-proxied scenes → uncaught `DataCloneError` whenever a show carried canvas
  scenes and synced to the server. Now a JSON round-trip clone (scenes are plain JSON data).
- **Hydrate migration**: `unionEffects` strips persisted `canvas:*` effects (a U6-era doc
  briefly saved the virtual defs; re-adding them duplicated every derived card → Svelte
  `each_key_duplicate` crash in the gallery).
- **ui-shot**: `effect-gallery-canvas` + `effect-gallery-lenses` added to `shots.json` (drive
  the typed Add ▸ Canvas palette, then open the locked gallery) — both captured clean, plus an
  Objects-view capture of the Built-in library group. NOTE (pre-existing, for U7): the older
  `effect-gallery-collection` / `effect-gallery-filtered` shots have been broken since U5's
  gallery lock (the rail collapses to one tab, so their tab-click actions time out).

Gates after the pass: typecheck 0 errors; core 633 / web 1217 / server 227 / io 51 / protocol 1.

---
name: router
description: Session bootstrap and navigation hub. Read at the start of every session before any task. Contains project state, routing table, and behavioural contract.
edges:
  - target: context/architecture.md
    condition: when working on system design, integrations, or understanding how components connect
  - target: context/stack.md
    condition: when working with specific technologies, libraries, or making tech decisions
  - target: context/conventions.md
    condition: when writing new code, reviewing code, or unsure about project patterns
  - target: context/decisions.md
    condition: when making architectural choices or understanding why something is built a certain way
  - target: context/setup.md
    condition: when setting up the dev environment or running the project for the first time
  - target: patterns/INDEX.md
    condition: when starting a task — check the pattern index for a matching pattern file
  - target: ../PRODUCT.md
    condition: when designing, restyling, or building UI — brand, register, users, and design principles (visual system in ../DESIGN.md once generated)
last_updated: 2026-07-02
---

# Session Bootstrap

If you haven't already read `AGENTS.md`, read it now — it contains the project identity, non-negotiables, and commands.

Then read this file fully before doing anything else in this session.

## Design Context

UI / visual work is governed by Impeccable design context, not the `context/` files:

- **`../PRODUCT.md`** — register (`product`), the two operators (rig-builder + performer), brand personality (*engineered, expressive, fast*), anti-references, and 5 design principles.
- **`../DESIGN.md`** — the visual system (tokens, type, components). Generated via `/impeccable document`; may be absent until then.

Read these before any redesign, restyle, or new-UI task, and drive the work with the `/impeccable` skill.

## Current Project State

**Desktop OTA/monitor/voice preview fixes (done — 2026-06-30, branch `main`):** desktop now uses a single `app` webview window (the bundled loading shell navigates to the local server when ready; the old transient splash capability is removed). Manual OTA checks are split correctly: the server exposes informational `/api/update-status` for any browser, while the desktop host gets Tauri command permissions to install/restart. `pnpm ota bump` patch-bumps desktop/Tauri versions; `pnpm ota` loads `.env.local` and publishes OTA using `BASE`/`OTA_PUBLIC_BASE`. The left rail includes Monitor as a first-class view. Connected preview is server-authoritative again, avoiding local-sim/voice-server flicker in voice mode.

**In progress: full UI redesign** (Impeccable `/craft`). Brief + locked decisions in [PRODUCT.md](../PRODUCT.md) and `docs/plans/2026-06-21-ui-redesign.md`.

**Active exploration (2026-06-21): trigger-model pivot.** Arrange is being rethought from the flat `(drum,slot)→clip` binding into a **trigger behavior tree** (Play one-shot/loop · All/Random/Sequence/Switch · Chance/Toggle) over **voice buses** (layers with a mono/poly polyphony rule + crossfade), plus section **morph/blend** (cut/morph/live two-deck). A throwaway probe lives at `apps/web/src/lib/trigger-lab/` — run `?proto=trigger` (no engine needed; see its `NOTES.md`). Three branches are under decision before the slice-3 core model refactor: voice model · block set · section blend.

**Working:**
- Engine, IO, core, WS protocol, existing panels — unchanged and functional.
- **New design foundation (done):** OKLCH token system at `apps/web/src/styles/tokens.css` (graphite canvas, AA-verified ink via `apps/web/scripts/contrast-check.mjs`, signal-flow role colours, state colours, motion/space/type scales). Geist + Geist Mono self-hosted. Base controls rebuilt in `apps/web/src/app.css`. Back-compat token aliases keep existing panels rendering on the new palette during migration. Living styleguide at `/?style`.
- **Locked decisions:** accent = phosphor lime; shells = mode-split (Perform HUD / Authoring Workbench); views = Perform · Arrange · Settings(Patch/Output/Kit); taxonomy = Content → Effects → Clip(+Preset), Blend on Layer.

**Unified shell (done — 2026-06-25, branch `feat/unified-shell`):** the cohesive app reconciling the prototype with the original control app, in `apps/web/src/lib/app/` on the `TriggerLab` store — now the default `App.svelte`. Mode-split Perform/Author crossfade, view router (Trigger/Patch/Sections/Kit), right-dock Inspector/Monitor, Layers/Buses dock, engine link + overlays reused. Pure `shell-nav` reducer (unit-tested) + thin rune store. Gates green. See `docs/plans/2026-06-21-ui-redesign.md` → "Unified application shell".

**Section-aware engine playback (done — 2026-06-25, branch `feat/unified-shell`):** `recallSection` InputEvent (deterministic, queued like other inputs) sets the engine's active song/section. On a pad hit the engine resolves the active section's per-drum slot graphs and fires them layered (in slot order), each with its own graph-key state prefix so PRNG/sequence state doesn't collide. Falls back to flat `graphs[padKey(drumId,zone)]` when no section/slots (back-compat). Web store mirrors locally + sends `recallSection` to server on section change or WS open. 8 new core tests (240 total). `Show.songs?` added to core types; `buildShow` passes songs through. Remaining: add/remove songs + setlist persistence, unify looks-recall vs slot-arrangement section notions.

**Parallel agent batch (done — 2026-06-26, branch `feat/unified-shell`):** orchestrated implementer agents, integrated + full-gate-verified (typecheck 0; 303 tests: core 156 / server 36 / web 99 / io 12).
- Duplicate-slot-key guard (`c311e81`): per-slot state prefix so two layers of one graph run independently.
- Live persistence + create-new-graph + resizable docks (`099b9db`/`82bdb8a`/`fcb2485`): localStorage autosave of authored state (no save button), `store.createGraph`, draggable `lib/ui/Splitter` panes (sizes persisted).
- All 41 original effects bridged (`1ec20f1`): voices host legacy `EffectGenerator`s via the registry (server + offline). Follow-ups: live-stream effects render single-hit; color/enum params not voice-editable; gallery should group by category.
- Per-zone trigger firing (`3b2da91`): section slots keyed by **padKey** (was per-drum → every zone fired zone-0's graph) + effects-union on hydrate so built-ins always surface.
- Arc-segment LEDs (`f9f67f6`+`f7fb63f`): continuous curved thick rings (was gapped box-tubes); cold geometry / hot colors; camera-framing fix preserved.
- Live Show re-sync (`0b03ed7`): store re-sends `setShow` (debounced, signature-guarded) on authored edits — effect/param/preset/wiring changes now reach the engine (previously frozen at connect).
- Real Patch Graph topology (`b1765a2`): `@xyflow` 8-stage device routing (input→trigger→zone→drum→hoop→dataline→output→controller), data-driven. Follow-up: wire data-line/output to server `dmxMap`; editable device settings.

**Unified node graphs on @xyflow/svelte (done — 2026-06-26, branch `feat/unified-shell`):** both graphs now run on one engine + one shared node design (8 commits, full suite green: core 156 / server 36 / web 107 / io 12). Implementer agent via twux (`unify-graphs-9a0267`), orchestrated + git-verified here.
- Shared `NodeCard` extracted from `PatchNode` (the source look); `PatchNode` + new `TriggerNode` both render it. Icon chip + title + mono sub + right-side `EffectThumb` slot (play nodes).
- Trigger Graph ported off the hand-rolled `NodeCanvas` onto `SvelteFlow` (`TriggerGraphView` + `graph-to-flow.ts` converter, 8 tests; store stays source of truth + autosaves). `NodeCanvas.svelte` kept ONLY for the lab `/?proto=trigger` (`TriggerLab.svelte`), retired from the shell.
- Inspector is now the full per-node editor for every kind (kind selector + Remove + per-kind controls); play-mode/layer are the **iconed** SegmentedControls (Zap/Repeat/Hand · Disc3/Activity/Wand2), not text. In-node controls removed → click a node = edit in Inspector.
- Patch Graph editable: wire delete/rewire + add palette (Data Line / Output), **ephemeral** local state — device-settings model (zone OSC/MIDI · 8 data lines · 4 outputs · controller IP + sACN/Art-Net) is still the deferred slice.
- **Graph UX (locked, see memory `graph-interaction-prefs`):** NO node lift/click motion; hover highlights node border + connected wires **instantly**; dropping a wire anywhere on a node body → its input (DOM hit-test, since xyflow only sets `toNode` near a handle). New validated `store.reconnect()` keeps the wire on a rejected rewire. xyflow 1.6 has no `edgesReconnectable` — reconnect is a custom `WireEdge` (bezier + `EdgeReconnectAnchor`s).
- Runtime behaviour (hover feel, drop-on-node wiring, iconed Inspector, patch rewire) verified by gates/typecheck/autofixer only — **needs a live `:5173` spot-check** (agent did not drive a browser).

**Switch node — value routing (done — 2026-06-26, branch `feat/unified-shell`):** the trigger-graph **switch** gains an `on:'value'` mode (additive; `velocity|section|beat` unchanged) with two sub-modes. **Gate** = one threshold + invert (pass when value ≤ threshold, or > when inverted; default = does nothing above). **Bands** = N contiguous value bands, *each its own source handle* on the node (`band-${i}`), so a different child wires per band; the value lands in exactly one band → that band's children fire. Value source = `ctx.velocity` (0–1; MIDI/OSC sources are a later trigger-source slice). Model: `GraphNode` += `valueMode/threshold/invert/bands`, `GraphEdge` += `fromPort` (the source handle); pure `bandIndex` resolver + `childrenViaPort`; defensive defaults so pre-value persisted graphs still eval. Nodes display-only (per-band handles + cutoff readouts); all editing in the Inspector (gate slider+invert · band cutoff sliders kept ascending + add/remove). Store carries `fromPort` through connect/reconnect (dup is per-port; removeBand remaps+dedups ports). 29 new web tests (gate/band eval, port wiring, band bookkeeping); web typecheck 0, 138 web tests green. Implementer agent `switch-value-4f4a8f` via twux (4 phase commits). **Server-side note (RESOLVED 2026-06-26 — see core-eval entry below):** core `voice` eval now models `value`/`fromPort` (gate + bands, defensive defaults), so `value` switches route through the **real engine + real output**, not just the local sim; the show-builder bridge cast was dropped (web↔core graph types re-converged). Runtime feel + per-band wiring still **need a live `:5173` spot-check** (no agent drove a browser).

**Switch value routing — core engine (done — 2026-06-26, branch `feat/unified-shell`):** mirrored the web sim's `on:'value'` switch eval into pure `packages/core` so a connected server routes value switches through the real engine (previously value routing worked only in the local/offline sim — the gap recorded in the switch-value entry above, now closed). `voice` types gained `SwitchOn:'value'` + `ValueMode` + `GraphNode.valueMode/threshold/invert/bands` + `GraphEdge.fromPort?` (structurally identical to web `sim.ts`); the engine got the gate/bands branch in `evalNode` (defensive defaults so legacy graphs can't throw), a `childrenViaPort` helper, and an exported pure `bandIndex` resolver — deterministic, no RNG (value source = `ctx.velocity`). web↔core types re-converged → the documented `show-builder` graphs bridge cast removed (plain `{ ...source.graphs }`). 17 new core tests (bandIndex; gate pass/block/invert/boundary; band picks incl. at-cutoff + above-last; empty band; port-less wires ignored; back-compat defaults; velocity-unchanged; determinism). Implementer agent `switch-value-core-eval` via twux (commit `e92bf88`). Full sweep green: typecheck 0; **426 tests** (core 174 / io 13 / server 40 / web 199).

**Patch Graph authoritative (active — 2026-06-26, branch `feat/unified-shell`):** making the **Patch graph** the authoritative editor of kit geometry + IO routing — rewiring the output half (`hoop→dataline→output→controller`) reorders the real Art-Net pixel stream, and each node edits the real device setting it represents. Locked with Trent: server `Project` is the source of truth (web adopts it from the `state` message, edits via WS); hoops take a **literal pixel count** (`pixelsPerHoop`), not density; universes are controller-handled (the app authors transmit order); MIDI/OSC lives on **zone** nodes. Mission + per-node spec: `docs/prompts/patch-graph-authoritative.md` (was mis-titled "trigger graph"; see memory `patch-graph-authoritative-intent`). Orchestrated `S1∥S2→S3→S4` via twux.
- **S1 done** (`ee90ac5`/`66fa91c`/`4034745`): core `pixelsPerHoop` literal override (`drumSchema` + `pixel-model`); **voice host made runtime-mutable** (`reloadKit`/`setKitTransform`/`setKitOutputs`/`setInputMap`/`setOutput`), edits routed to the active `voiceHost` in `main.ts`; new `{t:'setKitOutputs';outputs}` WS msg + `pixelsPerHoop` on `setKitTransform`; `stateMessage` now sources the voice model so broadcast geometry matches transmitted frames. Full sweep green (core 157 / server 38 / web 158 / io 12). **Limit:** `setKitOutputs` is voice-mode only (legacy `Engine` has no reducer — accepted no-op; dev runs `LEDRUMS_ENGINE=voice`).
- **S2 done** (`5139115`): pure `apps/web/src/lib/app/patch-routing.ts` — `PatchRouting ⇄ OutputConfig[]` (order-preserving; same-drum ascending-contiguous coalescing matches `buildDmxMap`) + `pixelRanges` first/last-pixel read-outs. Round-trip verified, 20 tests.
- **S3 done** (`9f3ac89`): store adopts the `Project` (was discarded); Patch output-half derives from `kit.outputs` via new pure `apps/web/src/lib/app/patch-graph.ts`; rewire/drag→recompile→`store.setRouting`→`setKitOutputs`. Transmit order = node **vertical (y) position**. Store API for editors: `project`, `setDrumTransform`/`setRouting`/`setInputMap`/`setOutput`. 16 web tests.
- **S4 done** (`b52ec84`): Inspector per-node editors — **Zone** (MIDI note/OSC→`setInputMap`), **Drum** (origin/rotation/`startAngleDeg`/`localSpinDeg`/**`pixelsPerHoop`**→`setKitTransform`), **Hoop** (pixels + first/last read-out), **Data line / Output** (universe + first/last read-out; `startUniverse`/`channelsPerPixel`→`setRouting`), **Controller** (Art-Net/sACN protocol/host/broadcast/rgbOrder/fps→`setOutput`), node **rename** (`patchLabels`/`setPatchLabel`). New pure `docks/patch-inspector.ts` + `CommitInput.svelte`. 21 web tests.
- **Integrated + full-sweep green** (2026-06-26): typecheck 0 (all 4 pkgs; web svelte-check 2066 files 0 errors); **402 tests** (core 157 / io 12 / server 38 / web 195).
- **S5 cleanup done** (`333f4b2`/`d3d7d37`/`f5a9e0c`): (a) node face shows the rename override (reactive, via `views/patch-context.ts`); (b) data-line/output read-outs derive from the **live graph** (`ShellStore.patchRouting` ← `routingFromGraph`; Inspector looks up `byDataLine[nodeId]`/`byOutput[parseOutputNodeId]`) so palette-added (`dataline:new-N`) + reordered lines resolve a span and update on drag without remount; (c) sACN **`priority`** (1–200, default 100) added to `OutputSettings`, `port`/`iface` plumbed through `setOutput` end-to-end (io emits priority in E1.31 + binds `iface` on both protocols; both hosts apply; Inspector Controller editor exposes Port · Interface · sACN-Priority). Full sweep green: typecheck 0; **407 tests** (core 157 / io 13 / server 40 / web 197).
- **S6 done** (`cd5bfae`/`e6bff44`/`f73d99d`): **data lines first-class** — `OutputConfig` now `{ id, startUniverse?, channelsPerPixel, dataLines: DataLineConfig[] }` (each line `{ id, startUniverse?, segments }`; legacy `segments` zod-preprocessed → one implicit line). **Dense channel packing** — global cursor over outputs→dataLines→segments→hoops→pixels; pixels may **straddle** universes; output/line `startUniverse` snaps to `U*512`; hard per-output cap **removed** (`maxPixelsPerOutput` advisory). `DmxMap`/`UniversePatch` restructured for straddle (`output-manager` byte-packer follows; `CHANNELS_PER_UNIVERSE=512`). Authoritative **kit pixel defaults** Kick 196 / snare 108 / tom1 108 / tom2 136 in `DEFAULT_KIT`. Patch round-trip preserves 8 data lines 1:1 (no collapse). Byte-exact golden tests. (PixLite A4 expanded: 16 hoops → 8 lines → 4 outputs → 1 controller; see `docs/kit-hoop-pixel-counts.md`.)
- **S7 done** (`14e7b35`/`7edc245`): **live project autosave** (debounced ~400ms, async/off-render-loop, atomic temp+rename to machine-local `default.local`, boot-recovers, shutdown-flush) — closes the "not persisted across restart" follow-up (no Save button). **Single-client lock** (newest-wins; evicts prior with close 4000; engine transmits through client churn). +17 server tests.
- **S8 done** (`63820cb`): left rail shows **Views above Songs**.
- **Cold-load fix done** (`04bc25d`): Patch view now **adopts the server `Project`'s `kit.outputs` on cold load** — a `$effect` re-derives the output-half when `store.project` arrives post-mount (signature-guarded via `routingSignature` so the user's own echo / in-progress drag never clobbers). Fixes "patch resets after a reload / localStorage clear": routing was always server-persisted (S7) + browser-refresh-safe, the view just wasn't reacting to it landing (`untrack` at mount). +4 signature tests.
- **Integrated** (2026-06-26): typecheck 0/0 (web svelte-check 2069 files); **462 tests** (core 180 / io 13 / server 57 / web 212).
- **Drum-node geometry done** (`4d2f3b6` hoop-spacing + `64d2f35` diameter — parallel worktree agents, merged): **Hoop spacing (mm)** + **Diameter (in)** editors on the Patch Drum node, both plumbed live through `setKitTransform` (host `reloadKit()` rebuilds geometry). Cold-load fix (`04bc25d`) also in. **Live `:5173` spot-check still owed** (no slice drove a browser): rewire-reroutes-in-voice-mode, dense/straddle on real hardware, drum geometry (spacing/diameter/pixel counts), MIDI/OSC trigger map, controller Art-Net/sACN incl. priority/port/iface, data-line stats on new/remapped lines, renames-on-face.

**Trigger-source + Sections rework (active — 2026-06-26, branch `feat/unified-shell`):** consolidating three specs under one orchestrator (they share `sim.ts`/`store`/`Inspector`/`core/voice` so they run as a **sequential queue** `U1 → V → U2 → U3 → U4 → U5`): the trigger node becomes an editable **Drum/MIDI/OSC source** (`docs/prompts/trigger-node-source.md`); the switch's **`velocity` mode folds into `value`** (`docs/prompts/switch-velocity-to-value-fold.md`); and the **Sections view flattens** (drop drum/zone grouping) with the **Play Surface** (the `TriggerGraphView` drum-grouped sidebar, lines 58–202) replaced by the selected section's **flat graph list** — select a graph → its section activates + opens + highlights — plus **copy/paste sections**.
- **U1 done** (`5b0217b`/`f6f021e`): `TriggerSource` union (`drum{drumId,zone} | midi{note?|cc?} | osc{address}`) on web `sim.ts` + core `voice/types.ts` (structural); `normalizeTriggerValue` 0–1 seam (web only — U3 mirrors to core); idempotent back-compat (pad graphs default `drum` from padKey; authored graphs left unset); `setTriggerSource` mutator. No engine resolution yet (U3).
- **V done** (`77beffa` core / `b261845` web, agent `v-velocity-fold`): `velocity` folded into `value` and removed from `SwitchOn` (web `sim.ts` + core `voice/types.ts`) — `value` is now the one intensity-routing mode. Behaviour-preserving migrator `foldVelocitySwitch` (web sim): each `on:'velocity'` switch with N outgoing edges → `value`+`bands` with N even cutoffs (`[1/N…(N−1)/N]`) + every edge re-homed onto `band-${i}` in **target-y order** (the order the old switch fired children); N≤1 → one band, N=0 → nothing wired; idempotent + immutable. Run over hydrated graphs in the store constructor (beside `unionTriggerSources`) so persisted `velocity` switches fold on load; `treeToGraph` assigns the same band ports for seed value+bands switches (so a seed graph == a migrated one); `makeNode`/`makeBlock` defaults + the tom fixture now `value`. Selectors (Inspector + NodeCanvas) drop `velocity` (NodeCanvas gains the missing `value`); `section`/`beat` stay count-based + untouched. Core has no migrator (receives migrated graphs) but eval treats a stray `velocity` as `value` defensively (no throw/mis-route). Full sweep green: typecheck 0; web 226 / core 181 (+ server/io) tests, incl. new `sim.velocity-fold.test.ts` (structure, parity vs pre-fold split, N=1/N=0, idempotency) + a store hydrate-fold test. **Owed:** live `:5173` spot-check of a migrated graph's per-band routing (no agent drove a browser).
- **U2 done** (`f606f1f`): trigger-node Inspector source editor (Drum/MIDI/OSC `SegmentedControl` + per-mode fields; MIDI/OSC channel/namespace shown as read-only patch-device hints) writing via `setTriggerSource`; node sub-line shows the resolved source (`kick · center` / `MIDI note 38` / `OSC /kick`) via pure `describeTriggerSource` (11 tests). Authored-graph rename via `graphNames`. web 237 tests.
- **U3 done** (`d75abe5` core / `534f0b8` web / `ea055ef` server, merged `8ec6745`): resolve a graph from its trigger source. **Precedence (no double-fire, structural):** the server host runs the inputMap zone-map first — a match attaches `(drumId,zone)` and takes the existing pad path; a **miss forwards the raw note/address** to the core engine, where `resolveDirectGraphs` matches authored graphs' trigger sources (`midi` note / `osc` address) and fires them. `drumId` present XOR absent ⇒ exactly one path. `normalizeTriggerValue` mirrored byte-identical into core; web sim mirrors via `resolveGraphsForFire`. Pure `zoneForNote/zoneForOsc` resolvers in `input-router.ts`. **Watch:** CC sources structurally supported but await a CC input message (future); slot↔numeric-zone convention left as-is. Merged + full sweep **519 tests** (core 195 / io 13 / server 63 / web 248).
- **U4 done** (`8778e54`, merged): `SetlistSection` = `{ id, name, graphs: string[] }` (flat ordered graph-key list; padKey 3-layer slots removed) · `activeSectionId`+`arrangeSectionId` merged → one `activeSectionId` (`setActiveSection`/`selectGraphInSection`) · Sections drum/zone grid dropped → flat-list UI · `TriggerGraphView` Play-Surface sidebar replaced by the active section's flat graph list (select → activate + open + highlight) · resolution: `resolveHitGraphsLocal` fires the active section's graphs whose trigger **drum source** matches the pad (new `sourceMatchesPad`; integrates U3, no dup; section gates resolution, falls back to pad graph only with no active section) · `persistence` migrates slots→graphs + `arrangeSectionId`→`activeSectionId` · `show-builder` bridges flat-graphs→engine padKey slot grid. Merged + full sweep **541 tests** (web 270).
- **U5 done** (`bc7cbd6`/`24e1588`/`c0cae3f`, merged): copy/paste/duplicate sections — `cloneSection` (deep-copies the graph-key list, fresh id, reuse-by-reference), store `sectionClipboard` (transient) + `copySection`/`pasteSection`/`duplicateSection`, copy+paste `IconButton`s on section-header hover (paste disabled until clipboard non-empty).
- **fix-integrity done** (`2d0594c`, merged): `assertShowIntegrity` now skips authored graph keys (`isAuthoredGraphKey` = `graph:`/`graph-` prefix) in the kit-drum check — a **connected** `setShow` with a MIDI/OSC-bound authored graph no longer throws (was a pre-existing U2/U3 bug; slot-ref + dangling-padKey checks intact).
- **✅ INITIATIVE COMPLETE** (2026-06-26, branch `feat/unified-shell`, HEAD `4915771`): `U1 → V → U2 → U3 → U4 → U5` + hoop-spacing + diameter + cold-load fix + integrity fix, all merged + full-sweep green (**555 tests**: core 201 / io 13 / server 63 / web 278; typecheck 0/0, 2075 web files). Built via parallel git-worktree agents (orchestrator-merged). **OWED: a live `:5173` spot-check** — trigger-source editing (Drum/MIDI/OSC), velocity→value-migrated graphs, sections flat-list + picker + Play-Surface→section-list swap + select→activate+highlight, copy/paste sections, drum geometry (spacing/diameter/pixel counts), rewire-reroutes + dense/straddle on hardware, MIDI/OSC firing an authored graph end-to-end. No agent drove a browser.
**CRUD + Context-Menu + Kit→Perform (✅ COMPLETE — 2026-06-27, branch `feat/unified-shell`, HEAD `2b2c651`):** the handoff mission, built via **7 parallel git-worktree impl agents** (orchestrator-merged, full-sweep-verified after each merge). Designed `/codebase-design` → `/to-prd` → `/to-issues` (PRD `docs/plans/2026-06-27-crud-context-perform-prd.md`; 7 slice briefs in `docs/prompts/`; live tracker `docs/handoff/2026-06-27-orch-status.md`). Final sweep green: typecheck 0 (2083 web files); **618 tests** (core 201 / io 13 / server 63 / web 341).
- **ContextMenu primitive** (`47f2b9c`): `lib/ui/ContextMenu.svelte` wraps bits-ui `ContextMenu` on oklch tokens (flat `actions` API + trigger snippet; `danger` reuses the `--live` red). NB `lib/ui/CommitInput.svelte` was found **missing** despite docs claiming it existed — created as a real general inline-rename primitive (commit-on-Enter/blur, revert-on-Esc, autofocus+select).
- **Shell de-mode + Kit→Perform** (`f088e67`): `Mode` removed from `shell-nav` (reducer + tests lockstep); `View` `kit`→`perform`; rail = Trigger·Patch·Sections·Perform; `App.svelte` is one unified shell (no crossfade); the Perform view hides the Layers/Buses drawer + right Inspector/Monitor dock and renders the mined `PerformView` (3D/2D visualizers + pad grid + section-recall strip). Deleted `KitView`, `ModeSwitch`, both `PerformShell`s.
- **Section CRUD** (`8897aec`): `setlist.removeSection` + store `renameSection`/`removeSection`; SectionsView inline rename + section-header ContextMenu (Rename·Duplicate·Delete).
- **Song CRUD** (`5b9dcd7`): `setlist.makeSong` + store `createSong`/`renameSong`/`duplicateSong`/`removeSong`; SongRail add button + per-row ContextMenu + inline rename (guards last-song; re-points active on delete).
- **Graph CRUD** (`aff97c8`): store `renameGraph`/`deleteGraph` (guarded to authored keys via new `isAuthoredGraphKey`; delete purges the key from every section across all songs + clears open selection); Inspector rename now routes through the store; SectionsView graph-row ContextMenu (Remove-from-section vs Delete-graph).
- **Show document model** (`2e2793e`): full multi-show docs — `Show {id,name,authored}` + `ShowLibrary` at `ledrums:shows:v1`; defensive `loadShowLibrary` (valid library wins → migrate legacy `ledrums:authored:v1` single blob → one "Default Show" → else fresh "Untitled"; idempotent, non-destructive, never throws); store `shows`/`activeShow` + `newShow`/`openShow`/`saveShow`/`saveShowAs`/`renameShow`/`deleteShow`/`closeShow` (full authored-rune swap, no cross-show bleed; never zero shows). Server `Project` (routing/geometry) untouched — shows are authored-state docs only. (`paneSizes`/`patchLabels` are per-show by design.)
- **Show browser UI** (`2b2c651`): `ShowBrowser.svelte` (Dialog: New/Save/Save-As/Close + show list — click a row to open, per-row ContextMenu Rename/Delete) + editable active-show title in TopBar — all via the show store API.
- **OWED: a live `:5173` spot-check** (no agent drove a browser) — checklist `docs/handoff/2026-06-27-spot-check.md`. The PRIOR initiative's spot-check (above) is still separately owed.

**Transport recall · generic graphs · server shows · Objects (✅ COMPLETE — 2026-06-27, branch `feat/unified-shell`, HEAD `8227e6d`):** second overnight initiative — 7 parallel git-worktree impl agents (orchestrator-merged, full-sweep per merge). Designed `/codebase-design` → `/to-prd` → `/to-issues` (PRD `docs/plans/2026-06-27-recall-objects-persistence-prd.md`; 7 briefs in `docs/prompts/`; tracker `docs/handoff/2026-06-27-orch-status-2.md`). Final sweep green: typecheck 0 (2097 web files); **699 tests** (core 201 / io 13 / server 80 / web 405).
- **Global MIDI/OSC transport recall** (`6c9ba2e`): one server-boundary handler (before zone-map) maps **Program Change → song** (+ its first section), **CC #0 value → section** in the active song, **OSC `/ledrums/song_<n>/section <v>`** → both — reusing the existing `recallSection`/`setActiveSong` engine inputs (core stays pure). Browser WebMIDI now parses + forwards PC/CC (the live shell wired none before); WS gains `{t:'cc'}`/`{t:'programChange'}`. **CC #0 reserved** (trigger-source editor rejects it). Pure `apps/web/src/lib/app/recall.ts` display helper. NB MIDI-driven recall drives the engine but the web UI active song/section doesn't visually follow yet (no server→client "active changed" msg) — follow-up.
- **Generic graphs** (`6b7e9eb`): authored/pad distinction dropped — `renameGraph`/`deleteGraph`/**`duplicateGraph`** work on any key; `graphNames` names every graph (pad labels hydrated, idempotent). Delete is real → a pad with no matching-source graph is silent (no respawn).
- **Effects/Presets CRUD backfill** (`ad9c093`): `renameEffect`/`duplicateEffect` (effects NOT deletable); `renamePreset`/`duplicatePreset` + `deletePreset` only when `presetUsageCount===0`. (Built-in non-default presets resurrect on reload by the existing hydrate design — possible tombstone follow-up.)
- **Objects view + rail reorder** (`d176eca`): new `objects` view; rail = **Perform · Objects · Sections · Trigger · Patch**; master-detail (Songs · Effects · Graphs · Presets) with per-type CRUD via ContextMenu (effects no-delete; preset Delete disabled when in use). Pure `views/objects-view.ts` row/`deletable` helpers.
- **Section Inspector + recall display** (`880e195`): a `section` Selection kind; clicking a section → Inspector rename + read-only recall strings (OSC + CC 0 value + the parent song's Program Change).
- **Server-authoritative shows** (`0c06266`): shows survive a localStorage clear — the server persists the show library as an **opaque versioned blob** (`projects/default.shows.local.json`, S7 autosave pattern), broadcasts it on the `state` cold-load, the web adopts it (signature-guarded, server-wins) with localStorage demoted to a write-through cache; single-client + project autosave untouched; legacy single-blob migration preserved.
- **Save indicator** (`8227e6d`): a `saveStatus` rune (pure `SaveStatusController`, ≥150ms floor) + a `Saving…/Saved` TopBar indicator (spinner→check cross-fade, reduced-motion).
- **Deferred (explicit next):** the **componentisation pass** — inspect all UI, maximize componentisation on bits-ui — gets its own explore (haiku fan-out) → scope → plan → build cycle. And renaming "Graphs" stays a vault exploration only (`~/TWA/Personal/Projects/LEDrums/Naming - rethinking 'Graphs'.md`; top picks Cue / Gesture / Reaction).
- **OWED: a live `:5173` spot-check** (no agent drove a browser) — checklist `docs/handoff/2026-06-27-spot-check-2.md`; especially the MIDI/OSC recall path end-to-end (voice mode) + the server-persisted-show cold-load/storage-clear. The two prior spot-checks remain separately owed.

**Componentisation pass (✅ COMPLETE — 2026-06-27, branch `feat/unified-shell`, HEAD `6c41172`):** the deferred "inspect-all-UI, maximize componentisation on bits-ui" initiative, run as a 5-phase implement→review pipeline of ~21 twux git-worktree slices (orchestrator-merged, `/code-review` per phase — all Clean). Designed via a 13-explorer reachability map (262 files) → PRD (`docs/plans/2026-06-27-componentisation-prd.md`) → file-bounded briefs (`docs/prompts/comp-*.md`; tracker `docs/handoff/2026-06-27-component-pass-slices.md`). Final: typecheck 0 (5 pkgs); **791 tests** (core 201 / io 13 / protocol 1 / server 80 / web 496).
- **P0 Reap (−6,065 lines):** deleted the dead legacy app (`lib/shell` + `lib/store/app-store` + `lib/panels` + old `lib/views` + `lib/routing` + unused `lib/ui` Card/Rail/Sidebar) + retired the `?proto=trigger` lab (`NodeCanvas`/`TriggerLab`); micro dead-code (server `encodeClient`/`showLibraryExists`, `makeBlock`, 4 unused tokens).
- **P1 Primitives:** new shared `lib/ui` — unified `CommitInput` (text+number), `ListItem`/`EditableRow`, `StatusPill`/`StatusDot`, `MasterDetail`, shared `node-options`; tokens `--overlay`/`--control-icon-size`; component-test infra (jsdom + @testing-library/svelte); Styleguide demos the real primitives.
- **P2 Adopt:** chrome + object/section views + graph views adopt the primitives (ObjectsView −69%, SectionsView −77%; shared `GraphCanvas`/`GraphPalette`/`GraphFitView`). Bug fixed: Patch INPUT-half hoop counts now derive from `store.project.kit` not `DEFAULT_KIT` (`topoDrumsFromKit`).
- **P3 Splits (API-preserving, contracts untouched):** `store.svelte.ts` 1888→1516 + 11 pure slices under `trigger-lab/store/`; `sim.ts` 1169→4 modules; `Inspector.svelte` 1359→239 + 11 per-kind editors under `docks/inspectors/`; server `main.ts` 428→277 (handlers/boot); core `voice/engine.ts` 760→422 + `compositor.ts` 388→154.
- **P4 Consistency:** new `packages/protocol` (`@ledrums/protocol`) single-sources the WS wire contract (out of pure core); web graph types core-canonical (type-only from `@ledrums/core`, D2); dropped the 8 backward-compat token aliases; motion tokens `--dur-120`/`--ease-control` + value-based `--dur` scale (120ms now reduced-motion-aware); comment hygiene + shared `dark-pixel` const.
- **Live-spot-check fixes:** TopBar setlist fixed 300px (no reflow); Inspector follows the active context (AuthorShell `$effect` bridge — section follows active section, node selection clears when gone); computer-keyboard plays the active section's graph list (keys 1–9/0 → graphs 1–10, via `store.fireSectionGraph`).
- **OWED: a live `:5173` spot-check** of the rewired shell (all prior initiatives' spot-checks also still owed).
- **Next (handed to a fresh feature-orch, 2026-06-27 → ✅ DONE 2026-06-28):** Trigger/Effects features — delay node, per-effect scope (kit/drum/hoop)+target-by-id, better effect thumbnails. Brief: `docs/handoff/2026-06-27-feature-initiative-handoff.md`. See the next entry.

**Trigger/Effects feature initiative (✅ COMPLETE — 2026-06-28, branch `feat/unified-shell`, HEAD `2be62b5`):** the three features from `docs/handoff/2026-06-27-feature-initiative-handoff.md`, built via parallel **Haiku-explore → Sonnet/Haiku-implement → orchestrator review-in-place** (5 commits, full-sweep green per merge — lighter than the worktree+`/code-review` pipeline since scope was modest). Final: typecheck 0 (2163 web files); **863 tests** (core 229 / io 13 / protocol 1 / server 80 / web 540).
- **Per-effect scope + target** (`584cd28`): `Scope` gains `'hoop'` (kit/drum/hoop); Play nodes gain an optional `targetId` (drum = `"id"`, hoop = `"id#hoopIndex"`; absent = auto). Compositor + web `render.ts` mask to the resolved range — drum/hoop use `targetId ?? sourceDrumId` (kit ignores target), dangling targets render nothing; **generator-backed effects are scoped too** (mask precedes the generator branch). Pure `getHoopPixelRange` helper. Inspector: 3-way scope `SegmentedControl` + target dropdown (drums / drum#hoop; "Auto (firing drum)" default); store `setScope`/`setTargetId` (scope change clears stale targetId). EffectGallery scope-tab clamps hoop→drum (effects carry no intrinsic hoop scope).
- **Live generator effect thumbnails** (`b6a3e47`/`a33dfc9`): `EffectThumb` renders the **real generator** (was a `'flash'` placeholder) for the 41 generator-backed effects via new `effect-thumb-render.ts` (renders into a cached `Framebuffer` → [intensity,hue]); pattern effects keep the `sampleWith` path. Perf: all thumbs share ONE rAF loop (`effect-thumb-ticker` singleton) instead of ~40; each subscribes only while its canvas intersects the viewport (IntersectionObserver) and renders a single static frame under prefers-reduced-motion; `genState` recreated on generatorId change; the inspector-header thumb also renders the real generator.
- **Delay node** (`971045a` core / `2be62b5` web): new `'delay'` BlockKind defers its children by absolute ms or a musical division (1/4·1/8·1/16 + dotted/triplet). Pure `computeDelayMs` (core `voice/delay.ts`, re-exported; **web imports it — single source, no drift**) resolves the division against bpm **AT ENQUEUE**; the engine holds a pending-fire queue drained in `tick()` at an absolute `fireAtMs` (snapshot-stable across later bpm changes — a **relative real-time offset from the hit, NOT grid-quantized**), re-entering the shared `applyActions` path so nested delays + the cycle guard behave identically to an immediate fire (`delayMs<=0` fires inline; queue cleared on `setShow`). Web `Sim` mirrors the queue (drained in its tick, cleared on show change/stop); `DelayNodeInspector` (mode Time/Division + ms / division); palette `NODE_KINDS` + `kindSummary` + `DELAY_MODE_OPTS`/`DIVISION_OPTS`; store `setDelayMode`/`setDelayMs`/`setDivision`. `packages/core` stays pure + deterministic.
- **#2 computer-keyboard graph triggering** was already shipped (`785d003`) — no work.
- **Spot-check fixes (2026-06-28, `3bd27d9`/`a1fc99f`):** Trent's live pass surfaced trigger-graph instability + weird thumbnails. (a) `TriggerGraphView.rebuildNodes` rebuilt EVERY flow-node object on each projection (runs on node-add/click/graph-switch); the new refs failed xyflow's equality check → it dropped edge `handleBounds` (**wires vanished on add**) and re-applied store `x/y` (**nodes snapped**; the snapped pos then autosaved → **first node reset every refresh**). Fix: reuse the existing flow-node object for any node whose structure + selection are unchanged (xyflow keeps `measured` + live position); only changed/new nodes rebuild. Plus a `beforeunload` autosave flush for the debounce race. (b) Generator thumbnails rendered on the FULL kit then painted the first 338 indices (meaningless content) and round-tripped RGB→hue→RGB (wrong colours) and froze in the portaled gallery; fix: render on a 26×13 synthetic `buildThumbPixelModel` (real geometry, index↔grid 1:1), paint true Framebuffer RGB, layout-robust IntersectionObserver. Gates green (typecheck 0; **867 tests**).
- **OWED: a FULL live `:5173` spot-check** (the above fixes are gate-verified but not yet browser-confirmed end-to-end) — trigger-graph add/drag/refresh stability; generator thumbnails (all 51 read true, correct colours, animate, offscreen-pause, reduced-motion); hoop/drum/target scoping incl. cross-drum targeting + dangling targets; the delay node end-to-end (musical-division timing relative to the hit, nested delays, before/after ordering). All prior initiatives' spot-checks remain separately owed.

**Monitor diagnostics slice (done - 2026-07-01, branch `prd/monitor-diagnostics`):** issues #40 and #41 implemented on top of the PR #43 monitor foundation. Server monitor events now use a bounded replay bus (300 retained events, oldest-first replay on new WS clients) so startup diagnostics are still visible when Monitor opens after boot. Startup diagnostics report engine mode, ports, project load source/path, show-library file state, web-root readiness, and remote-access posture without exposing PIN/token values. Persistence paths emit coarse scheduled/saved/failed events for project/show autosaves; accepted `setShowLibrary` pushes emit a persistence event; WS decode/handler errors, native MIDI decode errors, output monitor events, and tunnel ready/error/exit paths feed the same `{t:'monitor'}` stream. Output diagnostics are coalesced inside `OutputManager` with a 1s default summary window so live Art-Net/sACN sends prove output is alive without per-frame/per-universe Monitor spam; output state changes, setup/send/blackout errors, and one blackout event per operation still emit immediately. Added pure `diagnostics.ts`, `monitor.ts`, `output-monitor.ts`, show-library file inspection, project path helper, autosaver hooks, and tests. Also fixed the Windows path expectation in `static-host.test.ts`.

**Server-authoritative graph diagnostics (done - 2026-07-01, issue #42):** voice-mode trigger resolution now emits pure core diagnostics for input-resolved, graph-fired, graph-missed, and section-recalled events without importing Monitor/protocol into `packages/core`. `VoiceEngineHost` maps those diagnostics into Monitor `graph` events with `source:'server/voice'`, `destination:'graph:<key>'`, resolution path, state prefix, action count, and effect id/name labels. Server inbound input Monitor events now include `destination:'voice-engine'` or `legacy-engine`, including WS/native MIDI/OSC paths, so Monitor distinguishes browser outbound attempts, server inbound input, and server-authoritative graph resolution. Added coverage for core direct MIDI/OSC, pad-section, pad-fallback, misses, recall diagnostics; server host graph/miss Monitor events; and voice-mode handler behavior for viewer MIDI plus channel-filtered drops.

**Trigger graph position persistence (done - 2026-07-01, PR #37):** trigger graph authored IDs are reserved from the persisted show library during web store boot, so `nid()` cannot reuse restored node/edge/section IDs after refresh. `TriggerGraphView` now projects xyflow nodes through a pure graph-scoped helper: unchanged nodes are still reused within the same graph to preserve the anti-snap fix, but graph switches with overlapping node IDs (including the shared `trigger` root) rebuild from the selected graph's stored positions. Added regression tests for persisted node/edge/section ID reservation and graph-scoped projection reuse.

**"Rock solid" investigation (done — 2026-07-02, context docs only, no code):** Trent's 14-item reliability/feature batch (IO confidence + PixLite Mk3 integration, desktop shell/updater UX, input-routing double-fires, section base-effects gap, effect color/param overhaul, envelope easings, restart-on-trigger timebase, thumbnails, effect modifiers, linked-instance removal, cross-show Song Library, dock splitter, drum/kit flips, trigger-graph blank-nodes bug) explored via 10 parallel Haiku agents → synthesized into `docs/plans/2026-07-02-rock-solid/` (INDEX + 9 section docs: verified file:line mechanisms, deep-module designs, touch lists, tests, PRD decisions). Key verified root causes: web `onInput` echo re-fires the sim (`store.svelte.ts:905-910`); core engine intentionally defers section looks (`voice/engine.ts:502`) while the sim implements them → LayersDock/visualiser divergence; `mapParamSpec` drops color/enum params (`fixtures.ts:122`) + ~38 effects hardcode saturation=1; envelope attack pinned at `yOf(1)` with one shared curve; ~15 effects free-run on `ctx.timeMs` instead of `trig.ageMs` (the retrigger complaint); `OutputStatus` streams every 500ms but no UI reads it. Cross-cutting principle for the PRD: server is the only resolver/renderer when connected, sim = offline adapter. **Product decisions LOCKED with Trent same day** (INDEX "Product decisions" section): both-fire kept + drum-link icon; PixLite v1 read-only+identify+test-patterns; fully in-app updates; colour picker over envable H/S/B sliders; **modifiers as graph nodes on a distinct `mod` handle** (full set); canonical Song Library + Setlist naming, delete blocked while in use. **Doc 10 added same day (locked): modulation system** — Envelope/LFO/CC as graph nodes; target nodes expose an initially-empty mappable-param list (one node-face row + scoped input handle per exposed param); mapping edits target-side only; per-voice envelopes / continuous LFO+CC; inline `env: EnvMap` migrated away; first wave, after doc 06's `toPort` wiring infra. **Doc 11 added: clipboard portability** — versioned ClipDoc envelope for graph/section/song/patch copy/paste across servers (offline authoring on Trent's dev server → paste to the drummer's server); re-key-by-default with identical-content reuse; new `setProject` bulk msg + diff-confirm for patch paste. Modulation source nodes get node-face signal previews (envelope curve / LFO waveform+phase / CC live bar, shared thumb ticker). **Process rule added to AGENTS.md: every UI-touching change applies `/make-interfaces-feel-better`.** **PUBLISHED (same day): PRD = issue #45** (`docs/plans/2026-07-02-rock-solid-prd.md`, seams confirmed: core engine / server handlers / web store + new PixliteClient·desktop-bridge·clipdoc); **sliced via `/to-issues` into 49 tracer slices (S01–S49, ~15–25 min each) in 12 feature groups = issues #46–#57**, plans in `docs/plans/2026-07-02-rock-solid/slices/` (INDEX has the full dependency table + issue map + model-guidance tags: effects/modifiers/modulation/ui-significant → strongest implementer tier, ~27-30 of 49; plumbing/mechanical → cheaper tier). Ten groups have immediately startable slices; S01 (#46) lands first. **Next: orchestration workflow discussion with Trent (possible master orchestrator → per-track orchestrators), then execution.** (Workflow finalized 2026-07-02 via /loop-me grilling → `ORCHESTRATION.md` rev 2; master `rock-solid-master-4091b2` running. The workflow was then **generalized into twux as the reusable "lanes pipeline"** — `master`/`lane` roles + `resources/lanes-playbook.md` in the twux repo, commit `bf16351`; future initiatives: write a project lanes doc from the playbook template → `twux launch --role master --doc <lanes-doc>`.) **✅ ROCK SOLID FULLY IMPLEMENTED AND MERGED TO MAIN (confirmed by Trent 2026-07-05)** — all 49 slices incl. modifier nodes (18 modifiers, `modifiers/registry.ts` + `chain.ts`), modulation graph (envelope/LFO/CC nodes, `modulation-graph.ts`, compositor param sweep), colour/enum param mapping. Downstream plans may build on this infra without lane coordination.

**Design-system artifact (done — 2026-07-02, `main`):** `docs/design-system.html` — a single self-contained HTML file (JS/CSS/Geist fonts inlined, opens offline from the filesystem) **generated from the real components** so drift is impossible by construction. One source: the `/?style` route and the artifact both mount `apps/web/src/lib/styleguide/Styleguide.svelte` (restructured into `sections/`); `pnpm design-system` (root) builds it via `apps/web/vite.design-system.config.ts` (dedicated entry + vite-plugin-singlefile, NOT part of `pnpm build`). Covers: full token coverage with OKLCH + browser-resolved sRGB readouts (surfaces/borders/ink+AA ratios/accent/states/roles, type, space, radii, shadows, z-index, control sizing, overlay), **live** motion-duration/easing lanes, every `lib/ui` primitive (incl. new Splitter + MasterDetail demos), composites from the shipped code (NodeCard all kinds + states, live EffectThumbs, OutputPill, Monitor on a reactive stub running the real filter code, ReadRow/RenameField inspector rows), a **live mini graph** on the real GraphCanvas/GraphPalette/WireEdge/graph-hover, and the locked interaction contracts (graph no-lift/instant-hover/drop-anywhere, focus, keyboard, density, reduced-motion). **Click-to-copy source pointers** on every demo/token resolve through `virtual:source-manifest` (`apps/web/scripts/vite-plugin-source-manifest.mjs`, generated from the filesystem at build time — stale keys render as visible ⚠). Verified headless (Chrome): all sections render from `file://`, zero console errors, zero network requests, thumbs animate, copy yields repo-relative paths. **Process rule added to AGENTS.md:** UI work must use/extend the design system and regenerate the file in the same change (see `apps/web/src/lib/styleguide/README.md`). Purpose: cheaper implementer agents compose Rock Solid's ~30 UI slices from the system instead of designing from scratch.

**Monitor workspace navigation fix (done - 2026-07-01):** clicking the left-rail Monitor view now opens the full workspace Monitor. The rail navigation reducer was already correct; the swap was aborting because `Monitor.svelte` keyed rows only by `event.id`, and replayed server diagnostics can share IDs with locally generated monitor events. Monitor rows now use a duplicate-safe key helper, with a regression test for duplicate IDs.

**Effect perf-hog fixes + telemetry plan (done — 2026-07-05, `main`):** Haiku audit of all 41 effects found two hogs + three minors; fixed in `60123e2` (output-identical, brute-force parity tested): new `packages/core/src/geometry/pixel-grid.ts` (dense uniform spatial hash over static pixel positions, exact radius/nearest queries — string-keyed Map version was 10× slower, dense array is the point) now backs **confetti-burst** (per-particle whole-model nearest scan → grid; 12.8→1.37 ms/frame at the 4096-particle cap; in-place particle advance/compaction) and **lightning** (`lightNear` whole-model scan per bolt segment → radius query; ~0.46→0.09 ms/frame @ 3 bolts; gained `createState` holding the grid); follow-hoop/comet-trails iterate contiguous drum/hoop ranges; gravity-wells uses pre-allocated scratch. Grids live in effect state, rebuilt if `ctx.model` changes. Remaining audit MINORs left alone (wave-collapse trigger×pixel bounded; spiral/tunnel sample-array allocs). The **perf-SLA telemetry plan** (`docs/plans/perf-sla-telemetry.md`, `913eee6`) is lifted to a code-verified S01–S08 sliced spec (frame budget 8ms, voice-engine-only, Performance = new workspace view) — saved for a later implementation pass.

**Effects system review + emission contract + Gen-3 3D effects (done — 2026-07-05, `main`):** full review in `docs/plans/2026-07-05-effects-system-review.md`. Provenance: all 41 effects landed 2026-06-20 in 3 waves (Gen-0 S21 12 foundational/weakest · Gen-1 S22p1 5 · Gen-2 S22p2 24 — the good textures/particles). Structural fix: new `packages/core/src/effects/emitter.ts` (per-hit emission lifecycle: spawn-once-per-seq, dt-accumulated hit-relative age, TTL compaction, cap 64) — the multiplicity primitive weak trigger effects lacked (repeated hits stacked identical copies; voice bridge feeds ONE synthetic trigger, absolute-timebase textures render the same frame per voice). Thumbnail black/play-once root cause fixed: `effect-thumb-render.ts` pinned `seq:1` forever; now bumps per 1600ms loop so seq-gated effects re-fire. Four new emission-based 3D effects (registry 41→45): `chase-bands` (per-hit comet band racing the hoops, rev/beat + hoop-fraction width — 4 beat-spaced hits = 4 bands chasing; `twist` corkscrews), `ripple-3d` (world-space spherical wavefront crossing drums + echo rings), `spark-arc` (lofted 3D bezier spark drum→drum + landing flash; flash reach sized to TARGET drum radius, kit-fraction misses), `rain-3d` (drops through kit airspace, grid nearest-pixel, hit bursts). batch-e tests (15). OWED: ui-shot of gallery thumbs. Follow-ups: mono-bus restart-not-layer is host-side (Rock Solid doc-09 territory); Gen-0 rehab = mechanical batch on the chase-bands pattern.

**Effects Library v2 (PLANNED — 2026-07-05, NOT started):** durable plan at `docs/plans/2026-07-05-effects-library-v2.md` (symlinked into the vault as `~/TWA/Personal/Projects/LEDrums/Effects Library v2 — plan.md`; the doc's own status tracker is the resume point). Scope (7 natural work units U1–U7, restructured from 21 slices 2026-07-05, all decisions LOCKED): A) effect metadata (description/tags/deprecated + alias map) + gallery redesign (collection tabs, tag chips, param pills/filter, fake-drum pseudo-3D thumbnails replacing the raw 26×13 grid); B) rehab all pre-Gen-3 effects to the emission bar, merge near-dupes (chase→chase-bands, burst→radial-wash, patterns retired via aliases); C) **playType seam on the play node** (`hosted` | `canvas`) — canvas play type = 2D scene docs (stripes/shapes elements) sampled through drum-geometry placements (`hoop`/`strip`/`cylinder`/`footprint` samplers) with **lens** coordinate-transform chains (polar, kaleido, möbius, hyper4d 3D→4D) — extensibility by data, composes with landed modifier/modulation infra; D) ≥10 seed canvas presets + thumb QA + pattern-path deletion. 5 open decisions tabled with recommendations (block specific slices). Dev server :4321/:5173 running for ui-shot. Check Rock Solid lanes for GraphNode overlap before C1.

**Not yet built (redesign):**
- `packages/core` model refactor: Content vs Effect split + per-instance Clip presets.
- Unified-shell view internals: Patch freeform node canvas. (Setlist/show persistence ✅ done via the show document model, 2026-06-27; Kit geometry editing lives in the Patch Inspector — the standalone Kit view was removed.)

**Known issues:**
- (Resolved 2026-06-27 by the componentisation pass: the legacy panels were deleted and the 8 backward-compat token aliases dropped.)

## Routing Table

Load the relevant file based on the current task. Always load `context/architecture.md` first if not already in context this session.

| Task type | Load |
|-----------|------|
| Understanding how the system works | `context/architecture.md` |
| Working with a specific technology | `context/stack.md` |
| Writing or reviewing code | `context/conventions.md` |
| Making a design decision | `context/decisions.md` |
| Setting up or running the project | `context/setup.md` |
| Designing, restyling, or building UI | `../PRODUCT.md` (brand, register, principles) + `../DESIGN.md` (visual system, once generated); drive with `/impeccable` |
| Any specific task | Check `patterns/INDEX.md` for a matching pattern |

## Behavioural Contract

For every task, follow this loop:

1. **CONTEXT** — Load the relevant context file(s) from the routing table above. Check `patterns/INDEX.md` for a matching pattern. If one exists, follow it. Narrate what you load: "Loading architecture context..."
2. **BUILD** — Do the work. If a pattern exists, follow its Steps. If you are about to deviate from an established pattern, say so before writing any code — state the deviation and why.
3. **VERIFY** — Load `context/conventions.md` and run the Verify Checklist item by item. State each item and whether the output passes. Do not summarise — enumerate explicitly.
4. **DEBUG** — If verification fails or something breaks, check `patterns/INDEX.md` for a debug pattern. Follow it. Fix the issue and re-run VERIFY.
5. **GROW** — After meaningful work, run this binary checklist:
   - **Ground:** What changed in reality? Name the changed behavior, system, command, dependency, or workflow.
   - **Record:** If project state changed, update the "Current Project State" section above. If documented facts changed, update the relevant `context/` file surgically.
   - **Orient:** If this task can recur and no pattern exists, create one in `patterns/` using `patterns/README.md`, then add it to `patterns/INDEX.md`. If a pattern exists but you learned a gotcha, update it.
   - **Write:** Bump `last_updated` in every scaffold file you changed. If the why matters, run `mex log --type decision "<what changed and why>"` or `mex log "<note>"`.

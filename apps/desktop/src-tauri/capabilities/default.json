{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core APIs for the share + app windows, plus clipboard write for the copy-to-clipboard buttons on the native share surface.",
  "windows": ["share", "app"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text"]
}

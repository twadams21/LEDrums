{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app",
  "description": "Main app window loaded from the bundled local server. Grants minimal IPC for manual updater checks.",
  "windows": ["app"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["core:default"]
}

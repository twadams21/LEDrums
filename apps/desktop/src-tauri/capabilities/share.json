{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "share",
  "description": "Native share window: core APIs (events + the get_boot_status command) and clipboard write for the copy buttons. Scoped to the 'share' window only — the 'app' window loads the ordinary web UI over http and gets no Tauri APIs.",
  "windows": ["share"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text"]
}

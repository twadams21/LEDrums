<script lang="ts">
  /* STUB (S2) — S4e fills this pane. Registered in SettingsModal now so the S4
     slices only ever touch their own pane file. */
</script>

<div class="stub">
  <h3>System</h3>
  <p>
    Coming in this initiative: desktop app updates and the backups entry move here
    from General and the top bar.
  </p>
</div>

<style>
  .stub {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }
  h3 {
    margin: 0;
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--ink);
  }
  p {
    margin: 0;
    max-width: 52ch;
    font-size: var(--text-xs);
    line-height: 1.5;
    color: var(--text-muted);
    text-wrap: pretty;
  }
</style>
